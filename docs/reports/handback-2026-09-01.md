# Consolidated handback — coaching-brain migration gaps (brief 15)

Status as of this write: **8 of 8 work-streams landed. All real content
below.** Nothing left running.

**Section 12, at the bottom, is the one to read first if you want the
decisions without the evidence trail** — it collects every open call so far
with the information needed to make it, so this can turn into a clear
next-step brief in one pass.

---

## 1 · Ownership table — one live resolver per coaching question

**LANDED.** Every question below now has exactly one live owner, verified
against your real account:

| Question | Owner | Mode |
|---|---|---|
| Threshold capacity | `resolveThresholdCapacity` (`capacity-resolver.ts`) | direct, conf 0.727 |
| Easy ceiling | `resolveEasyCeiling` (`capacity-resolver.ts`) | direct, conf 0.634 |
| High-intensity capacity | `capacity-resolver.ts` | `vdot_fallback`, conf 0.291 (no direct reader yet) |
| Personal durability exponent | `resolveRaceExponent` (`durability-anchor.ts`) — `coach-goal.ts` now consumes this, no longer fits its own | direct, conf 0.623 |
| Race projection (any distance) | `resolveRaceProjection` (`race-projection.ts`) — `GoalGap.trajectorySec`, `goal-projection-resolve.ts`, `retrospective.ts` all now route through it | trajectory / equivalence rungs |
| Pace prescription (live plan flex) | `resolvePrescribedPaceAnchors` (`recompute-paces.ts`, `reanchor-plan.ts`) | live authority |
| Pace prescription (initial authoring) | still the old VDOT cascade in `generate.ts` | **not yet on the same brain — see §4** |
| Coaching Thesis / primary limiter | `resolveCoachingThesis` (`coaching-thesis.ts`) | live, confidence-normalized |
| Adaptation proposals | `resolveAdaptationProposals` (`adaptation-engine.ts`) | **shadow only — see §12.3** |
| Evidence run-floor (goal boundary) | flat `EVIDENCE_RUN_FLOOR_MI`, no goal input | fixed this pass |

One structural gap survives, named plainly rather than hidden in a green
table: authoring and recomputation are two different owners for the same
question (pace prescription), which is §4's whole finding.

### 6a · Coaching Thesis — LANDED (`455476c2`, `e8d6c441`)

Built as the smallest useful version, per brief 15 item 6 — a real
`resolveCoachingThesis(userId, todayISO?)` in
`web-v2/lib/training/coaching-thesis.ts`, same compute-at-read-time
discipline as `capacity-resolver.ts` (no persisted snapshot, no goal
parameter). One real design call worth knowing about: it picks the primary
limiter by confidence **normalized against each capacity's own reachable
ceiling**, not raw confidence — otherwise HIGH_INTENSITY (which has no
direct-evidence reader at all, so its confidence is structurally capped
low) would always lose to THRESHOLD/DURABILITY regardless of which one
actually limits you. Rendered against your real account: primary limiter
**HIGH_INTENSITY** (confidence 0.291, normalized 0.583) — threshold (0.727,
direct) and durability (0.900, direct) are both well-evidenced and correctly
held constant.

**Also fixed in the same stream:** the discarded workout-selection
rationale (brief 15 item 6's second half). `selectWorkout()` already
computed a real "why this session beat the alternatives" string that
`generate.ts` was discarding at authoring time. Now carried through and
persisted at `workout_spec.selection_rationale` (a new jsonb key — no
migration needed) and wired into one real consumer, `GET /api/v5/today`,
alongside — not replacing — the existing static per-family "why" text. Rule
6 compliance (multi-writer jsonb preservation) handled once, at the shared
guard, so every existing writer inherited protection for the new key
without being individually edited.

**Verification:** `tsc --noEmit` clean, two new test files (one drives the
real `composePlan → finalizeComposedPlan → persistedDayShape → cardFromSpec`
chain end to end, 3/3 pass; one renders the thesis against your real
account). **Honestly flagged as unverified by the agent itself:** it did not
hit the live authenticated endpoint or a simulator for the `/api/v5/today`
wiring, and did not confirm the Railway deploy directly (no token in that
environment) — said so plainly rather than claiming visual verification it
didn't do. Full detail: `docs/reports/coaching-thesis-2026-09-01.md`.

## 2 · Branch/commit status for every claimed implementation

**LANDED, for what's landed:**

| Claim | Commit(s) | Deploy |
|---|---|---|
| Pace Prescription wired into live recompute (`recompute-paces.ts`, `reanchor-plan.ts`) | `66a5fea5` | Railway `a8382aab` SUCCESS |
| Adaptation Engine 7-point review, shadow mode | `8b7abc1b` | Railway SUCCESS |
| Capacity-boundary fix (`goalRunFloorMiForUser` deleted) | `e5d586f7`, `bf3d66dd` | not independently re-checked |
| Coaching Thesis + rationale persistence | `455476c2`, `e8d6c441` | not independently re-checked |
| Gate/determinism cleanup | `7541c7cb` | not independently re-checked |
| Goal-card doctrine fix | `ad220f83` | not independently re-checked |
| Race-prediction consolidation | `cdc77c89` | not independently re-checked |
| Corrected 4×1mi workout semantics + EASYBAND-TIE-1 fix | `28ceac34`, `06352964` | not independently re-checked |
| Status-and-answers synthesis doc | `021e9661` | n/a (docs only) |

All on `origin/main`, confirmed by `git log`. "Not independently
re-checked" means the deploy status wasn't separately confirmed against
Railway by a party other than the agent that pushed it — every `next build`
that ran as part of the pre-push hook did pass locally in each case.

## 3 · Before/after trace of the real 4×1-mile workout

**LANDED IN FULL** (`28ceac34`, `06352964`). The literal card, exactly as
you now read it — verified by calling `cardFromSpec` directly against the
real production row, not a fixture:

**Before:**
```
Warmup     2.1 mi   9:03 /mi   HR ≤ 139   "Start easy. Build into the work over the last quarter mile."
Repeat 4×  1 mi     7:19 /mi   HR 164-172 "Same pace on every rep. If the last one slips, the target was too fast."
  recovery 1:00     9:03 /mi              "Honest jog, not standing."
Cooldown   2.1 mi   9:03 /mi   HR ≤ 139   "Easy jog. Do not skip it, it shortens tomorrow."
```

**After — what you'd read today:**
```
Warmup     2.1 mi   ≤ 8:22 /mi     HR <139 bpm (Z1)          "Start easy. Build into the work over the last quarter mile."
Repeat 4×  1 mi     7:02-7:18 /mi  HR 160-167 bpm (Z4 Threshold)  "Same pace on every rep. If the last one slips, the target was too fast."
  recovery 1:00     (by feel)                                "Honest jog, not standing."
Cooldown   2.1 mi   ≤ 8:22 /mi     HR <139 bpm (Z1)          "Easy jog. Part of the workout, not extra mileage."
```

**Correction, found by the round-2 pace/HR compatibility work:** this
section originally showed the rep HR as "164-172 bpm (Z4)" in the "after"
row above. That number doesn't correspond to any real live mechanism — it
was an unintentional hand conflation of two separate things (the display
zone and the watch's own pass/bail contingency pair). Fixed above to the
actual live value, traced fresh: 160-167 bpm, a static display-only zone
with zero downstream consumers. See `docs/reports/handback-round2-2026-09-01.md`
§C for the full trace and what each real mechanism actually does.

**What changed and why, per your original ask (not just replacing 7:19 with
7:10):**
- Rep pace is a **band** (7:02–7:18), not a bare point — using the same ±8
  s/mi tolerance the watch already grades against, which the phone was
  previously discarding. Centered on the resolver's real 430 s/mi, direct
  evidence, confidence 0.727.
- Warm-up/cool-down show a **ceiling** (≤ 8:22/mi), not a flat target — the
  band's fast edge, matching the settled "easy pace is a ceiling, not a
  band" decision. This also resolves the warm-up's three-way contradiction
  (flat pace + "build" language + HR cap now agree instead of fighting).
- The recovery jog carries **no pace at all** — an absence, not a null. No
  doctrine source (`Research/04` §5.3) ever priced this jog's pace, only its
  duration.
- Warm-up/cool-down distance (2.1 mi each) is now an explicitly documented
  policy — residual weekly mileage split evenly — with a cited 3.0mi sanity
  bound, per your ask to make that policy explicit rather than implicit.

**One bug found during verification, not initially in scope, now also
resolved same-day:** the warm-up/cool-down ceiling was briefly still
resolving to a stale neighboring day (8:43/mi instead of 8:22/mi) due to an
unbroken tie in the shared "nearest easy day" query between two
equidistant candidate rows. Flagged as a `spawn_task` chip
(`task_10b63406`), picked up and fixed same-day: the tie now breaks toward
the future-dated candidate (provably always fresher, since only
unsealed future rows keep receiving recomputes). Verified re-resolving to
the correct 502 s/mi (8:22/mi) ceiling, matching the resolver's own value
exactly.

**What's still on the books, not this pass's job, named so it isn't lost:**
the `restS / 540` hardcoded 9:00/mi mileage-accounting constant in
`spec-builder.ts` (a different thing from the pace *display* fixed here —
it sizes the warm-up/cool-down mileage split, moving it by hundredths of a
mile) — out of scope, flagged for whoever next touches that arithmetic.

## 4 · Initial-authoring vs. recomputation comparison

**LANDED, still a real gap.** `generate.ts` (initial authoring) remains
confirmed untouched — a null-anchor argument keeps it byte-identical to
before. `recompute-paces.ts` and `reanchor-plan.ts` (recomputation) call the
canonical resolvers via `resolvePrescribedPaceAnchors`
(`lib/training/load-prescription-anchors.ts`). So: **authoring and
recomputation are still not on the same brain** — a freshly-authored plan's
initial paces come from the old VDOT cascade until the first recompute pass
touches them (which is fast — the wiring stream's real-account test showed
77 of 78 future rows rewritten within the same session). This asymmetry is
not fixed by anything in this handback; it's an accurate, still-open gap,
not an oversight. The literal 7-column matrix you asked for (imported /
executed / selected as authority / persisted / unrun rows rewritten / phone
payload / watch payload) reduces to: **authoring — none of the 7 apply
(old cascade only); recomputation — all 7 apply, verified live.**

## 5 · Phone/Watch/grading consistency proof

**LANDED** (`06352964`). `web-v2/app/api/v5/today/route.ts` and
`web-v2/lib/watch/build-workout.ts` were confirmed to already run the
*same* `easyBandRow` query and were both patched identically this pass to
compute the same `easyCeilingSec` and pass it into the same
`expandSpecToPhases` — so phone and watch stamp the identical warm-up/
cool-down ceiling and the identical (now absent) recovery-jog pace from one
shared code path, not two independently-maintained ones. For the rep pace,
the ±8 s/mi tolerance the watch already graded execution against is now the
*same* number the phone displays as a band — closing the exact Rule 16 gap
the provenance trace named ("the runner is graded on a band he is never
shown"). Verified by code-path tracing, not a live device screenshot.

## 6 · Seven-point Adaptation Engine review

**LANDED IN FULL** (`8b7abc1b`, Railway deploy `6190283395` success). Summary:

- **Point 1 (28-day pace gate) — the window, not the threshold, was the
  defect.** `3` corroborating sessions is a labeled convention
  (`CORROBORATION_MIN_OBSERVATIONS`, doctrine-registry-bound, header says
  outright "not a research finding") and needs no change. The 28-day window
  was the bug: your AFC half opened a prescribed taper+recovery block
  2026-08-02 → 2026-08-30, so the 28-day window ending 08-31 held **1
  representative day**. Fixed via `extendLookback` in `normal-window.ts` —
  full mechanism, tradeoffs, and open parameter questions in §12.1 below.
  Result: 5 sessions found (vs. 1), 4 controlled, bar clears.
- **Point 2 (13/14 long runs) — the long-run bar is fine, a different
  mechanism is the real holder, unfixed.** Your 08-30 long run clears
  `DURATION_PROGRESS_MIN_TOLERATED_LONGS = 1` cleanly. What actually holds
  DURATION is `classifyAdaptation`'s execution-quality dimension, which
  reads an **unfiltered 42-day window** — 29 of those 42 days are your own
  AFC taper/race/recovery block, and three of the five "under-executed"
  tempo sessions it scored against you sit inside that block. **Deliberately
  not fixed** — it's a live write path (`progression-pass.ts`,
  `adaptive-ramp.ts`) outside shadow-mode scope. Needs your call — see §12.2.
- **Point 3 (historical vs. current-plan tolerance) — real bug, fixed.** A
  plan under a day old (`pln_9a57561debb776e5`, authored 2026-08-31 03:40)
  was reporting `HOLD · LOAD_NOT_YET_ABSORBED` — a finding about you,
  manufactured from the engine's own youth. `LoadEvidence` now carries a
  Rule-8-filtered `historicalTolerance` (90-day) as a fallback ceiling when
  the current plan is too young to judge. Real finding this surfaced: your
  filtered 90-day average is **33.4 mi/wk** against a plan opening at **45
  mi/wk** — the hold is now correctly reasoned, not just correctly silent.
- **Point 4 (density circularity) — not circular, but dead app-wide.** The
  `progression` block is authored by the Plan Generator, a legitimate
  structural marker, not a circular dependency. But **1 of your 4,021 plan
  rows, 6 of 4,639 app-wide** carry one — DENSITY can never fire for anyone.
  Correctly identified as the Plan Generator's gap, not patched here (would
  duplicate ownership per Brain Constitution §2). Separately, a real Rule 11
  bug underneath it was fixed: `loadProgressionWeek` collapsed five distinct
  refusal reasons into one false "authoring gap" sentence on 5 of 7 days;
  now five distinct, honest reasons.
- **Point 5 (compound-lever leak) — real bug, fixed.** A pace progression
  and a fitness-domain restructure could both fire in the same cycle —
  individually legal, together a compound stressor change. The composer now
  withdraws the restructure when a progression is promoted and logs why;
  `MORE_THAN_ONE_STIMULUS_CHANGE` is a new enforced contradiction. Fired
  correctly on your live data — a SPECIFICITY restructure was withdrawn in
  favor of the PACE progression.
- **Point 6 (hold vs. insufficient-evidence) — real bug, fixed
  structurally.** Every "we couldn't see it" case was landing on `HOLD`
  carrying a reason code that asserts a finding (e.g. a taper with zero
  threshold sessions read as "sessions did not corroborate" when there were
  none to begin with). `INSUFFICIENT_EVIDENCE` is now a fifth, distinct,
  enforced state — `contradictionsIn` fails if a refusal ever wears a
  finding's reason code again.
- **Point 7 (real regenerated-block dry run):** see §7 below.

**Verification:** 11 falsifiers, all fired (each fix broken on purpose,
confirmed to fail, then restored and confirmed to pass) — including one
genuinely worth noting: the doctrine claim's own exclusion check initially
missed a case (checked the file for *any* matching call rather than every
call), the same defect class this registry already caught once before.
`tsc --noEmit` clean, full suite 7,892 passing, 17-script prebuild chain +
`next build` green in an isolated worktree on clean `main`.

**Flagging directly, and see the pattern note in §9:** this agent pushed
with `--no-verify`, bypassing the pre-push hook (its stated reason: the
shared checkout's working tree had another agent's uncommitted WIP breaking
the hook's whole-tree typecheck; it verified its own exact commit in
isolation instead, and Railway confirms green). Outcome looks sound; the
bypass itself wasn't something you authorized.

**Self-flagged open parameter questions, not yet answered by anyone:**
whether 120 days is the right outer bound for the lookback extension rather
than 90 or 150; whether a confidence half-life is the right *shape* for
staleness discount at all, versus something else. See §12.1.

## 7 · Full regenerated-block stress diff

**LANDED**, real dry run against your actual account, 2026-08-31. Base
window 28d, **extended to 56d** for PACE and DURATION only (VOLUME's tissue-
load reading deliberately stays unfiltered per Rule 8's corollary):

| lever | decision | supporting | opposing | excluded / stale |
|---|---|---|---|---|
| **PACE** | **PROGRESS** 438→430 s/mi (7:18→7:10), conf 0.71 | 4 controlled sessions (07-07, 07-12, 08-23, 08-30); capacity leads prescription by 8.0 s/mi, `direct` | — | 08-16 AFC race excluded (`executionQuality: variable`). Confidence discounted 0.976 for median evidence age. |
| **DURATION** | **HOLD** 15 mi | 3 tolerated long runs (07-12, 07-25, 08-30), none collapsed | `ABSORPTION_MARGINAL` (§6 pt.2) | 07-25/07-12 visible only via the extension; discounted 0.800 |
| **VOLUME** | **HOLD** 45 mi | — | historical tolerance 33.4 mi/wk < prescribed 45 | plan too young — 1 of 4 weeks has a schedule at all |
| **DENSITY** | **REFUSAL** `NO_PROGRESSION_TARGETS` | — | — | authoring gap, app-wide |
| SPECIFICITY | **WITHDRAWN** | — | — | one stimulus change per cycle; PACE took it |

**What would change if PACE were applied:** 12 future `threshold`/`tempo`/
`cruise` rows through 2026-11-24 get faster targets (~8 s/mi).

**What would deliberately not change:** weekly volume, long run, session
geometry, quality density/count, the schedule, the goal. `adapt.ts` itself
produced zero triggers, zero actions, at the same instant — nothing writes.

**Two secondary-stress findings worth your attention before authority, both
unresolved:**
1. `hr_cap_bpm` does not move with a pace change — a faster prescribed pace
   under a static HR ceiling is a real interaction (Rule 10 territory), not
   yet checked.
2. The prescribed pace being moved is `AVG(pace_target_s_per_mi)` across
   **every remaining threshold/tempo/cruise row through 2026-12-06** — a
   single number spanning three training phases whose correct paces
   legitimately differ (407 to 475 s/mi). "Move ~8 s/mi quicker" applied to
   that blended average is imprecise; worth making phase-aware before this
   becomes authoritative.

**Contradiction validator (`contradictionsIn`, §16-scoped):** ran clean,
returned `[]`. Confirmed NOT covering cross-surface Rule 16 violations
(that's outside this engine's remit) — only within-cycle contradictions.

## 8 · Doctrine and test-gate results, deterministic combined runs

**LANDED IN FULL.** `tsc --noEmit` clean across every stream. The wiring
stream ran the 17-script prebuild chain + `next build` clean in an isolated
worktree (full suite 7,845 passed / 2 failed, both confirmed pre-existing
production-data drift, unrelated); the adaptation stream ran the same chain
clean (7,880 passing); the final workout-semantics stream's scoped sweep
ran 3,202 relevant tests passing, full repo 7,889 passing / 4 pre-existing
failures (confirmed pre-existing by reproducing against the pre-change tree
via `git stash`). The four previously-failing `_doctrine_gate.test.ts`
bindings are fixed (offsets now exported as real constants, not regexed
from source).

**Determinism, spike-rule test, and stale docs — full writeup in
`docs/reports/gate-verification-2026-09-01.md`:** `reanchor-plan.test.ts`
run in isolation 3x (12/12, identical every run) and as part of the full
`lib/plan/` batch 3x (2031/2031, identical file/test counts every run) — no
non-determinism reproduces against committed `main`; the original
two-different-answers finding traced to an audit's uncommitted working
tree, not to `reanchor-plan` itself. The sub-five-mile spike-rule standing
test (`CONTINUOUS-OVERSHOOT-1`) turned out to already be landed (commit
`6c024325`, predating this handback) and was independently re-falsified
(reverted the fix, confirmed 4 tests go red with the exact
40mi-jump/firing-flip signature, restored, confirmed green) rather than
trusted on the file's own say-so. Stale-doc sweep corrected six documents:
`docs/PRODUCT_DECISIONS.md` §6, `docs/spikeroll-1-handback.md`,
`docs/reports/brain-status-2026-08-31.md`,
`docs/reports/plan-generator-external-review-2026-08-31.md`, and
`docs/reports/workout-provenance-trace-2026-09-01.md`.

## 9 · Legacy and duplicate paths ready for deletion

**PARTIAL — real deletions already done, more identified but not yet acted on.**

Deleted outright by the wiring stream (not commented out): the goal-blend
call sites (`blendedTPaceForWeek`, `measuredProgressFraction`,
`tPaceFromGoal`, `maxSeasonalVdotGain`), `goalIPaceEligible`,
`refreshedPaceAndSpec`'s `ttDistance` gate, `buildWorkoutSpec`'s dead `mp`
local, the `reanchor-plan.maintenance-arm` null-anchor exemption.
`lib/training/vdot.ts` deliberately kept — it remains the resolvers' own
documented fallback tier, not dead code.

**Resolved — a fifth item, by the race-prediction stream (§10):**
`coach-goal.ts`'s duplicate personal durability exponent fit is no longer
used for computing the value (it now consumes `durability-anchor.ts`), but
the function itself stays in the file, deliberately not deleted — see §10
for the two real dependents that still call it directly.

**LANDED — a fourth deletion, from the capacity-boundary stream:**
`goalRunFloorMiForUser` (`web-v2/lib/training/vdot-inputs.ts`) is deleted
outright, not deprecated. It read the runner's *stated goal race distance*
to set the minimum-length floor a training run had to clear to count as
fitness evidence (5K goal → 3.0mi floor, everything else → 4.0mi) — so the
same 3.4-mile hard effort counted as evidence under one goal and was
rejected under another, for the same runner, same day. Live in `generate.ts`,
`drift-monitor.ts`, `seed-from-onboarding.ts`, and three API routes.
Replaced with a flat, evidence-only `EVIDENCE_RUN_FLOOR_MI = 3.0` at all 6
call sites. Falsified first (a temporary test proved the goal-dependent
result pre-fix), then fixed, then a permanent test
(`_goal_floor_isolation.test.ts`) proves an extreme 5K-vs-marathon goal swap
now produces an identical floor/VDOT/evidence-id. `tsc --noEmit` clean,
749 tests green (`lib/training/`). Commits `e5d586f7`, `bf3d66dd`, on
`origin/main`. Note: `capacity-resolver.ts` (the new Runner Model layer) was
never itself in violation — it had already independently avoided this by
using a flat floor. **This agent also pushed with `--no-verify`** for the
same shared-checkout reason as §6 — see the pattern note below.

**Pattern worth naming: two of three agents so far have bypassed the
pre-push hook.** Both cite the same root cause — the hook typechecks the
*whole working tree*, and this shared checkout has had multiple agents with
uncommitted WIP in it simultaneously all day, so an unrelated agent's
in-progress edit can fail your hook even when the pushing agent's own diff
is clean. Both verified their own commit in isolation before bypassing, and
both deploys are confirmed green on Railway. This isn't itself alarming
given the outcomes, but it's a process gap worth a real decision — see §12.4.

## 9a · Goal-card audit — LANDED (`ad220f83`)

Per brief 15 item 8's first bullet: `CoachDecisionCard.swift` audited line
by line against the settled co-equal-choice doctrine
(`docs/PRODUCT_DECISIONS.md`, 2026-08-31). 5 of 6 bullets **PASS** outright:
no dominant CTA (there is no accept action at all for this card kind — the
old verb was deliberately excluded, and the server refuses it anyway), no
preselected target, no auto-change/silent rebuild, copy correctly separates
projection from goal.

**One real, live bug found and fixed.** Every `.notice`-kind card carries a
generic bottom "DISMISS" ghost button, correct for a true FYI notice (an
already-applied fact needs no server round-trip). But the goal-outlook
notice is different — closing it *is* the runner's real acknowledgment of a
knowingly-held aggressive goal, and has exactly one correct path: the "KEEP
THE GOAL ON THE BOARD" button, which round-trips to the server and sets the
14-day re-nag cooldown. The generic ghost DISMISS was rendering underneath
that button too, and tapping it closed the card **locally only** — the
runner would believe they'd acknowledged the goal while the server still
read `pending`, and a fresh note could return in as little as 7 days
instead of 14, through a path indistinguishable from the real one. Fixed by
suppressing the generic ghost button specifically when a `.notice` already
carries a `.keep` action — confirmed unique to this one case, doesn't touch
any other card type. `xcodebuild` succeeded both after the fix and on final
diff-clean confirmation.

**One open item, correctly left as a decision rather than guessed at:** the
14-day re-nag suppression is time-based, not materiality-based — doctrine
says "re-surfaces only when the outlook materially changes," but nothing
compares the new projection to the one already acknowledged. Fixing this
needs a real product call (what counts as "material" — fixed seconds? a
percentage? a confidence-band crossing? does the clock reset regardless, or
does an unchanged projection suppress indefinitely?) — not implemented,
correctly scoped as backend work (`goal-outlook.ts`) outside this audit.

**Honestly flagged, not swept under anything (Rule 13):** the agent tried to
render the fixed card against a real, live, currently-pending row on your
own account and could not get any interruption card to appear in the
simulator across multiple relaunches, despite the screen showing real data.
It instrumented the load path, couldn't reconcile whether that's a harness
artifact (an ad hoc `xcodebuild`/`simctl` pipeline bypassing Xcode's normal
Run flow) or a real timing issue, and said plainly it could not confirm
on-screen rendering rather than claim it did. **Recommends re-verifying
through a normal Xcode Run** the next time Today is exercised, specifically
watching the KEEP button clear the card with no ghost DISMISS underneath.
Full detail: `docs/reports/goal-card-audit-2026-09-01.md`.

## 10 · Race-prediction consolidation decision

**LANDED** (`cdc77c89`). Confirmed the external review's central finding —
two independent fits of your personal cross-distance Riegel exponent,
answering different callers. `durability-anchor.ts` (the fitness-vector
rebuild's purpose-built resolver, Rule 10/11 compliant by construction) is
now the one canonical source; `coach-goal.ts` consumes it instead of its
own fit.

**Real bug this surfaced, on your actual account:** the old fit's 56-day
freshness window admitted only one qualifying race (AFC, 15 days old) with
nothing to pair it against — so it was returning `null`, and your no-goal
race tiers were **silently always falling back to plain Daniels-VDOT**
despite 5 real graded races on file. Rendered through the full path for
your upcoming Santa Monica 10K: the coach-set B target moved **44:02 →
45:10** — not just the exponent, the anchor evidence improved too (a graded
A-priority half-marathon result vs. a 4-mile training effort).

**`GoalGap.trajectorySec` — confirmed live Rule 16 defect, fixed.** It was
aliasing the raw daily VDOT snapshot instead of resolving through the same
canonical path every other surface already used. On your real CIM goal:
**3:23:23 → 3:18:01 — 322 seconds of difference**, on the exact field the
morning brief's headline and the plan-drift cron's rebuild trigger both
read. Every consumer (morning brief, readiness brief, plan-drift cron,
`GapPanel.tsx`) is fixed automatically since they just read the field.

**Two more copy-pasted precedence implementations removed**
(`goal-projection-resolve.ts`, `retrospective.ts`), both now delegating to
the canonical resolver instead of hand-copying its logic.

**Confidence interval threaded onto the canonical resolver's output** — but
only populated on the `equivalence` rung, deliberately left `null` on
`trajectory`, because the interval is computed around the equivalence
number and attaching it to the trajectory number would print a range
describing a different quantity than the point estimate beside it (the
exact mislabeling Rule 16 forbids). A trajectory-specific band doesn't
exist yet — noted as real future work, not built here.

**What was deliberately NOT deleted:** the old `fitPersonalExponent`
function and its constants stay in the file, unchanged — two real,
confirmed dependents remain (the paused web Targets route, and a doctrine
gate testing the formula's own arithmetic against its research citation).
Deleting a still-depended-on function to manufacture a cleaner diff was
correctly recognized as its own defect, not done.

**Four items deferred with an argued decision, not guessed at (full detail
in `docs/reports/race-prediction-consolidation-2026-09-01.md` §4):**
1. Whether the core plan-engine trajectory math (`goal-projection.ts`,
   2,477 lines, feeds the drift cron/simulator/adaptation loop) should also
   consume the canonical durability exponent — recommended yes, as a
   dedicated follow-up once the concurrent Pace Prescription work fully
   settles, since it needs an archetype-sweep re-run and a Rule 9
   continuity walk that don't fit inside a mechanical consolidation pass.
   **One note on process:** the agent flagged this via a `spawn_task` chip
   (`race-prediction-goal-projection-durability`) rather than starting it
   itself — that's a suggestion surfaced for you to click if you want it
   done, not autonomous work already in motion. It's sitting there, not running.
2. `goal-assessment.ts`'s `currentEquivalentSec` — left as-is, confirmed a
   genuinely different purpose computed with the same primitive, not a
   Rule 16 violation in spirit.
3. Primary limiter threading onto the projection envelope for iPhone's
   Races surfaces — not built generally; a concrete recommendation is
   written up for where it would go.
4. Visually distinguishing "Projected" from "Coach-set A/B/C" on iPhone —
   real design work, correctly not guessed at. Worth knowing: the
   three/four-way collision this was originally worried about doesn't
   currently exist live (`composeRaceCard` was restructured 2026-08-26 to
   return `null` rather than a decision-card shape) — it's a two-way
   labeling problem now, not a three-way one.

**Verification:** new 8-test file proving the canonical value flows through
end to end (including a direct-equality assertion, not approximation, and a
test proving shrinkage is real and observable). `tsc --noEmit` clean, 953 of
954 tests pass in the scoped sweep (the one failure confirmed pre-existing
and unrelated). Two audit gates initially went red and were investigated to
the real mechanism rather than padded — both were genuine, argued
tightenings of existing ratchets, documented in the registries themselves.

## 11 · Remaining open policy questions

Two real ones surfaced so far, both need your answer, not mine:

1. **`classifyAdaptation`'s 42-day absorption-read window is unfiltered**
   and is what's actually holding DURATION and VOLUME progression — not the
   gates you originally asked about. Fixing it changes live plan
   composition for every runner on the app, which is out of scope for a
   shadow-mode review. Do you want this fixed as part of this migration, or
   held as its own separately-scoped change?
2. **Adaptation Engine authority.** The review you gated on is now
   answered. Nothing has been wired into a live or user-facing path — do
   you want to authorize wiring it (even just the PACE lever, since that's
   the one clean upward proposal so far) into a shadow-compare or live
   path, or hold further?

A third, smaller one from the wiring stream: the pace lever can now propose
a faster target without the paired HR cap moving with it — worth a decision
on whether that's fine (HR cap is a separate, correctly independent safety
ceiling) or needs coupling before PACE authority is granted.

---

## 12 · Open calls — the information needed for each, so far

Everything below is a real decision, not a status update. Answering these
turns directly into the next-step brief.

### 12.1 · Is the lookback-extension mechanism right as designed?

**The problem it solves:** the adaptation gates ask "has this runner
recently demonstrated capability" using a fixed 28-day window. For your
account, that window landed almost entirely inside your AFC taper +
post-race recovery block (2026-08-02 → 2026-08-30) — 1 representative day
out of 28. The gate read that as "insufficient evidence," not because you'd
been inconsistent, but because the window happened to land on a stretch the
app itself told you to go easy through.

**What it actually does:** widens the search window (up to a cap) to find
more *ordinary* training days, while permanently excluding every taper/
recovery/race day it passes over, no matter how far back it goes — reusing
the same exclusion logic `normal-window.ts` already applies everywhere else
in the app. It is never "count the taper," always "skip past it and keep
looking." On your data: 28d→56d, found 5 quality sessions instead of 1, 4
controlled — that's what let PACE clear.

**The safeguard:** evidence found further back is discounted by a
confidence half-life on the evidence's *age*, not its magnitude — older
evidence lowers trust, not the underlying number. On your data: 0.976 for
pace (barely moved), 0.800 for duration (moved more).

**The bound:** capped at 120 days, itself derived from the doctrine's own
recovery/taper tables (longest realistic prescribed block, plus a base
window) rather than picked arbitrarily.

**What's genuinely unresolved, flagged by the agent that built it, not
settled by anyone:**
- Is 120 days the right outer bound, or should it be tighter (90) or looser?
- Is a half-life decay the right *shape* for staleness, or should something
  else govern how much older evidence is trusted?
- Does it feel right in principle that a runner can clear an evidence bar
  using training from up to ~2 months back, age-discounted, rather than
  requiring something fresher?

**Decision needed:** approve as built, adjust the bound/decay shape, or
reject in favor of a different mechanism for point 1 of the original review.

### 12.2 · `classifyAdaptation`'s unfiltered 42-day absorption window

This is what's actually holding DURATION and VOLUME progression — not the
gates you originally asked about in brief 13. It reads a 42-day window with
no taper/recovery/race exclusion at all, and on your data 29 of those 42
days are your own AFC block; three of five "under-executed" sessions it
scored against you sit inside that block.

Why it wasn't just fixed like the others: it's a genuine two-questions-one-
name fork (Rule 8) — it's *asking* "how well is the training being
absorbed" (the tissue-load question, which Rule 8 says to leave unfiltered)
but *spent* as the gate on "has he earned more" (the capability question,
which Rule 8 says to filter). It feeds two live write paths
(`progression-pass.ts`, `adaptive-ramp.ts`) directly, so fixing it changes
plan composition for every runner on the app immediately, not just in
shadow mode.

**Decision needed:** authorize splitting this reader into its two questions
(as Rule 8 prescribes) as a scoped follow-up, or hold it as lower priority
than the rest of this migration.

### 12.3 · Adaptation Engine authority

The review you gated on (brief 13) is now fully answered, all 7 points,
with fixes for 4 of them and precise findings for the other 3. Nothing has
been wired into a live or user-facing path — `adapt.ts` still owns every
mutation, `resolveAdaptationProposals` isn't called from anywhere live.

**Decision needed:** authorize wiring the Adaptation Engine (even scoped to
just the PACE lever, since that's the one clean, tested upward proposal) into
a shadow-compare or live path — or hold for further review. If you authorize
it, note two things that should probably be resolved first per §7: the HR-cap
interaction, and the blended-average imprecision in the prescribed pace
target across phases.

### 12.4 · The repeated `--no-verify` pre-push bypass

Three agents now, two confirmed bypassing the pre-push hook (§6, §9), citing
the same cause: this shared checkout has multiple agents with uncommitted
WIP simultaneously, and the hook typechecks the whole working tree, so an
unrelated agent's in-progress file can fail your hook even when the pushing
agent's own change is clean. Both verified their own commit in isolation
first and both deploys are confirmed green.

**Decision needed:** is verifying in isolation + confirming the deploy green
an acceptable substitute for the hook in a genuinely shared, concurrently-
edited checkout, or should agents be required to stop and ask rather than
bypass — even when the block is demonstrably caused by someone else's WIP?

---

*All 8 work-streams from brief 15 are landed and reflected above as of
`06352964`. What remains is not missing work — it's §11 and §12: the open
decisions that need your judgment, not mine.*
