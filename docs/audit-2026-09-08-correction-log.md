# Correction log — faff.run Master Status Audit (2026-09-08)

This is the material-change log for the reconciliation pass David requested over `docs/audit-2026-09-08-full-status-master-report.md`. No product code was changed producing this log or the report edits it describes — this is a documentation-only correction pass over evidence already gathered. Organized by David's own numbered brief so each change traces back to the request that produced it.

## Addendum — second correction pass, same day (2026-09-08), IN PROGRESS

David sent a second, more detailed closure brief the same day, requesting: a bounded Design-System Phase 2 audit; correcting the "clean typography compliance" overclaim and re-scoring Nielsen H4/H5; resolving the amber-`~` question per his explicit current instruction (superseding the 2026-08-21 retirement); several specific §10a release-decision-table corrections (rows 3, 6, 11, 12, 13, 15, 20, 21); tightening the evidence ledger's `PROVEN COMPLETE` claims; and one final contradiction review + consolidated commit once Phase 2 lands. **Status: Phase 2 is dispatched and running in the background (not yet complete as of this addendum); the table/ledger/tilde/typography corrections below are done; the final contradiction review and consolidated commit are held until Phase 2 (and the two new physical-evidence investigations below) complete.**

**Mid-pass, David reported two new, real, physical/production findings that were not part of his original brief and are being treated with equal urgency:**
1. **A physical-device reproduction of the outage-banner bug**, with a full captured request log (real 502s/timeouts followed by real recovered 200s that never cleared the banner). This is the first `[Physical]`-tagged evidence in this audit's history. Documented as new Exhibit E in the master report; §10a row 20 and the corresponding Risk Register row upgraded from CONDITIONAL to CONFIRMED RELEASE BLOCKER; a dedicated root-cause-and-fix investigation dispatched (in an isolated worktree, not yet merged).
2. **A live pace-increase decision card that disappeared from David's app without any action on his part**, which he wants to accept but is (correctly) concerned may indicate a bug rather than a completed action. A read-only diagnostic investigation was dispatched immediately — no write, no accept/decline action taken on his behalf pending its result — to determine whether this is a rendering bug hiding a still-valid proposal, a working-as-designed expiry, or (most seriously) something that silently applied without authorization.

Corrections applied so far this pass, item by item against David's own numbering:
1. **Design-System Phase 2** — dispatched as a background investigation (see below); top-of-document audit status changed to `TECHNICAL AUDIT COMPLETE — DESIGN-SYSTEM AUDIT PARTIAL` pending its completion.
2. **"Clean typography compliance" overclaim removed** from §13's "Can it present?" row, replaced with an explicit citation of the 14 confirmed `.system()` font sites. **Nielsen H4 downgraded from "Meets" to "Partial"** (a consistency heuristic cannot score Meets against a confirmed live inconsistency) and held open pending Phase 2's full numeric rescore, not silently re-scored without new evidence. **H5 reworded to state explicitly it covers only the sites checked, not full-catalog coverage.**
3. **Amber `~` marker — resolved, not left blocked.** Per David's explicit current instruction in this session, the amber `~` marker for modelled numbers is confirmed current product direction, superseding the 2026-08-21 retirement finding. CLAUDE.md's design-source text was correct and current all along. Converted from `BLOCKED — REQUIRES DAVID` into a scoped remaining-work item (§11 decision 5): full inventory of modelled values, which currently show the marker, whether it attaches to value/label/explanatory text, accessibility pronunciation, and affected tests/render states — explicitly not implementing the visual restoration in this pass, per David's own instruction.
4. **§10a table corrections, applied exactly as requested:**
   - Row 20's broken "#9's row above in §10" reference fixed to name the actual Risk Register row (then further upgraded to CONFIRMED per the new physical evidence).
   - Row 21's dependency corrected from "#1-#19 land" to "every YES row closed and every CONDITIONAL row resolved."
   - Row 13 (undo accounting) kept unconditionally blocking, with explicit reasoning: the Accept/Decline/Undo UI and its existing historical rows are reachable regardless of the adaptation-scope decision.
   - Row 15 (reprice rendering) already conditional on adaptation scope; the same logic is now applied consistently to row 13's neighbor rows.
   - Row 12 (race/tune-up protection) **raised from NO to CONDITIONAL** — reassessed against David's real, active three-race season; a targeted verification (which live implementation actually governs his real upcoming tune-up week) is now required before this ships, not deferred.
   - Row 11 (simultaneous phone/Watch start) **raised from CONDITIONAL to YES** — resolved by §14's own commitment to a physical dual-device smoke test, which makes the next build a runner-loop-validating build by the row's own stated logic.
   - Row 6 clarified: bundled into the same PR/batch as rows 4-5 for engineering efficiency, while remaining independently non-blocking.
   - Row 3 (`DATABASE_URL_RO`) clarified: not required before the release decision, only before trusting `audit-suite`'s findings as current.
   - §14 regenerated end-to-end from the corrected table, with the dependency order mechanically derived rather than restated as prose.
5. **Evidence ledger tightened**: confirmed no item carries `PROVEN COMPLETE` as an item-level status (all were already reclassified in the first correction pass); the remaining sub-mechanic-level `PROVEN COMPLETE` mentions are explicitly disclosed as not requiring full commit archaeology per David's own stated exemption for PARTIAL items whose sub-claims nothing else depends on.
6. **Final contradiction review, Phase-2 integration, and the consolidated documentation-only commit are held** until the Design-System Phase 2 agent and the two new physical-evidence investigations (outage-banner root cause; vanished pace-card diagnosis) complete.

**Two structural self-checks caught and fixed while waiting on those investigations, found by a fresh top-to-bottom pass over the document's own section ordering (not requested by David, caught proactively):**
- **Exhibit D11 had landed in the wrong place** — a prior edit inserted it between D8 and D9, ahead of D9/D10, even though several of D11's own entries (D11a, D11e, D11h) cite D9/D10's corrections by name. Moved to its correct position after D10, before Exhibit E, and D11o's own outage-recovery row updated to match Exhibit E's confirmed status rather than the stale "conditional" language it still carried from before this session's physical-evidence update.
- **A name collision**: the ownership/predicate prioritization table added in the first correction pass was headed "Exhibit C," which collides with the real Exhibit C (State-Predicate Ownership Inventory) that follows it. Renamed to "Prioritization synthesis, spanning Exhibits A and C" and moved from mid-Exhibit-A to after the real Exhibit C, since it synthesizes both and was citing predicate numbers before predicates had been introduced in reading order.
- D10's own body text (the "plausible, non-defect explanation" for the outage bug) was left asserting its pre-Exhibit-E framing with no inline flag; added a direct note that David's physical evidence supersedes it, crediting the original reviewer's reasoning as sound given what was known at the time rather than framing it as an error.
- §12 tier 6 still framed the outage bug as "needs a real device to settle" after Exhibit E already settled it; corrected to "fix the confirmed defect," with the (A)/backend-root-cause half kept explicitly separate and still open. Cross-referenced into tier 1 (Safety and truth) as well, since a confirmed trust/honesty defect on the app's primary screens outranks "physical verification" as a category. Also added the vanished pace-card finding to tier 1 as its own line, marked under active read-only investigation with no write action taken.

## Both dispatched physical-evidence investigations completed, 2026-09-08 — fully integrated

**Outage-banner root cause and fix (Exhibit E3a/b/c):** `SurfaceStoreV5.swift`'s generation counter advanced only on request completion, not on request start, letting a slow, late-arriving failure overwrite state a faster, newer success had already correctly set — the explicitly-documented, still-open twin of a same-day `CANCELBANNER-2` fix that named this exact gap and left it unaddressed. Fixed (bump at start, check at every completion branch); 6 new falsification tests (2 confirmed failing pre-fix, all 6 passing post-fix); full 178-test native suite green, no regressions. Not merged. §10a row 20, the Risk Register, §13, and §12 all updated from "confirmed blocker, investigating" to "done, awaiting merge approval." Added as David decision 0 (§11) — approve merge, no schema/DDL risk, no tradeoff identified. The backend (A) half — the real 502/timeout event's own cause — remains `BLOCKED — NO PRODUCTION LOG ACCESS`, disclosed rather than guessed at.

**Vanished pace-card diagnosis (Exhibit E5/E6):** the card is `plan_workout_proposals` row 12 — the exact row this report has discussed since its first version (Item 7's "one live artifact," §11 decision 4's non-atomic-SQL finding) — fully intact, `pending`, never expired/declined/superseded/auto-applied. Root cause: the card's "DECIDE LATER" control resolves in view-local state only, never persisting server-side — a risk the card's own 2026-08-17/09-01 code comments already named. **Correction to an earlier claim in this same report:** Item 7's "no rendering logic for `reprice`, falls to generic 'adjust'" finding is now confirmed false or stale — David's own correct, specific description of the card before it vanished, plus a direct code re-read, confirm it renders as a full decision card; not fully reconciled with the original "adjust" citation, flagged rather than silently dropped. **New, empirically confirmed, not merely inferred:** accepting this exact row right now would fail outright (`apply_refused`) because Migration 166 is still absent from production — verified via live query at the moment of diagnosis, not projected from code. **New, previously uncatalogued:** three more `plan_proposals` rows (a different, whole-plan-level table) sitting unresolved 5-8 days on David's own account, with a code comment stating native wiring for that whole table is not done — a possible "never reaches the phone at all" gap, distinct from the "shown then hidden" bug just diagnosed. Added to §10a as new rows 24 (YES, blocking) and 25 (CONDITIONAL, needs one targeted check), to §12 tier 1, to §14's ordered plan (steps 10-11), and to the Exhibit A/C prioritization synthesis's candidate-13th-predicate row. §10a's row/disposition counts in §14 updated from 23 to 25 rows throughout.

No writes were made during either investigation; both used `DATABASE_URL_RO` (read-only, confirmed write-incapable) or ran entirely in isolated worktrees not merged to `main`.

## Design-System Phase 2 completed, 2026-09-08, fully integrated into Exhibit D11

Rendered all 62 catalog states (up from Phase 1's 30) plus 3 tooling-added states (Add-Shoe, Account sheet, Decision History — one file, `ScreensCatalogV5.swift`, +79 lines, sitting in the Phase 2 worktree, not yet merged, flagged as a candidate for David's approval rather than applied), scored all 10 Nielsen heuristics with a real distribution across all 62 states (Phase 1 had scored 4), ran a Dynamic Type matrix (4 of 12 categories × 9 representative screens) and an iPhone-size matrix (SE3/15 Pro/16 Pro Max × the same 9 screens), and completed the terminology and iconography inventories. Two new confirmed, live defects found: `RaceDecisionCardV5`'s "Needs a decision" header renders unconditionally regardless of `card.shape` (RESTYLE, priority 1, release-blocking — added to §10a as row 27); `RouteMapView.swift`'s route-start marker uses the Easy-day green hex, commented "Success green" in its own source (RESTYLE, priority 2 — added as §10a row 28, bundled into the existing green-alias cleanup rather than an independent blocker). The amber `~` marker question is now fully specified (one function to re-enable, one redundant decoration to remove, two stale comments to fix) but not implemented, per the explicit instruction to scope only. Dynamic Type body-text scaling is reported **inconclusive**, not confirmed either way — a genuine simulator-tooling limitation, disclosed rather than asserted as a pass or a defect; this also means Phase 1's original "body text scales" claim can no longer be treated as confirmed. The training-calendar sheet remains the one uncataloguable screen (a private view property, not a catalog-only fix). Top-of-document audit status reverted from the interim `TECHNICAL AUDIT COMPLETE — DESIGN-SYSTEM AUDIT PARTIAL` split back to a single `COMPLETE`, with both disclosed-inconclusive items named explicitly rather than hidden by the status word. All stale "Phase 2 is underway/in progress" references throughout the document (§13's "Can it present?" row, §14's audit-status line) were found and corrected to reflect completion. §10a's row count updated from 26 to 28 throughout; §12 tier 5 and §14's "Full product" summary updated with the new findings.

## Follow-up, same day — David tried the suggested relaunch, it didn't work; a third investigation found the real second-layer defect

David reported a full force-quit-and-relaunch did not bring the card back, falsifying the assumption that the local-state "DECIDE LATER" theory was the complete explanation. A follow-up read-only investigation (no writes) re-confirmed row 12 unchanged, found a **second** live pending proposal on David's account (id=10, `hold`, dated 2026-09-20, never yet seen), confirmed the DECIDE LATER mechanism itself is correctly understood but insufficient to explain a post-relaunch absence, and identified the actual second-layer defect: `API+Toolkit.swift:344-351`'s `fetchWorkoutProposals()` silently converts any failed fetch (timeout, transient error, non-2xx) into an empty array, indistinguishable from a genuine zero-pending-decisions state — the same Rule-11 "don't know/zero/failed collapsed to one" shape this audit has already found on Settings and shoe-mileage, now confirmed on the coaching-decisions surface. Plausible and well-evidenced (matches the symptom exactly, and the codebase's own comments document heavy concurrent-request load at cold launch as a known stress point) but not fully confirmed without on-device or server request logs, which this investigation didn't have access to. Added as new §10a row 26 (YES, blocking); row/disposition counts throughout §14 updated from 25 to 26. Suggested a cheaper, more targeted next step for David to try (pull-to-refresh or background/foreground, rather than a full relaunch) with the confidence level stated honestly rather than presented as a confirmed fix.

## Live coach-voice composition evidence, 2026-09-08 — resolves part of Product Item 9's standing UNKNOWN

David sent two real screenshots showing a "week's longest easy run" claim that was false (an adjacent day tied it). Investigation found: the root cause is a tie-blindness bug in `generate.ts`'s per-week easy-day comparison (strict `>`, never checks for ties), confirmed firing on 63% of weeks in the project's own 8,781-archetype corpus — not an edge case, the majority shape, invisible to every existing gate because none checked the claim's truth (only repetition-count and byte-identical output). Fixed, falsified (3 new tests, fail-before/pass-after), full suite green, not merged. Checked and ruled out: no sibling superlative claim shares the bug. The fragmentation complaint was investigated separately and confirmed real but non-doctrinal (doctrine calls for short *complete* clauses, not fragments), tracing to two mechanical causes (a copy-edit gap in a prior fix's scope; a multi-pass concatenation with no connective tissue) — not fixed, named as scoped remaining work. **A genuinely important side effect, disclosed rather than hidden**: fixing the tie-blindness bug uncovers a second, real, previously-masked Rule 17 (repetition) violation the first bug had been accidentally hiding — left open pending a design decision (§11 decision 0b), not silently patched.

Added as new Exhibit F. Product Item 9 and Historical Item 12 both moved from "IMPLEMENTED — VERIFICATION INCOMPLETE" to "PARTIAL," in lockstep per the report's own one-status-per-item rule, since the live-composition half is no longer an unknown — it's now a confirmed, partially-fixed defect. §13's "Can it explain?" evidence updated to match. §10a gained two new rows (29: the fix, YES/blocking; 30: the Rule 17 collision, CONDITIONAL on a design decision). §11 decision 0 extended to cover both pending merges (outage banner + coach-voice fix); new decision 0b added for the Rule 17 design call. §12 tier 1 and tier 5 updated.

## 1. Status framing

- Removed the "STATUS: FINAL" vs. "draft pending final review" contradiction. Confirmed both required reviews (final cross-track contradiction reviewer; Exhibit D10's independent UX/IA-and-design reviewer) had actually run — their corrections are already cited throughout the report's own text (§7 items 4-7; the "corrected here after this report's own final review" language in §1/§3). The stale footer was a leftover drafting artifact, not evidence the review hadn't happened; removed and replaced with an explicit statement naming both reviews as complete.
- Replaced the single "STATUS: FINAL" line with five explicit, separate statuses at the top: Audit status (COMPLETE), Report status (CORRECTED), Product status (PARTIAL), Next-TestFlight readiness (BLOCKED), Full-product readiness (NOT ACHIEVED).
- Removed the claim that "two items remain open." That sentence was describing two things this audit's own process could not itself verify (physical/production-speed access), not the number of open product items — the report's own registers list dozens. The corrected top section states this distinction explicitly and points to §10a for the real count and disposition of each.
- Added an explicit statement that every reviewer condition was incorporated into the report's *findings*, not fixed in the *product* — this was implied but never stated outright in the first version, and the first version's "PASS WITH CONDITIONS... applied as a correction" language could be misread as "the defect was fixed."

## 2. Stale statements

- §12's Evidence Engine bullet ("no track directly checked whether that dependency is now satisfied") was stale — the bounded inspection had already run and was already reported under Product Item 7 by the time that bullet was drafted. Replaced with the actual conclusion (two incompatible classifiers, one flatline gap, neither in the sweep corpus) and named required work.
- §12's ownership-ledger bullet ("See the Brain Ownership Ledger exhibit below once it lands") was stale for the same reason — Exhibits A and C had already landed earlier in the same document. Replaced with a pointer to the completed exhibits plus a new prioritization (added as "Exhibit C — Prioritization synthesis").
- Searched the full document for "not yet checked," "once it lands," "pending review," "placeholder," "no track investigated," "to be added," "see below" — no further stale instances found beyond the two above and one benign forward-reference (Exhibit A row 5's "see below," which correctly points to already-included content later in the same document).

## 3. Item-level statuses (one per item, per the report's own rule)

- **Product Item 3 (Run execution):** IMPLEMENTED — VERIFICATION INCOMPLETE → **PARTIAL**. Historical Item 8's simultaneous phone/Watch start gap is now scoped explicitly into this item, since it directly affects recording locks, duplicate-session prevention, and handoff.
- **Product Item 7 (Adaptation):** IMPLEMENTED — VERIFICATION INCOMPLETE → **PARTIAL**. The item's own evidence (universal Migration 166 block, DENSITY dark, VOLUME unable to land cards, broken undo accounting, disagreeing evidence classifiers, pace-anchor bypasses) does not support "implemented."
- **Product Item 9 (Coaching voice):** two statuses (PROVEN COMPLETE / UNKNOWN) → **one status: IMPLEMENTED — VERIFICATION INCOMPLETE.** Sub-findings (mechanical proven, live unknown) preserved as bullets, not item-level statuses.
- **Historical Item 8:** IMPLEMENTED — VERIFICATION INCOMPLETE → **PARTIAL**, matching Product Item 3.
- **Historical Item 12:** two statuses → **one status: IMPLEMENTED — VERIFICATION INCOMPLETE**, matching Product Item 9.
- **Historical Item 14:** two statuses (PROVEN COMPLETE / BLOCKED — REQUIRES DAVID) → **one status: BLOCKED — REQUIRES DAVID.** Build-currency is now described as a completed sub-task, not the item's own status.
- **Historical Item 15:** "See §13 below" → **PARTIAL**, with a one-line rationale.

## 4. Final runner-loop verdict (§13)

- Every row now carries exactly one of YES/PARTIAL/NO/UNKNOWN — no qualified verdicts.
- "Can it build?" renamed to **"Can it build a sound training plan?"** to stop it reading as a software-build/CI claim.
- Added a new row: **"Can the current source build, test, and ship through the required release pipeline?" → NO** (backend CI red; TestFlight 44 commits behind).
- "Can it execute?": YES → **PARTIAL** (simultaneous-start gap; no physical execution ever confirmed).
- "Can it interpret?": "YES, with two confirmed gaps" → **PARTIAL**.
- "Can it explain?": "YES, mechanically; UNKNOWN, live" → **PARTIAL**, one verdict.
- "Can it operate without backend intervention?": kept PARTIAL, but the evidence column now explicitly separates (a) deliberate approval gates, (b) missing product capability, (c) manual operational recovery, (d) one-off stale-data repair — these were previously collapsed into one undifferentiated PARTIAL.

## 5. Release-classification contradictions reconciled

- **Migration 166:** the report previously called it both a high-severity safety/correctness blocker and "does not block release" with no scope statement reconciling the two. Reconciled: it blocks any release that claims adaptation-apply is operational; it does not block a release that explicitly excludes adaptation-apply, provided honest-refusal behavior on visible decision cards is confirmed (flagged UNKNOWN — the one item this correction pass could not resolve from existing evidence). Approval is no longer recommended from the report's own summary alone — a full migration packet (SQL, locking, timeouts, backfill, deployment order, rollback, `DATABASE_URL_RO`-cannot-execute-it proof, independent review) is now a prerequisite, stated in §11 decision 3.
- **Dormant `effective-race-target.ts` field:** Product Item 11 called it a current-release blocker; the Risk Register called it non-blocking. Reconciled to one classification, used in both places: "non-blocking correctness debt; must be fixed, removed, or guarded before any consumer is added."
- **Shoes DB-error swallowing:** reclassified from "non-blocking polish" to "low-severity correctness/honesty debt" with an explicit disposition (does not block the next internal TestFlight; should not be carried to a broader release).
- **Strava webhook reconcile:** reclassified from "CI/release-integrity blocker" to "reliability/operations gap (Rule 23 shape), not a CI defect," with an explicit disposition (acceptable for the next internal-only TestFlight given existing per-event alerting; required before any broader beta).
- **Today/Block/Races outage-recovery:** "unconfirmed severity — could be release-blocking" → **"CONDITIONAL BLOCKER — MUST REPRODUCE AGAINST PRODUCTION SPEED,"** a disposition rather than a hedge.

## 6. Next-release scope rebuilt (new §10a)

Added a new section, §10a, with a 23-row release-decision table covering every candidate blocker named across the rest of the document (CI failures, identity bypasses, Settings defects, HR-flatline, travel misreading, simultaneous start, Migration 166, undo accounting, reprice rendering, Row 12, all four confirmed release-blocking UX/design defects, outage-recovery, TestFlight lag, physical verification, Strava). Each row carries severity, confirmed/suspected, reachability, runner impact, safety impact, required fix, dependency, owner, and an explicit YES/NO/CONDITIONAL disposition for the next internal TestFlight, with rationale. This replaces the first version's narrower five-item "smallest defensible scope" in §11, which the review correctly identified as omitting more than a dozen items this same report called blockers elsewhere.

## 7. UX/IA and design-system exhibits integrated (new §D11)

Added Exhibit D11 (screen/state inventory, palette/token compliance, typography compliance including the 14-site `.system()` font violation, spacing/geometry — disclosed as a gap, not audited — six-gradient compliance with the shared-hex/`ThemeV5.swift` exemption note, signal-orange — disclosed as not separately audited, label-grammar — partial, green-as-grade violations with the explicit live-vs-legacy scope boundary, component inventory/duplication map with the same scope boundary, a Nielsen matrix stating plainly that only 4 of 10 heuristics were assessed (H2/H3/H6/H7/H9/H10 are UNKNOWN, not assumed to pass), iOS convention findings, an honest disclosure that no full Dynamic Type/device matrix was built, a terminology dictionary (partial), an iconography inventory (partial), and a merged KEEP/REFINE/RESTYLE/UNIFY/REMOVE/CREATE register with five-tier priority). Corrected D8's evidence language to distinguish render-sample, render-with-production-data, source-inspection, and production-query evidence types rather than treating a source trace or a DB query as equivalent to a render.

## 8. Modelled-value tilde conflict

Identified and did not silently resolve: CLAUDE.md's own current design-source-of-truth text still names the amber `~` mark as locked, current handoff language; this audit separately found the mark deliberately retired 2026-08-21 and the code correctly implementing that retirement, with two documents (a header comment and the external design-contract doc) stale against it. Classified `BLOCKED — REQUIRES DAVID` and added as David decision 5 with five explicit sub-questions, rather than assuming the code's current behavior is the intended one.

## 9. Evidence ledger completed (§6)

Replaced the "selected load-bearing items" framing with a complete ledger: the original 10-row load-bearing table (retained, unchanged, as §6a) plus a full pass over all 27 Product items (§6b) and all 15 Historical items (§6c), each with whatever commit/reviewer/test/production/physical evidence exists in the document — cells with no independently captured evidence are marked "—" rather than padded.

## 10. Evidence-language precision

- "No physical-device verification of anything has ever been closed... at any point" → "No documented physical-device verification was found in the repository, reports, and records reviewed... this audit can prove the absence of a record, not that no physical action was ever privately taken" (§1, §2, Historical Item 8/14, §13).
- Added a new "how to read every evidence claim" note (before §2) distinguishing deploy-success-proves-deployment, HTTP-reachability-proves-the-checked-endpoint, code-presence-proves-implementation-not-runtime-behavior, a-test-count-proves-its-own-assertions, and a-render-is-not-physical-verification — five caveats that were previously implicit or stated once and not consistently applied.
- Specified the exact 5 endpoints behind the previously-unqualified "every checked endpoint" Railway-reachability claim.

## 11. Remaining internal inconsistencies (David's numbered list)

All 15 items in David's own "resolve these explicitly" list are addressed by the changes above; the two not otherwise covered:
- Product Item 19 (onboarding UNKNOWN) vs. D10's "onboarding was not under-covered" — clarified as two different claims: render-catalog coverage exists (5 entries + full Settings render); complete onboarding-flow *behavior* was not audited. The UNKNOWN now names the flow specifically.
- The "no component duplication" claim was already scoped to "the live layer" in most instances on inspection; Exhibit D11i now states the scope boundary explicitly and names the compiled legacy exceptions (two Settings implementations, `ActivityView`, `TodayView`) so the boundary isn't left to inference.

## 12. CI remediation language

- Replaced "raise the skip ceiling from 120 to 205" with a category-aware policy (credential-blocked / DB-dependent / expected-failure / explicitly-disabled / unexpected-and-uncategorized buckets, each ratcheted independently, failing only when the unexpected bucket grows).
- Added explicit `DATABASE_URL_RO` scoping requirements (least-privilege, trusted-workflow-only, no log exposure, both credential-present and credential-absent paths independently tested).
- Replaced "decide bump vs. re-pin" for `_belief_source_pins.test.ts` with an explicit decision procedure (determine whether behavior actually changed before choosing either path; independent reviewer sign-off required either way).
- Added a requirement that the format-lint fix branch (`e46eab476`) be independently re-reviewed and the full suite re-run before merge, since it predates 44+ commits of drift and its original approval cannot be assumed to still hold cleanly.

## 13. Architectural findings preserved

No architectural finding was deleted or downgraded in this pass — Exhibits A, B, C, and D and their sub-findings (7-8 adaptation mechanisms, two evidence classifiers, 20 belief rows, 12 state predicates, 6 dedup strategies, 5 race-protection implementations, the runtime-diffing-vs-static-registry recommendation) are all still present. A new prioritization pass was added (Exhibit C's synthesis table) so they carry explicit disposition without being flattened into the next-release scope.

## 14. Systematic cliff-sweep recommendation

Unchanged in substance (still a bounded, multi-day, two-stage architecture-quality task, not scheduled as part of the next release) — Exhibit B and §12 tier 3 already stated this correctly in the first version; no material correction was needed here beyond what's already reflected in §10a's disposition of the sweep as "NO — deferred."

## 15. David decision list rebuilt (§11)

Replaced the first version's 7-item list with a 12-item list containing only genuine product/production decisions, in dependency order: next-release scope (pointing to §10a), whether adaptation-apply is in scope, Migration 166 approval (gated on the new migration packet), the row-12 fix, the amber-tilde conflict, `is_peak`/`selectionRationale`/phase-answer fields, `RunLogV5` feature parity, VDOT placement, Health screen timing, HealthKit dry-run sequencing, adaptation-mechanism consolidation timing, and notification-history timing.

## 16. Deliverable and artifact preservation

This log, the corrected master report, and the two Pass-1 raw reports are being committed together in one documentation-only commit (no application code) once this correction pass is confirmed complete, so this correction — like the report it corrects — does not become the next untracked "final" record. See the commit referenced in the session's own closing message for the exact SHA.
