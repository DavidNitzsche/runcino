# v1 → v2 Delta Log — faff.run Historical-Run Data Forensic Audit

Organized by the 10 items in the correction brief. "v1" = the original consolidated handback pinned to `8559245496b...`. Evidence tags are carried from the six correction passes; nothing here upgrades a tag's confidence beyond what its originating pass supports.

## 1. Today-run settlement (Part 1 — today-run agent)

- **§9's central dispute (`sectionPieces` vs `workoutPhasePieces`) is SETTLED as to mechanism** — both v1 sides (original diagnosis and Domain D's reviewer) were arguing about the wrong code path. The real explanation is a **third, separate Swift component**, `workoutPhasesTile`/`phaseTrailingText` (introduced by WORKOUTPHASES-1, 2026-09-04; deleted by `154edbf97`, 2026-09-08), which is **pace-blind by wire-type design** (its `V5WorkoutPhase` struct has no pace field at all) and stamps the literal string `"not completed"` for **any** `completed:false` phase of **any** type — independent of whatever `sectionPieces`/`routePhases` produce.
- **[RENDER]-grade confirmation obtained** (new for v2): a live call to the actual current-`main` `/api/v5/today` route, via the documented walk-substrate harness against a real copy of today's row, confirms `routePhases` is phase-keyed (14/14 entries, matching every phase), every recovery phase carries a real non-null pace, and the literal string `"not completed"` occurs zero times in the response body. This confirms Domain D's reviewer was correct about the *server/current-client* mechanism specifically.
- **[SOURCE]+[PROD-QUERY] confirmation** that build 290 — the highest build ever uploaded to App Store Connect, confirmed live via the ASC API, distributed ~40h before the run — predates `154edbf97` by ~18.5 hours, so RUNNER_DAVID's phone was still running the old, pace-blind `workoutPhasesTile` at the time of the run. Feeding today's real raw phase data into that deleted component reproduces the reported symptom exactly.
- **New, more consequential correction, supersedes v1's P0 framing:** the recovery-duration tolerance check that v1 said "denies today's run the `'executed'` verdict" is traced, at the exact call site, to resolve `prescribedSec` via `workout_spec.rep_rest_s` — a field **absent** on this strides-shaped spec (only the unrelated, unread `strides_recovery_s=60` exists). So `prescribedSec` is `null` for all 6 recoveries, `recoveriesHonestOf` returns `null` (not `false`), and the check **does not block** `'executed'`. Verified by a standalone script replicating the exact logic against the real data, and corroborated by the live render's own positive narrative ("Easy run stayed controlled. Six strides completed."). **This directly conflicts with the current-main reconciliation pass (item 10 below, Part 9 §2d), which re-derived the same check assuming `prescribedSec=60` and concluded the run remains denied** — flagged as an unresolved cross-pass disagreement in v2 §13, resolved there in favor of the more rigorous, code-verified trace.
- **Stride/recovery pace evidence, four sub-questions answered separately:** (a) raw distance/duration exist for all 14 phases, including the 8-second walk-back; (b) average pace is trivially computable and matches the watch's own stored value; (c) finer intra-phase metrics (sub-splits, cadence-pace curves) are genuinely unsupportable on the shortest phases — as low as **1** sample on an 8-second phase — a real sampling-density limit, not a bug; (d) the UI's pace suppression is real and structural on both the old (build-290-live) and new (post-WALKBACK) components, correctly reasoned for a treadmill belt but applied unconditionally to outdoor GPS phases too.
- **Deliberate, disclosed stopping point:** no actual Swift/simulator render of build 290 itself was performed (git-history proof judged sufficiently unambiguous relative to cost); a fixture + reproduction instructions were prepared for a future pass.

## 2. Phase-table math (Part 1, Part 3 — today-run agent)

- v1's table had a genuine **arithmetic error** at idx 2 (stated Δ=36; actual is `|30−60|=30`) **and** a **boundary error** (the predicate is `<=`, inclusive — a Δ of exactly 30 passes). Corrected: **idx 2 is PASS, not FAIL.**
- Mechanically recomputed (standalone script, assuming `prescribedSec=60` for all six — see the caveat below): **4 pass (idx 2, 4, 6, 10), 2 fail (idx 8, 12).** v1's *prose* ("2 of 6, phases 8 and 12") was already correct by coincidence, even though its own table cell was wrong.
- **Critical caveat carried from item 1:** this corrected table assumes the code reads `prescribedSec=60`. It does not, for this run — see item 1. Both facts (the corrected hypothetical arithmetic, and the actual live resolution to `null`) are now presented side by side in v2 §2/§9 rather than one replacing the other.

## 3. 14-day cohort truth table (Parts 3+4 — Domain-B completion agent)

- v1 had 5 hand-picked named cases plus aggregates. v2 §2 now carries the **full, systematic 13-row per-run table** (not 11 — v1's implicit `startUtc` windowing silently drops 2 treadmill rows; `data->>'date'` is the only safe windowing key for this schema, confirmed empirically by the drop itself).
- **New:** two independently-coded plan-match distance bands (`[0.7,1.3]` symmetric vs `[0.7,2.0]` asymmetric) live in two different routes; only one received the OVERRUN-MATCH-1 fix; the other (`watch/workouts/complete`) never did — still true at current `HEAD`.
- **New:** 4 cohort rows carry `planWorkoutId` dated *before* its only documented writer's stated go-live (2026-09-03) — points to an untraced backfill, not confirmed.
- **New:** a live `elevGainFt`/`elevGainSource:'absent'` contradiction (8/162 account-wide) that the sanitizer's own code has no path to produce — a new Rule-6-shaped instance, applied to a field *pairing* rather than a single field.
- **New:** a traced (not asserted) answer that the HR-flatline guard does **not** block VDOT eligibility for the flatlined 09-03 treadmill session, because its `intervals` type label satisfies the honesty gate independent of HR — a genuinely different consumer than the LTHR/readiness question v1 already answered.
- **New:** two previously uncatalogued auto-applied plan changes dated inside the cohort, including the origin of RUNNER_DAVID's *current* active plan (a 09-03 `silent_rebuild`, engineer-dispatched, not runner- or drift-triggered).
- **New:** a second, larger (2.28 mi / 1215 s) unreconciled phase-total gap (08-26), distinct in shape from the known walk-back class.
- **New:** a long-run "hit" vs. "missed" contrast (09-04 vs. 08-30, materially similar overperformance, opposite stored verdicts) and an easy-day-too-fast "missed" reading (08-26) — concrete, real instances of CLAUDE.md's own Rule-21 asymmetry concern, surfaced from stored verdicts rather than argued abstractly.

## 4. Data-quality counts (Part 3+4 — Domain-B completion agent)

- Full, queried counts now exist across every requested category, replacing v1's spot-check register: missing GPS/route (7/13, 5/11 outdoor), missing stored pace field (6/13, 0/13 non-derivable), implausible pace (0/13 by an absolute bound), flatlined/stale HR (1/13, matches the known case, confirmed isolated not systemic within this window), missing/implausible cadence (2/13 missing, both treadmill; 0/13 implausible), elevation discontinuity (`[BLOCKED: no per-point stream]`, closest proxy quantified), unmatched/duplicate prescriptions (4/13 unmatched, 1 duplicate-match case), supplemental runs (2/13, both correct by design), phase-total reconciliation (3/7 phase-bearing runs do not reconcile, including a newly-found second, larger gap class), pause effects (8/13 real submissions nonzero, 0/13 canonical rows retain any of it), HealthKit/Strava disagreement (0 material on distance, 3/10 comparable groups >60s on duration — one exactly explained by the discarded-pause mechanism, i.e. the same defect counted twice under two names).

## 5. Field-lineage completion (Part 5 — field-lineage agent)

- **13 new field-lineage rows added**, none in v1's original matrix: route/GPS points (plus a discovery of an orphaned, richer, 24-row legacy `workout_routes` table, dead since PORT-1); per-sample pace/speed (46/162); cadence raw samples (confirmed genuinely **never persisted** past a transient HealthKit query — 0/162, a stronger gap than "discarded downstream"); HR sample timestamps/freshness (no explicit field exists — only a symptom-detector); mile splits plus 3 sibling confidence flags (`splits_unreliable`/`splits_validation`/`splits_source`) never given their own row before; phase-boundary timestamps (confirmed absent entirely, client- and server-side); pause/resume events (two independent pipelines, both discard the discrete event before persistence — see item 7); Skip/Next/End transition events (mostly indistinguishable from timer completion; one narrow, brand-new, still-unfired exception); shoe identity + cumulative mileage (the stored `shoes.mileage` column independently re-confirmed stale on 6 of 8 shoes, up to ~12× off, one shoe inverted, one marked retired with zero canonical runs ever assigned); treadmill speed/incline (runner-typed, not measured — confirmed exhaustively 40/40 phases); perceived effort/RPE (a live table feeding real coaching consumers including the Evidence Engine and adaptation load — entirely absent from v1); weather (full object traced — a genuine two-provider apple_hk/open-meteo split found); completion/verdict state (a new `execution/verdict.ts` resolver landed between the pins — explicit recompute posture for the phase-level verdict; still no run-level stamped verdict); confidence/data-quality flags (a previously-missed `runs.provenance` per-field source-attribution column, 148/162 populated).
- Every overlapping v1 count (`data.source`, `elevGainSource`, `weather.dewpointF`) independently re-derived and came back byte-identical — nothing in v1's original rows needed correction, only addition.

## 6. Source-distribution labels (Part 5 §2 — field-lineage agent)

- `data.source` distribution re-run and **labeled** (v1 gave the raw counts with no category names): *(absent)* 88/30.7%, `apple_watch` 69/24.0%, `watch` 56/19.5%, `strava` 33/11.5%, `apple_health` 21/7.3%, `strava_webhook` 11/3.8%, `treadmill` 9/3.1% — sums to 287, byte-identical to v1's unlabeled figures.
- **New secondary finding:** `apple_watch` and `watch` are two distinct string values, both plausibly meaning "this row came from an Apple Watch," coexisting with no canonical spelling (125 rows total) — flagged as a cheap follow-up (which writer, if any, still forks), not chased to a conclusion.

## 7. Pause/gap end-to-end trace (Part 6 — pause/gap agent)

- Full hop-by-hop trace completed (capture → wire → route → storage → absorption-merge → grading → UI), replacing v1's summary mention. **Three Swift senders identified, not two** — the live Watch app's own Pause/Resume control (`WorkoutEngine.swift`, confirmed a genuinely-compiled, currently-shipping target via a real symlink, not orphaned legacy code) is the third and, empirically, the one actually firing.
- **The exact drop point, quantified for the first time:** the ingestion route's `clockAudit` closure discards `pausedSec` **unwritten** the moment it correctly explains the wall-clock gap — i.e., exactly the success case a coaching product would most want recorded. Confirmed empirically: **all 8 of this account's real nonzero-pause submissions ever** (2026-08-26 to present, 2–1619 seconds) resulted in a matched `runs` row with `clockAudit` **entirely absent** — zero exceptions, including today's own run (`pausedSec:281`) and the account's longest recorded pause (27 minutes, 2026-08-30).
- Secondary, independently-sufficient drop point: `postrun/load.ts`'s type-level destructuring never carries `pausedSec`/`declinedSec` even on the rare row that survives the first drop.
- The two in-repo comments asserting "no Swift file sends `pausedSec`" (already flagged false by v1) are corrected **precisely, in three separable ways**: undercounted senders (2 named, 3 real); an empirically false "always zero" claim (falsified by real data since 2026-08-26, before either audit pin); and — the part neither original comment stated — the *consequence* they draw happens to still be safe advice today, for the wrong reason, and will stop being safe once someone "fixes" the false premise without also fixing the ingestion-gate drop.
- Grading (`execution-semantics.ts`/`verdict.ts`) confirmed to have **zero** wiring to `pausedSec`/`clockAudit` at any point — not stripped, never connected. A pause during a tempo or long-run segment has no grading-layer representation at all, gated or otherwise.
- **New, latent (not yet observed) structural exposure:** `clockAudit`/`pausedSec`/`droppedGapSec` are absent from `canonical.ts`'s `NEVER_COPY` set, so a future second writer to these keys could be clobbered wholesale on absorption — a Rule-6 shape waiting to fire, not yet fired (zero canonical rows show evidence of it happening).
- Runner-facing consequence restated with precision: **no product surface, at any layer, has ever told RUNNER_DAVID how long he paused on a run where he genuinely paused** — not live, not post-run, not in grading — including as recently as this morning's run.

## 8. Remaining unknowns chased (Part 7 — unknowns agent)

- **`recoveryExtensions`/`ceilingLift` consumers:** v1's "no confirmed consumer, collected but unused" claim is **FALSE**. A real, live, five-file-deep, independently-corroborated consumer chain was found: `RunDetailV5.swift`'s "Wrist Decisions" panel, reached via `/api/runs/[id]` → `loadRunDetail`. Moves from "collected but unused" to "used, wired, render not yet performed."
- **12.1/4.9 → 12.0/5.0 rounding gap:** still genuinely unresolved, but **6 specific candidate mechanisms individually ruled out with direct evidence** (no audit-trail table exists; no later mutation/proposal/rebuild touches these rows; the accept-path writes unrounded values; none of the 3 live `distance_mi` writers produce this shape; the legacy `web/` tree live at the time no longer exists in-tree; a prior reviewer's "EVENT-2" guess does not exist anywhere in the codebase).
- **7 run IDs from the 2026-08-30 incident:** **FULLY RESOLVED.** Recovered from the user's own memory store (outside the repo), confirmed by exact ID against production: 7 rows, exactly 63.0 mi, all still un-absorbed, all covered by the repair-verification predicate.
- **All 6 races reaching the Races/PR payload:** **RESOLVED.** v1 conflated two different screens. The schedule/history screen includes all 6 unconditionally by construction (no filter exists that could drop any). The bucketed PR screen correctly shows only 2 (fastest-per-distance-bucket) by design — not a defect.
- **Proposal 13's actual wire text:** fully traced and quoted verbatim. **The rendered card never states any individual anchor's direction at all** — only a single threshold-lever headline, a net "push" badge, a reason sentence, a session count, and a two-sentence evidence detail. This resolves the "was the audit wrong or was the card wrong" question decisively: it was the audit's prose; the card was never at risk because it doesn't render that data at all — though it surfaces a genuine, unresolved UX-design question of its own (see v2 §13).
- **"14 PROGRESS outcomes":** **RESOLVED and reproduced live**, via a still-existing, DB-free replay harness (`scripts/adaptation-real-replay/real-replay.test.ts`) — a different mechanism entirely from the three tables v1 correctly checked and found empty. Re-run this pass: `PROGRESS 14 / HOLD 64 / REGRESS 4 / REFUSE 38`, byte-identical to a 2026-09-04 closure document. **Classification changes from "genuinely open" to VALID-BUT-DIFFERENTLY-SCOPED** (not the fallback "invalid/unsubstantiated" — a real source was found): it is a capability/replay metric ("what the engine would have proposed against his real history"), not a production log, and the three production tables correctly show nothing because none of them is what "14" describes. Of the 14 hypothetical proposals, arbitration would have let through only 1; the other 13 were suppressed by an already-documented weekly-cadence rule.

## 9. Redaction note

- All personal identifiers (name, email, literal `user_uuid`) removed from both deliverables; the runner is referred to as **RUNNER_DAVID** throughout, matching how all six correction passes already wrote their output. Run IDs, plan IDs, workout IDs, and commit SHAs are retained as non-personal identifiers, per instruction.

## 10. Current-main delta table (Part 9 — main-reconciliation agent)

- **New section added to v2** (§11a) — v1 had no such table. Reconciles every "unmerged"/"pending" claim in v1 against the actual live tip, which moved a further time during this very pass (to a docs-only commit) — the exact CLAUDE.md-warned pattern, disclosed, not acted on.
- **WALKBACK-1: now MERGED** (`1a26aae87`). v1's "REVIEWED — MERGE PENDING" language does not survive into v2 unqualified.
- **WALKBACK-2: now MERGED** (`e5bcc430b`), but with a real split that must be stated explicitly: the **server (TS) grading logic is merged** (Railway deploy status itself not independently confirmable in a read-only pass — disclosed gap, not assumed); the **Swift/watch capture code for `recoveryEndedEarly` is confirmed absent from every TestFlight build ever shipped** (build 290 predates the merge by two days; no ship commit exists after it). Today's specific run's stored data lacks the field either way and is unaffected by this merge in that sense — though see item 1's conflicting finding on whether the underlying tolerance check needed this field to begin with.
- **`SHADOW_EVIDENCE_EPOCH` pin mismatch: FIXED** (`52c5e8dd0`), independently re-recomputed via SHA-256 against both the task's pin and the live tip. v1's "live, reproducible mismatch" language does not survive.
- **`plan_decision_ledger`/Migration 166: UNCHANGED**, still absent from production — freshly re-queried this session.
- **`coach_intents` zero-upward (Rule 21, narrow scope): UNCHANGED** — still 321 rows, zero upward reasons, freshly re-confirmed.
- Two commits (`38b33f0f5`, `49be10229`) confirmed genuinely still unmerged, but flagged as belonging to a **separate, concurrent audit thread** — not part of this audit's own finding set; included only because the task instructed verification.
- **§9's central dispute:** the main-reconciliation pass itself reported this as "no commit in range touches this; still unresolved" — but that conclusion is **superseded within this same v2 pass** by the today-run agent's deeper trace (item 1), which did settle the mechanism. Flagged explicitly in v2 §13 as a case where two of the six correction passes reached different depths at different times; resolved in v2 in favor of the deeper, render-corroborated trace.
