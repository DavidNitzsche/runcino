# 08 — Device Verification

**Not started.** This container has no iOS simulator or physical-device access. Every item in the mandate §17 checklist requires a session running on hardware capable of it (the pattern established by prior sessions' handbacks, e.g. natural-coaching's round-3 handback, which explicitly marked most render paths `BLOCKED` for the same reason and refused to fake them against `ScreensCatalogV5` per CLAUDE.md Rule 13).

This ledger will track device verification once a Mac/simulator-capable session is dispatched or resumes work; until then every device-dependent claim from any prior report is treated as unconfirmed by this session, not as false — the distinction matters (Rule 11: "don't know" / "measured zero" / "read failed" are three different facts).
