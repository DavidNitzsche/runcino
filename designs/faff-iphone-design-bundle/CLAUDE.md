# Faff Web — project rules

## Writing style
- **Never use em dashes (—) anywhere** in copy, UI text, or labels. Ever.
- Use periods, commas, or the middot separator (·) instead.
- En dashes (–) are fine for numeric ranges (e.g. "May 19–25").
