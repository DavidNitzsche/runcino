# BA PROGRAMME CONTROL — David's execution mandate, 2026-09-15, checked at least hourly

**REAL STAKES, David direct, verbatim, 2026-09-15 — see F207: "You have one chance to get this app up and running and smooth with no errors or I'm pulling it."** Not routine pressure — the last chance before he walks away from the app entirely. **The correct response is MORE rigor, not less.** Nothing about the standing discipline below changes: no partial pass gets described as done, no merge gets called shipped before Railway/TestFlight actually confirm it, and BA-01R's exit gate (David's own words, below) is not relaxed under this pressure and is not treated as satisfied by momentum alone. This is exactly the wrong moment to cut a corner to move faster.

**RESUMED, David direct, 2026-09-15 — see F199.** A full stand-down (F198, all agents/simulators/builds stopped, verified) was lifted the same day: *"keep pushing now to get the app up and running as planned. I want the backend firing and workign correctly so the app is not buggy and works as fast as humanly possible."* BA-01R's own pacing question (F196) is resolved by this — it starts now, in its own fresh worktree, not picked up cold later. Standard restated: genuinely finished, not another partial pass. Production spot-checked on resume: `/api/up` returns 200 healthy, latest Railway deploy (`26441ac8a`) SUCCESS, no new unexplained failures since F195.

**Governing documents, both authored by David directly**: [`BACKEND-DATA-BRAIN-APP-ARCHITECTURE-BRIEF.md`](../../for%20backend%20architecture%20review/BACKEND-DATA-BRAIN-APP-ARCHITECTURE-BRIEF.md), [`ASAP-IMPLEMENTATION-SEQUENCE.md`](../../for%20backend%20architecture%20review/ASAP-IMPLEMENTATION-SEQUENCE.md). Full instruction and ruling in register **F165**. **No unit advances on prose alone — measured evidence only.** No owner stops after reporting; if the next step is unblocked, they begin it.

**PROGRAMME RESTRUCTURED, David direct, 2026-09-15 — see F193.** Four new governing documents (`WHOLE-BACKEND-MASTER-PLAN-AUDIT-2026-09-15.md`, `BACKEND-IMPLEMENTATION-SCOPE-AND-SETUP-2026-09-15.md`, `BACKEND-PROGRAMME-MANIFEST.json`, `00-INDEX.md`) supersede the old flat BA-01→BA-05 sequence below. **Read all four in full before acting on this section — do not work from this summary alone.** New unit sequence: BA-01R → BA-00A → BA-00B → BA-02A → BA-02B → BA-02C → BA-02D → BA-03A → BA-03B → BA-03C → BA-04 → BA-05. The informal completion bar this board tracked all night (F173/F175) is now formally BA-01R's own exact required proof, quoted below — same substance, now precise and file-scoped.

**BA-01R's exact required proof, from `BACKEND-IMPLEMENTATION-SCOPE-AND-SETUP-2026-09-15.md` (supersedes the informal 5-point bar, same substance):**
1. Cold shell launch: at most two runner-state requests.
2. Hidden Block/Races/Run hosts: zero live request until selected or specifically invalidated.
3. Thirty in-snapshot date taps: zero requests.
4. Seven rapid foreground signals without a version-changing event: at most one conditional refresh, no repeated snapshot build.
5. Retry: exactly one owning request.
6. Every snapshot generation has one recorded reason.
7. Server returns its structured deadline envelope before the client timeout.
8. A forced server timeout on active networking never renders offline copy.
9. Physical build trace shows Today/Block/Races/Plan Snapshot/Watch within agreed transitional budgets, no 12-second client timeouts.
10. Matching Railway evidence records route stages and pool headroom.
**Exit gate, David's own words: "Do not close BA-01R until the real-device reproduction is green. Passing unit tests alone is insufficient."**

**FORMAL VERDICT, David direct, 2026-09-15 — see F186 for his literal words in full (confirmed identical from two independent sources): build 303 is a FAILED BA-01 physical-device acceptance test, not a partial pass.** Nine numbered instructions: trace the 3 correlation IDs through Railway telemetry; explain the sync-generation climb to 7; audit remaining request duplication; implement BA-01's still-missing item 9 (server deadline envelope); measure F164's real effect; fix the root cause, add regression coverage, and wire the storm-regression gate into mandatory release; get independent re-review with documented falsification; ship clean; correlate the next test with Railway evidence. **Explicit, standing, binding prohibitions**: do not ask David to reproduce again until the above is done; never describe BA-01 as "fixed" or "mostly fixed"; BA-02 does not start as a substitute for finishing BA-01; no Cloudflare, no paid capacity increase, no longer client timeout authorized as a fix.

**GOVERNING ROOT-CAUSE DOCUMENT, David direct, 2026-09-15 — see F189, `for backend architecture review/BA01-CODE-FORENSIC-2026-09-15.md`, spot-checked and confirmed exact against the real code.** Five confirmed causes: (1) the real app shell mounts Today/Block/Races/Run simultaneously, hidden via opacity not removal — BA-01 never touched this; (2) interactive routes still recompute too much live (`/api/v5/plan-snapshot` ~329 queries/request); (3) the shared race-outlook `withDeadline` uses `Promise.race` and does NOT cancel underlying DB work when it times out — directly explains F184's 184s outlier, and means copying this exact pattern onto `/races` (F187) would NOT be sufficient on its own; (4) snapshot generation churn from 5 untracked triggers; (5) timeout genuinely renders as "offline" in `API.authedSend`, confirmed in code. **David's own 9-step corrective sequence is now the governing plan for finishing BA-01** — work from F189 directly, not from F187 alone.

**RESEQUENCING IN EFFECT, 2026-09-15, David's Desk — see register F167.** David just reproduced the original complaint live, on his current real device, after tonight's fixes (F147/F152/F156/F157/F158/F162/F164/BA-01-Stage-0). Diagnosing that live repro now outranks continuing BA-01 as previously scoped. Not yet diagnosed — do not assume any prior fix failed or that BA-01's plan is wrong, per `feedback_symptom_recurrence_check_second_cause_first.md`. Correlation-ID tooling (F161) already covered: David's first capture WAS the on-device Request Diagnostics screen, 89 requests with real correlation IDs, transcribed and in the backend session's hands now — same evidentiary shape as F162, ready to trace server-side.

| Unit | Owner | Branch / base-head SHA | Current step | Next step | Reviewer | Blocker | Last real evidence | Deploy / TestFlight | Device verification |
|---|---|---|---|---|---|---|---|---|---|
| **BA-01R** — finish the live incident (formerly tracked as BA-01; David's formal FAILED verdict on build 303, F186, is what this unit exists to resolve) | Backend architecture review session | `main` @ `f326ce792` (items 1-7 + 10/11 CONFIRMED merged + deployed, F213 — 3rd gate flake resolved). `codex/ba01r-deadline-envelope-2026-09-15` @ `f12517ebb` (items 8/9/12), dispatched for review | **11 of 12 items merged and deployed, confirmed via two independent sources (Railway + GitHub, F213). Only items 8/9/12 remain, in review.** Once `f12517ebb` clears: all 12 on `main`, then one new TestFlight build carrying the complete unit, then David's real-device re-trace | Await external review verdict on `f12517ebb` — the last remaining piece | External review lead — CONFIRMED items 1-7/10-11 (F201/F204/F208/F210); `f12517ebb` dispatched, awaiting verdict | None | `f326ce792` CONFIRMED via Railway + GitHub — see F213 | 11 of 12 items MERGED + DEPLOYED. Item set 8/9/12 not yet merged. No TestFlight build carries the complete unit yet | **Not yet — the actual bar.** All 12 items need review+merge, then one new TestFlight build, then David's real-device re-trace — the 10-point required proof (top of file) needs the complete unit on one real trace, not a first pass |
| **F164** — `v5/today` parallelization (sequenced with BA-01R, not a separate unit) | Backend architecture review session | `main`, merged `8af77c757` + `32ea432a4` (was `audit/v5-today-independence-2026-09-15` @ `a70bce72a`) | **MERGED and DEPLOYED** — landed immediately after BA-01 Stage 0 per David's ruling. Item 6: evidence-based recommendation to KEEP (F191) | Measure combined request count/latency/pool pressure now that both are live; remove only on evidence of worse peak pressure | External review lead (already confirmed pre-merge) | None | External review CONFIRMED pre-merge; merge order independently verified via `git log origin/main`; Railway deploy confirmed SUCCESS at exact commit `32ea432a4` (HEAD of `main`), 2026-09-15 12:29:39 PT | **DEPLOYED — Railway SUCCESS, verified commit hash match** | N/A (backend-only change; server-side, no native surface) |
| **BA-00A** — automated additive schema deployment | Not yet assigned | — | Not started — `manifest.json` status "ready", no dependency | May run in parallel with BA-01R (file sets don't overlap per the manifest) | — | None named yet | `WHOLE-BACKEND-MASTER-PLAN-AUDIT` confirms migrations 166-169 absent from production, manual deploy process is the real prerequisite gap (F193) | — | — |
| **BA-00B** — workload budgets, route-stage metrics | Not yet assigned | — | Not started, depends on BA-00A | Sequenced after BA-00A | — | Sequenced after BA-00A | — | — | — |
| **BA-02A** — runner versions + `app_projections` store | Not yet assigned | — | Not started, depends on BA-00A | Sequenced after BA-00A | — | Sequenced after BA-00A | — | — | — |
| **BA-02B** — shadow projection builders | Not yet assigned | — | Not started, depends on BA-02A | 7-day dark-parity gate before BA-02C | — | Sequenced after BA-02A | — | — | — |
| **BA-02C** — bootstrap/calendar read API | Not yet assigned | — | Not started, depends on BA-02B | Sequenced after BA-02B, behind feature flags | — | Sequenced after BA-02B | — | — | — |
| **BA-02D** — iPhone `AppSnapshotStore` | Not yet assigned | — | Not started, depends on BA-02C + BA-01R | Sequenced after both | — | Sequenced after BA-02C/BA-01R | — | — | — |
| **BA-03A** — command receipts + transactional outbox | Not yet assigned | — | Not started, depends on BA-00A + BA-02A | Sequenced accordingly, dark | — | Sequenced after BA-00A/BA-02A | — | — | — |
| **BA-03B** — per-runner worker | Not yet assigned | — | Not started, depends on BA-03A + BA-02B | Sequenced accordingly, shadow | — | Sequenced after BA-03A/BA-02B | — | — | — |
| **BA-03C** — high-value write migration | Not yet assigned | — | Not started, depends on BA-03B | One command at a time, per David's migration order | — | Sequenced after BA-03B | — | — | — |
| **BA-04** — SSE + APNs version hints | Not yet assigned | — | Not started, depends on BA-02D + BA-03B | Sequenced accordingly | — | Sequenced after BA-02D/BA-03B | — | — | — |
| **BA-05** — surface migration + deletion | Not yet assigned | — | Not started, depends on BA-04 + BA-03C | Surface-by-surface, compatibility-gated | — | Sequenced after BA-04/BA-03C | — | — | — |
| **F177** — remote diagnostics access, so David stops manually screenshotting | Not yet assigned | — | Not started — scoping only, sent to backend session | Decide: fold into BA-00B/BA-02's observability layer, or ship sooner as a standalone "send full diagnostics" action reusing F161's `request_failures` infrastructure | — | None yet — explicitly tracked here per David's Desk's own concern so it doesn't get lost once BA-01R ships and pressure eases | David, direct via David's Desk, 2026-09-15 — real friction all night, every diagnostic capture tonight depended on him personally screenshotting | — | — |
| **F195b** — `lib/plan/_rolling_seven_ceiling.test.ts`, 2 failing tests, unowned, could block any future deploy | Not yet assigned | — | Not started — found incidentally, correctly not fixed under BA-01-9's own time pressure | Needs its own owner and diagnosis, independent of BA-01R | — | Not yet a live incident — the risk is any future full-suite deploy tripping on it unprepared, same shape as the `check-action-kinds`/`_coercion_scan` gate misses tonight (F185/F195) | Backend session, 2026-09-15, confirmed unrelated to their own change via `git diff --stat` | — | — |
| **F204b** — `sessionMoved` push tap routes through the generic `.foreground` reason, no bypass of BA-01R's new 60s cooldown | Not yet assigned | — | Not started — found during external review of F202, narrow and not a merge blocker | Route `sessionMoved`'s deep-link handling through `.mutation` or a dedicated `SyncReason` instead of the generic foreground path | — | Narrow: 60s window, secondary snapshot-backed surfaces (week strip, other days) only, no wrong action found — real but low severity | External review lead, 2026-09-15, independently spot-checked by programme lead against `NotificationsAppDelegate.swift` directly | — | — |
| **F212** — Railway's `check-action-kinds` build gate has failed 3 times tonight on unrelated commits, a different missing key named each time against unchanged, locally-clean source | Not yet assigned | — | Not started — retriggering each time (the only lever available from a code worktree), not root-caused | Needs someone with Railway build-cache/infra visibility, not achievable from a code worktree — this is an infra reliability question, not a code defect | — | **Real, standing risk given David's explicit "no errors" bar (F207)** — a deploy pipeline failing ~1-in-3 tonight on unrelated commits, currently only worked around by retriggering | Backend session, 2026-09-15; independently re-derived by the programme lead running `check-action-kinds.sh` directly against the exact failed commit, confirmed clean both times | — | — |

**Production resource policy (David's own words, standing until he says otherwise)**: no additional Railway replica, larger tier, paid Redis/cache, separate paid worker, Cloudflare, or any other recurring infra increase without David's explicit approval — bring him the measured threshold, proposed change, monthly cost, expected headroom, and rollback plan first. Railway only; Cloudflare explicitly out of scope for this build.

**Independent audit log, STANDING requirement, David direct via David's Desk, 2026-09-15: "keep adding to it and I want to keep a record of it."** `for backend architecture review/FIXES-AND-PROCESS-LOG.md` — a standalone, zero-context, reviewer-ready record of every fix (real SHAs, Rule 18 falsification evidence per fix, independently-confirmed deploy status), for eventual review by a source outside the programme. Not a one-time document — ongoing for the life of this programme. Being built and maintained by the backend architecture review session; not yet read/verified by the programme lead — check its content against the register before pointing David to it as complete. **Gate, effective now: no BA unit merges without a corresponding entry in this log.** Add it to each unit's own completion checklist alongside the 7-test gate/external review — a merge with a clean gate and review but no log entry is incomplete per this instruction, same standing as the other completion criteria.

---

# Ownership Board — live, single source of truth for who's doing what

**Read this before dispatching anything. Update it every time you dispatch, every time a session
reports, and on every self-check.** This file exists because sessions were allowed to go idle after
reporting instead of being immediately re-dispatched — David's direct correction, 2026-09-14 night.

**Standing rule for every role, restated in every dispatch message:** end every report with your
own recommended next investigation. A report with no next-step is incomplete.

**Standing rule for the programme lead (this session):** a cron (job `8987d97c`, every 30 min, see
bottom) re-reads this board, checks every session's real state via `list_sessions`, and re-dispatches
anything idle with a NAMED task — immediately, not after being asked. Any gradecard row C-or-below
with no active owner is a programme failure and gets assigned in the same check.

---

## COMPLETE PAUSE, ALL WORK EXCEPT F159 — 2026-09-15, David's direct instruction via David's Desk, superseding the stand-down below

**David, direct, verbatim: "pause and log all other work until this backend issue is resolved."** This supersedes the earlier "F156 as the sole stand-down exception" framing — that thread is now itself paused, logged, and folded into F159 as evidentiary input, not an active parallel track. As of this instruction:

- **Code agent**: paused. F156/F158 (the live-bug thread) fully logged and closed out in the register at their real, honest state — not overclaimed, not left dangling. F160 (the third "no post-run data" symptom) logged as its own entry, explicitly paused rather than investigated. Told directly: stop, don't just deprioritize.
- **External review lead**: paused. F158's confirmation logged and complete. Told directly: don't start any independent trace, even on something that looks related.
- **Coach consultant, independent product review, design & UX review**: already stood down from the earlier instruction, unchanged.
- **Programme lead's own 30-min self-check cron**: already cancelled (Cycle 20).
- **The ONLY active work on the entire programme right now**: the new dedicated backend architecture review session (`for backend architecture review/`, home fully set up — brief plus all three research reports), and whatever the programme lead does to help stand it up. David confirmed he cannot start a new top-level session from David's Desk's side; the programme lead confirmed honestly it cannot create one either — creating the actual session needs David himself (a new terminal/window, same as how the other dedicated sessions started tonight).
- **Do not resume ANY work, dispatch ANY task, or treat anything as active** until David (via David's Desk) explicitly says the backend issue is resolved or otherwise lifts this. This line supersedes the stand-down section below and stays at the top of the board.

---

## FULL PROGRAMME STAND-DOWN IN EFFECT — 2026-09-15, David's direct instruction via David's Desk

**"I need all agents and work to STOP immediately. I need all 30 min timers to stop. Nothing
running. Nothing checking in. Full stand down."** Followed immediately by the sole exception:
F156 (the offline/missing-run-data failure family that survived three independent "fixed and
confirmed" verdicts tonight — F056, F147, F152), full priority, all hands, "until we fix this
nothing else moves."

- Programme lead's own 30-min self-check cron (`8987d97c`) **cancelled**, confirmed via `CronList` returning empty.
- Coach consultant, independent product review, design & UX review: **stood down explicitly**, told not to resume without direct word.
- Code agent: **redirected to F156 as sole active work** — widen investigation to the real backend data path, not client-code tracing alone; confirm exact build David's failing test ran on; the third symptom (missing post-run data) was never actually root-caused.
- External review lead: **standing by**, explicitly told not to start a parallel independent trace — one coherent investigation, not two.
- **Do not resume ANY other work, dispatch ANY other task, or re-arm the 30-min cron until David (via David's Desk) explicitly says so.** This line stays at the top of the board until stand-down lifts.

---

## Board (last verified against real `list_sessions` state: 2026-09-15 ~14:00 UTC)

**Cycle 20 (interstitial — F151/F152/F153 crisis fully resolved, not a scheduled 30-min check)**: David reproduced F147's exact "offline" symptom on build 296, the build that had just been triple-confirmed shipped — immediately logged as F151 (urgent), no premature conclusion either direction. Device state confirmed first (David genuinely on 296, ruling out staleness). Code agent traced a real, concrete second mechanism (F152: a Retry-button same-date guard, silently a no-op, unrelated to F147) and correctly declined to self-merge given the stakes. External reviewer independently re-ran the Rule 18 falsification themselves and proved the load-bearing claim with hard evidence (`git show` on F147's shipped commit: zero references to F152's code) — F151 closed on that result, not on reasoning alone. Process lesson written and memory saved: the real lesson is narrower than "the chain can't be trusted" — a symptom can have more than one cause, confirming a fix for one doesn't rule out a second coexisting one. Merge authorized, new TestFlight build in progress. In parallel, David's separately-requested run debrief (F155) and the elevation-gain accuracy investigation he flagged (F153, real and recurring across 54 days, root-caused to netting-granularity noise, ruled by the coach consultant as Option B) both closed cleanly, plus an independently-found citation defect (F154) fixed and deployed same-night. Register push during this stretch hit an extraordinarily long `next build` (52+ min CPU, genuinely still progressing not hung, on a host that spiked past 400 load earlier) — 36 commits queued locally without data loss risk (git commits are durable on disk regardless of push timing), eventually landed via a fresh isolated-worktree push once general load eased, in under 3 minutes. Hub republished honestly at every step of this arc — urgent when genuinely urgent, resolved only once actually proven, never claiming more than was known at each point.

**Cycle 19 (interstitial — real on-device resolution, not a scheduled 30-min check)**: David directly confirmed, relayed via David's Desk, that TestFlight build 295 is installed on his phone and the tilde mark is genuinely gone — his own eyes, not a code claim. **This is the first fix all night to actually clear Rule 13's full verification bar** (BUILT/MERGED/DEPLOYED/SHIPPED all four independently confirmed, not just the first three). F132/F035 marked CLOSED citing this as their Rule 13 evidence; F145 (the SHIPPED-vs-DEPLOYED gap) marked resolved for the fixes build 295 carries. Hub republished replacing every "not yet confirmed shipped" line with the real confirmation. Not resolved by this: the "workout unavailable offline while connected" report persists identically on build 295, ruling out stale-build staleness as the explanation — logged as its own finding, **F147**, rather than left buried in F145's text, since it needs independent tracking to a real root cause. Code agent confirmed (via transcript, not assumed) actively investigating in an isolated worktree. Register push for this cycle backgrounded due to a legitimate, concurrent `next build` under extreme host load (223+) — not touched, left to complete rather than interrupted or worked around.

**Cycle 18 (interstitial — SHIPPED-vs-DEPLOYED correction, not a scheduled 30-min check)**: David live-tested his phone (relayed via David's Desk) and found the tilde mark still showing plus a new "workout unavailable offline" report, after three "fixed" claims tonight including this session's own hub. Root cause independently determined: `check-deploy-status.sh` only confirms Railway SUCCESS for `web-v2`/backend — irrelevant to native iOS/TestFlight shipping — and every native-Swift fix tonight (F110/F111/F112/F119/F124/F132/F144/F057/F061/F065) was verified at BUILT/MERGED/DEPLOYED(Railway) but never at SHIPPED(device). TestFlight build 294 (cut 00:23) genuinely predates every one of tonight's native merges (earliest at 01:31) — confirmed by checking actual commit timestamps, not assumed. Corrective actions taken: hub Artifact republished with honest "confirmed at the code level, not yet confirmed shipped to your phone" language (Updates-log + Gradecard's Run execution & Watch row, B+ → B pending real confirmation); **F145 logged** in the register as its own systemic finding; code agent messaged directly and — confirmed via its own transcript, not assumed — is actively cutting **TestFlight build 295** via `ship-testflight-v2.sh` to carry all fixes landed since 294, citing the standing "merged is not shipped" authorization. Cycle 17's board-push retry (`2fe993092`) also confirmed landed on `origin/audit/brain-forensic-2026-09-10`. A new feedback memory saved on this failure mode (Railway DEPLOYED was read as proxy evidence for native SHIPPED, distinct from the earlier hub-staleness memory). `list_sessions` at time of writing: code agent genuinely RUNNING; external reviewer, independent product review, design review, coach consultant, and David's Desk all had activity within the last ~6 minutes (not idle, no forced dispatch needed this cycle). Awaiting: build 295 confirmed live in App Store Connect with a real build number, before F145 and its affected findings can be honestly closed.

**Cycle 17 (30-min self-check)**: real `list_sessions` showed code agent and David's Desk genuinely RUNNING (active within seconds of the check). External reviewer, design review, coach consultant, and independent product review all genuinely idle since 11:16-11:25 (~51-59 min, since before cycle 16's check) — four named, bounded dispatches: independent product review → spot-check the programme lead's own hub-republish claims against the real register (self-check on a large unreviewed edit, closing a real gap — nobody was verifying the hub the way everything else gets verified); external reviewer → independently verify F139's newly-discovered implementation scope (arithmetic consumers, CI-enforced checks) before a large safety-adjacent rewrite starts; coach consultant → sanity-check whether F139's ruling requires all 4 paths fixed together or permits a staged lower-risk approach; design review → recheck simulator load, resume F137 render verification if viable. Between-cycle activity: **hub Artifact fully republished** after David flagged it stale twice — Gradecard/Pulse/Updates all rewritten against real current register state (144 findings, 48 RRs, 55+ confirmed), not a timestamp patch; a feedback memory saved on the failure mode (heavy findings-stream processing crowding out the "now republish" step). Gradecard: no unowned gap. No all-idle condition, no push notification.

**Cycle 16 (30-min self-check)**: real `list_sessions` showed David's Desk genuinely RUNNING; code agent `isRunning: false` but lastActivityAt seconds before the check — not idle. External reviewer, design review, coach consultant, and independent product review all idle 20-28 min, all within cycle 15's own dispatch window (active through/after ~11:15-11:25) — normal post-delivery pacing, not staleness, no forced dispatch this cycle. Between-cycle volume: F061 CONFIRMED, closing tonight's entire external-review backlog (RR-042/043/046/047/048 all done); F132+F144+F057 merged and independently confirmed DEPLOYED (Railway SUCCESS at `7775b3563af5`); F139's real implementation scope discovered (much larger/riskier than the ruling suggested — CI-enforced doctrine-registry checks that will hard-fail the build, zero test coverage on lthr-reanchor.ts, safety-adjacent territory) and correctly NOT rushed, a proper brief being scoped instead; F097's wiring scoped correctly (unit-conversion fix now, live-route-caller as a separate follow-on); F083 live-reconfirmed with a new 4th-total-freeze pattern connecting to F134's `/api/settings` anomaly, dispatched for a shared-cause check; F107 strengthened and concretely re-scoped (a genuinely disciplined product read that found the real cheap fix instead of proposing new UI). Gradecard: no unowned gap. No all-idle condition, no push notification.

**Cycle 15 (30-min self-check)**: real `list_sessions` showed external reviewer, code agent, and David's Desk genuinely RUNNING. Design review, coach consultant, and independent product review all genuinely idle since ~10:23-10:26 (~49-52 min, since before cycle 14's check) — three named, bounded dispatches: design review → recheck simulator load and resume render items 3/4 or F137 if viable; coach consultant → draft the actual F097-to-live-grading wiring design ahead of code landing, same "ahead of it" pattern as F096; independent product review → a real product read on whether F139's evidence-weighting change creates a genuine "why this counts" disclosure opportunity, or whether silence is correct per the UX simplification doctrine. Between-cycle volume since cycle 14: F124 confirmed a SECOND independent time; F132 CONFIRMED with a significant gate-scoping gap found (F144 — check-modelled-mark.sh exempts exactly the file with F132's own prior-incident history from its scan); F057's remaining 4 sentences CONFIRMED with zero findings, closing that thread; F065 CONFIRMED; F143 (the "Scratch Merge Sim" git-identity mystery, hit independently by this session earlier and by a second sighting tonight) fully root-caused, fixed going forward, and independently re-verified fact-by-fact — traced to a shared `.git/config` misconfiguration from a scratch dry-run that should have stayed local, explaining every sighting tonight with one root cause; the history-rewrite question correctly flagged to David rather than decided unilaterally. Gradecard: no unowned gap. No all-idle condition, no push notification.

**Cycle 14 (30-min self-check)**: real `list_sessions` showed external reviewer and David's Desk genuinely RUNNING; code agent `isRunning: false` but lastActivityAt seconds before the check — mid-cycle, not idle. Design review, coach consultant, and independent product review all idle 24-26 min, but each delivered real new work in that window (F134 harness recheck + dead-code note, F133 coaching-trust ruling, F140 trigger-condition ruling respectively) and got acknowledged — normal post-delivery pacing, not staleness, no forced dispatch. Between-cycle volume: F110 CONFIRMED post-hoc by external review; F124 independently re-confirmed a SECOND time by external review (beyond design review's earlier confirmation), also resolving F125's original concurrent-HKWorkoutSession caveat; F111+F112 confirmed DEPLOYED after a real flaky-gate retry (F142, verified via git history as a genuine 3rd occurrence, not overclaimed); F141 new (a test-filter false-positive risk — `-only-testing:` silently matching zero tests while reporting SUCCESS); F134's harness partially cleared (`/api/shoe` fixed, `/api/settings` still broken with a real diagnostic lead handed to the code agent); F137's render verification blocked by genuine F116 touch-input failure (25 simulators concurrently booted tonight), correctly held rather than forced or guessed around. Gradecard: no unowned gap. No all-idle condition, no push notification.

**Cycle 13 (30-min self-check)**: real `list_sessions` showed code agent and external reviewer genuinely RUNNING, David's Desk running. Design review, coach consultant, and independent product review — flagged as "recent, not stale" last cycle at 15-18 min idle — are now genuinely idle since BEFORE cycle 12 (45-48 min, no activity since), confirmed via each session's own transcript: all three are correctly holding for direction, not stalled (independent product review explicitly asked whether to keep pace or take a breather — answered: keep going). Three named, bounded dispatches: design review → recheck F134's harness routes (`/api/settings`/`/api/shoe`) now that time has passed, resume items 3/4/5 if cleared; coach consultant → a coaching-trust ruling on F133 (should the app ever show a modelled value for something that already happened, as a standing principle, separate from the code agent's data-investigation half); independent product review → a product read on F140's trigger-condition scoping (is watch-signal-only sufficient, or is there a path gap). Between-cycle activity: F111+F112 CONFIRMED, fully closing F125's long-standing hold (F110/F124/F111 all independently confirmed now); F080/F126 and F029/F060 both confirmed DEPLOYED via Railway (not just merged); F061's real root cause found (API.fetchShoes() silently swallowing bad-status errors, same "named pattern was right but incomplete" shape as F132); F131's actual pbxproj root cause confirmed (deterministic xcodegen TEMP_UUID churn, not drift) — closes that whole investigative thread. Gradecard: no new unowned gap. No all-idle condition, no push notification.

**Cycle 12 (30-min self-check)**: real `list_sessions` showed code agent, external reviewer, and David's Desk genuinely RUNNING. Design review, coach consultant, and independent product review all `isRunning: false` but each delivered real work between 09:26-09:30 (F139 ruling, IPR-016 race-tiering findings, F083 narrowing respectively) — 15-18 min idle is a normal post-delivery window, not staleness, so none re-dispatched this cycle. Between-cycle volume since cycle 11 was very high: F077/F097/F110/F124/F080/F060/F029/F063/F078 all independently CONFIRMED by external review and merged+deployed (Railway-verified, not just trusted); F139 (priority must never weight evidence) RULED decisively by coach consultant and dispatched as ready-to-build across 4 paths; F132's root cause corrected (an un-reverted Rule 24 fabrication commit, not the CI gate as first diagnosed) plus 6 more live tilde sites found; F131's packaging-defect thread now has 4 distinct classes, all centering on `project.pbxproj`; F138/F140 (race-tiering UI, orphaned race-finish screen) logged and dispatched; F134/F137 (harness anomaly, outdoor HR bug) strengthened with more evidence. Gradecard re-checked at a light pass given volume already covered — no unowned gap. No all-idle condition, no push notification.

**Cycle 11 (30-min self-check)**: real `list_sessions` showed code agent, external reviewer, design review, and David's Desk all genuinely RUNNING (all 4 with lastActivityAt within the same minute as the check) — no dispatch needed for any. Coach consultant idle ~21 min but intentionally released after closing F130's full audit set, correctly not re-dispatched without real new work. Independent product review genuinely idle ~45 min, since well before this cycle — dispatched a named, bounded product-lens audit: check the real season/race-calendar screens (Dodgers 10K/Malibu/CIM/LA Marathon) against the newly-locked Race Tiering & Season Philosophy doctrine (priority vs. evidentiary value must never share labels), concrete pass/fail per screen. Between-cycle activity since cycle 10: F119/F097/F077/F110/F124 all merged+deployed (independently confirmed via Railway); all 14 of tonight's review-request packets (031-045) now confirmed VALID after a real root-cause fix (F131); F126 (Safety/Readiness detect-only mismatch logging) submitted and independently re-verified VALID (RR-045); F063 and F078 both CONFIRMED by external review with independent falsification, qualify for standing merge authorization; F082 fully render-confirmed and closed; F132 (banned tilde mark, live on David's real account, 3rd instance) logged urgent and dispatched ahead of the queue, scope confirmed app-wide; F133 (possible real race-data provenance bug on a completed race) logged and dispatched for a first investigation pass. Gradecard re-checked at a lighter pass this cycle given the volume of between-cycle activity already covered above — no new unowned gap found; Runner Control (F072/F073) is now genuinely closed, not just watched. No all-idle condition, no push notification.

**Between-cycle report processing**: F119 (PhoneSync status mechanism) implemented — real fixes to the one-way sync-state guard and the silent retry-queue eviction, 9 new tests + full 243-suite pass. F097+F077 merged to `main` (`f46f1c963`), then F110+F124 merged (`a1eab3a9b`) now that F125's hold cleared both — **independently confirmed DEPLOYED, not just trusted**: `scripts/check-deploy-status.sh` shows Railway SUCCESS at `a1eab3a9b`, and `f46f1c963` confirmed as its ancestor via `git merge-base`. **Real process finding: RR-20260914-044 (F119) has the identical not-actually-submitted gap as RR-031 — no READY sentinel, broken evidence_paths[0] — independently re-verified with the real validator rather than assumed clean given the RR-031 precedent. 2-for-2 on this exact failure from the code agent tonight; flagged as likely systemic (a manifest-generation habit/template issue), not two coincidences.** Register corrected on F119 same as F074/RR-031. Code agent told to fix both RR-044's packaging and whatever's producing this pattern; external reviewer warned off reviewing RR-044 until it's actually ready.

**Cycle 10 (30-min self-check)**: real `list_sessions` + transcript checks (not assumed) showed: code agent NOT idle — mid-verification on F119, self-paced a `ScheduleWakeup` to resume when its test run finishes, my F129/F096-policy dispatches queued behind that; David's Desk running. Three sessions genuinely idle with empty queues: external reviewer (flagged F102's strengthened evidence to David's Desk, then true idle), design review (still standing down from last cycle's legitimate stop, ~30 min now), coach consultant (intentionally released after F096, ~25 min now). Named bounded dispatches, none vague: external reviewer → independently review `RR-20260914-031` (F074's onboarding evidence-wiring fix), unreviewed and verified real via the register + review-requests folder before dispatching; design review → close the actual "watch for regressions" gap on F072/F073 (gradecard said watch, nobody was) if render's recovered from F116, else report plainly that it's still blocked; coach consultant → audit PO-008's copy against tonight's freshly-locked "coach explains like a coach, not a data readout" CLAUDE.md addition, which nothing has been checked against yet. Independent product review left alone — active through and past cycle 9, only ~14 min idle after a real handoff, not stale. All gradecard C-or-below rows re-checked: no unowned gap this cycle (F102/Baseline-plans now has a decisive verdict sitting with David's Desk; everything else unchanged from cycle 9's assessment). No all-idle condition, no push notification.

**Cycle 9 (30-min self-check)**: real `list_sessions` call showed external reviewer, coach consultant, and independent product review all genuinely idle since ~07:48 (~30+ min, confirmed via each session's own transcript — all three had reported their last dispatch, gotten acknowledged, and gone idle waiting with nothing queued). Code agent, design review, and David's Desk all genuinely active/running, no dispatch needed — design review explicitly stood down after a real stopping point (F110/F124 both confirmed). Three NAMED bounded dispatches this cycle: external reviewer → falsify whether `81bf30eb8` really fixed Rule 12's easy-day defect or the gate got weakened (Baseline Plans C+ row, was unowned); coach consultant → draft the actual F096 arbitration POLICY ahead of F126's detection data landing; independent product review → audit Onboarding + Today/Block's input controls for Rule 25 violations (David's own "does this shape appear elsewhere" question). All 5 gradecard C-or-below rows checked — Adaptation & Post-run-learning remain legitimately idle (waiting on real-world evidence / code-agent capacity respectively, not neglect), everything else has a real active owner. No all-idle condition, no push notification warranted.

**Cycle 8**: F110 and F124 both independently re-confirmed by design review beyond the implementer's own verification — F110 by a fresh on-device render (isolated worktree, `pacebail` archetype), F124 by independently running the code agent's own test suite and observing the refusal log fire. F125's hold now clears both; F111 remains held (blocked by F088's harness limitation, needs console debug logging). Register + F125 updated, pushed (`bf8a435ec`), code agent notified F124 is clear to merge. Design review also refreshed the night summary to reflect both resolutions before it goes into tomorrow's recap.

**Cycle 7**: TestFlight build 294 confirmed live (F056/F057-partial/F072/F073 SHIPPED, register updated). F124 fully resolved (3-way convergence: refuse, mirror blockedByPhone), dispatched to code agent. This cycle: external reviewer → fresh merge-sim across 4 newly-implemented unmerged branches; coach consultant → scoped-first-step read on F096's missing arbitration layer; independent reviewer → contradiction/gap check across F100-F124; design review → status check-in (render still likely blocked).

**Cycle 6**: TestFlight build 293 actively shipping (code agent, ~20-30min ETA from dispatch, carrying F072/F073/F056/F057-sentences/F029). External reviewer, coach consultant, independent reviewer all re-dispatched on F124's verification/product-decision split (safety verify + training-context read + UX read, three angles on one real control-loss bug). Design review and David's Desk both genuinely active, no dispatch needed.

**Cycle 5 dispatches**: external reviewer → independently verify F119's PhoneSync claim before a fix is built on it. Independent reviewer → single strongest next-investment read across the full register (F001-F120), not just their own reports. Coach consultant → doctrine read on F102's Rule 12 question, to hand David both sides before he decides. Design review → status-line design for F119's fix, or continue source audit if render's still blocked. Code agent and David's Desk both actively running — no dispatch needed.

**Current top priority, all roles aware: getting merged work SHIPPED, not just merged.** David directly (live, testing on his own phone): user-facing fixes touching his current day/week must reach Railway/TestFlight, not sit merged-and-dormant. Locked into CLAUDE.md's deployment doctrine. Code agent is actively working this now (F057's deploy was blocked by a coach-voice gate violation, being fixed; no TestFlight build yet exists carrying tonight's native work — TF 292 predates all of it).

| Role | Session | Current task (real, verified) | Next task — NAMED | Last real output | Blocker |
|---|---|---|---|---|---|
| Programme lead | this session | Processing the continuous stream of findings (now at F120)/decisions/rulings, keeping register/hub/board/CLAUDE.md doctrine current | 30-min self-check cron (job `8987d97c`) | Register at F120, CLAUDE.md Rules 25 + "merged is not shipped" (both locked + merged to main) | none |
| Code agent | Faff code agent lead (`local_dda85548`) | **RUNNING** — fixing the coach-voice gate violation blocking F057's deploy; F056/F057/F072/F073/F060/F037/F080 all landed tonight; F110/F111/F112/F119/F065/PO-008/F057-remaining-4/F118 all queued, F120's scoping guidance (grep sibling sites before closing) relayed | Named: unblock the deploy, then cut a TestFlight build carrying F056/F057-native/F072/F073 (standing authorization in CLAUDE.md), THEN F110 (safety) first among the rest | F056/F057/F072/F073 confirmed MERGED+DEPLOYED (F072/F073) via Rule 19 chain | Coach-voice gate violation, being fixed now |
| External reviewer | Faff external review lead (`local_b83f01e6`) | Verified idle since ~07:48 (confirmed F97/F77 merge-sim clean, then held with nothing queued) | **Dispatched cycle 9**: falsify F102 — did `81bf30eb8` really fix Rule 12's easy-day defect, or did `_coach_sensible.test.ts` get weakened to pass (Rule 18 check) | F97/F77 4-way merge-sim confirmed clean | none |
| Design & UX review | Faff design and UX review (`local_e54440f7`) | **RUNNING** — independently re-confirmed both F110 (on-device render) and F124 (ran the code agent's own test suite) beyond the implementer's own verification tonight; stood down at a genuine stopping point after, diminishing returns on further sweeps | Self-directed / available on request — no forced dispatch, this is a legitimate stop | F110 + F124 independent re-confirmation, refreshed night summary | none |
| Coaching consultant | Faff coach consultant (`local_7adc730c`) | Verified idle since ~07:48 (F096 minimal-first-step scoped, dispatched as F126, held with nothing queued) | **Dispatched cycle 9**: draft the actual F096 arbitration POLICY now, ahead of F126's detection data landing, so there's no second round-trip delay | F096 minimal-first-step scope (→ F126) | none |
| Independent product review | Faff independent product review (`local_a2d4e4ca`) | Verified idle since ~07:48 (F100-F124 cross-finding contradiction check → F125, held with nothing queued) | **Dispatched cycle 9**: audit Onboarding + Today/Block's stepper/slider/picker controls for Rule 25 violations (David's own "does this shape appear elsewhere" question on F103) | F125 cross-finding meta-check | none |
| David's Desk | Davids Desk (`local_699a5e84`) | **RUNNING** | Standing by for the code agent's concrete answer on build timing | David's "merged isn't shipped" directive relayed and now doctrine-locked | none |

**All 5 role sessions + David's Desk operational.** Faff Asks (live db-backed question queue) is the
canonical "needs David" channel now — hub's Needs From You tab is a thin pointer to it, not a
duplicate.

---

## Mission-critical (gradecard C-or-below) active-owner check

| Area | Grade | Current state | Active owner | Next action |
|---|---|---|---|---|
| Onboarding | D | F074 landed (RR-031), F103 (mileage-stepper silent snap-down, high priority) + F100 addendum (discarded writes) newly found | Code agent (F074 merge pending review confirm), design review (found F103) | F103 needs a product decision (rung-only stepper vs. show-resolved-value) before code work |
| Adaptation & progression | C | F034 (headroom-reserve) already fixed and deployed earlier tonight; no second real upward-adaptation instance confirmed yet | Coach consultant defined the proof bar | Watching for a real second instance over the coming days — no active task right now, correctly so |
| Runner model & evidence | C- | F037 (stale VDOT anchor honesty fix) landed, RR-035; F096 (safety-arbitration gap) and F080 (goal-feasibility ownership) both ruled | Code agent (F080 in flight) | F080 landing next |
| Today & Block | C | F100 (discarded writes, 7 screens), F105 (Block retry wrong params), F103 (onboarding stepper) all found tonight — real defect density, several already logged, none yet fixed | Design review (finding), code agent (queued) | Needs code agent capacity once F080/F065 clear |
| Post-run learning | C- | F077 fully ruled (doctrine + calibration), ready for implementation once `TRAINING_CONSISTENCY` consolidation happens | Coach consultant (ruling done) | Consolidation of 3 disagreeing consistency readers is the real blocker, not a decision gap |
| Health/readiness | D+ | Deliberately deferred, unchanged | N/A | Correctly idle |
| Runner control | C+ | F072/F073 merged to `main` tonight (`e6fbd4045`) under David's standing merge authorization | Closed for now | Watch for regressions |
| Baseline plans | C+ | `_coach_sensible.test.ts` confirmed 6/6 passing live (F102) — contradicts its own "red on purpose" header comment, not yet resolved which is true | External reviewer (found it) | Needs someone to confirm `81bf30eb8` genuinely fixed it vs. gate weakened (Rule 18) |

**Net assessment this cycle: every C-or-below area has real, recent movement — none are unowned.**
Onboarding and Today/Block have real new defect density from tonight's full-file reads; that's
findings outpacing fixes, not neglect — the code agent has a real queue (F080 → F065 → PO-008 → the
Today/Block backlog) and is moving through it steadily.

---

## Standing mechanism

- **Cron** `8987d97c`, every 30 min (`7,37 * * * *`), session-local, auto-expires 7 days from
  creation (2026-09-14 night) — renew before expiry, watch for the renewal-check instruction in the
  job's own prompt.
- **Faff Asks** (https://claude.ai/code/artifact/670b93b3-86b7-45d0-bbe0-f4bad9ac4c1e) is the live
  "needs David" queue, owned by David's Desk — check it via `read_db` (`collection: "questions"`) for
  a cheap open-count without pinging David's Desk each time.
- **This file is the durable record** — checked into the repo, not memory.
