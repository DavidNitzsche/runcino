# Findings register

Every finding gets a stable ID for life. Never delete a row, never let a finding disappear into a
replacement document — update its disposition in place. If a reviewer disagrees with a finding,
record `REJECTED WITH EVIDENCE` and the reasoning, never silence removal.

All findings below originate from `EXTERNAL-REVIEW-DECISION-2026-09-14.md`, the first review run
under this process. Individual finding files (`ER-20260914-F0NN.md`) hold full detail; this table
is the at-a-glance disposition.

| ID | Severity | Summary | Disposition | Fix SHA | Re-review |
|---|---|---|---|---|---|
| F001 | Blocker | Lane 1: `!priorPrescription` stand-down permits an unverified replacement instead of refusing | **Three submission attempts, zero substantive reviews.** `RR-20260914-001` (missing file in list) and `RR-20260914-007` (worktree claimed clean, wasn't) both returned INVALID REQUEST. Resubmitting a third time (`RR-20260914-007`'s successor) from a genuinely clean detached worktree, validated with the now-hardened `--worktree=` check that independently verifies `git status` rather than trusting the manifest. UNCONFIRMED, not closed. | `090d48b56` | Pending third resubmission |
| F002 | Blocker | Lane 1: `no_active_plan` fallback calls `generatePlan` with no recovery-window gate at all | Same submission history as F001 — two INVALID REQUEST verdicts, third resubmission in progress. UNCONFIRMED, not closed. | `090d48b56` | Pending third resubmission |
| F003 | Blocker | Lane 2: a decision-relevant `Date.now()` remains in `plan-drift`, controlling race-window eligibility | `RR-20260914-004` returned INVALID REQUEST — changed-file list omitted `programme-internal-working/00-master-programme/LANE2-FIXROUND2-F003-2026-09-14.md`. No substantive review performed. UNCONFIRMED, not closed. Resubmitting with the complete 3-file list from a clean worktree. | `bb5e6b7ef` | Pending resubmission |
| F004 | High | Lane 3A: `executionFailure=established` branch has no permanent test coverage, and no caller supplies `executionQuality` yet — field is live-shaped but practically vacuous | Open | — | — |
| F005 | High | Phase 3: round 1+2 together still produce two identical 8mi/64.5min exposures, not progression; the 6-session fast_finish mechanism places zero sessions on the real calendar | Open — doctrine lane (dose-band vs. density) in progress | — | — |
| F006 | High | Lane 5 round 2 branch points at the superseded round-1 commit with 3 uncommitted files; not actually complete despite being reported as in-progress-but-real | **CLOSED — EXTERNALLY CONFIRMED** (`RR-20260914-019`). Non-blocking note: reviewer separately found `_plan_updated_receipt_live.audit.test.ts` now fails a hardcoded headline assertion — traced to a real, already-documented headline override activating for the first time against real data (PW-20260914-001's execution), not a regression from this fix. Someone should update the hardcoded assertion when convenient; not urgent, not blocking. | `e2cc2811f` | **CONFIRMED** at `review-results/RR-20260914-019/review.md` |
| F007 | Blocker | RC6 and Lane 1 are semantically dependent (RC6's `run-adaptations`→`ensureRaceResultsSealed` can archive/replace plans through the exact pathway Lane 1 gates) despite zero file overlap. File-level independence is not functional independence. | RC-006 rebuilt on Lane 1's reviewed SHA; independent reviewer reproduced the F007 falsification from scratch (candidate passes, pre-Lane-1 checkpoint swap fails with the exact claimed assertion, restore passes again) — CONFIRMED WITH NOTES. **Process gap flagged by the reviewer:** Lane 1's own formal external-review request (`RR-20260914-001`) is still pending per `CURRENT.md`'s closure rule, even though this rebuild's narrative and `ER-20260914-F007.md` treat Lane 1 as closed based on this session's internal review only — F007 cannot be considered fully closed through the formal channel until RR-20260914-001 itself resolves CONFIRMED. Not yet closed. | `621d2fd06` | Pending `review-results/RR-20260914-001/review.md` (Lane 1 dependency) — RC-006 side independently confirmed |
| F008 | Blocker | RC6's "code-only rollback is complete" claim is false — sealed race results and archived/replaced plans are persisted effects a code rollback cannot undo | A real rollback/reconciliation procedure (`rollback-reconciliation-procedure.md`) independently traced through actual code — `POST /api/plan/undo` confirmed to genuinely exist and do what's claimed for the replacement path; fresh-authoring fallback's lack of an equivalent confirmed real. CONFIRMED WITH NOTES. Full test-suite comparison (baseline vs. candidate) and iPhone build (including embedded Watch app) independently reproduced exact-match. | `621d2fd06` | Independently confirmed — closed pending RC-006's own release-candidate RR submission |
| F009 | Blocker | Recovery-window packet, Monday statement: restores the archived plan's pace band, which is now known (via the VDOT-466 investigation) to be STALE — the current 46.6/440 value is the legitimate one. The packet would replace a correct value with a wrong one. | **EXTERNALLY REVIEWED: CHANGES REQUIRED** (`RR-20260914-003`, handback `b5a86be2b`). Monday untouched is correct; the required fresh, independently-replayable current-state/resolver evidence (source SHA + SQL hash bound, live schema not a May-30 dump) is missing (RR003-R4). Not closed. Rebuild in progress, to be resubmitted with `supersedes: RR-20260914-003`. **CLOSED — EXTERNALLY CONFIRMED (`RR-20260914-011`) and EXECUTED.** Coach-consult pass also cleared it (`for coaching consult/consult-log/2026-09-14-002-recovery-window-correction-coaching-check.md`, "grounded confidence... recommend authorizing"). David authorized directly; executed against production 2026-09-14T20:29:37Z UTC. Live-verified: Mon/Wed untouched, Tue/Thu now easy 5mi non-quality, Fri now the conditional threshold re-entry session, plan version advanced, all 3 audit rows present. | Executed `15606a84629eff4cd3a0ffc726053640153855c3230ccf69d8ee428100114f11` | `review-results/RR-20260914-011/review.json` CONFIRMED; `production-writes/PW-20260914-001/execution-receipt.md` |
| F010 | Blocker | Recovery-window packet, Wednesday statement: same defect as F009 — injects the archived, now-known-stale pace band | **EXTERNALLY REVIEWED: CHANGES REQUIRED.** Easy-band preservation confirmed, but the packet ADDS unsupported strides + a longest-easy-run note that contradicts the untouched 7.5mi Friday (RR003-R3); same R4 evidence gap as F009. Not closed. Rebuild in progress. **CLOSED — EXTERNALLY CONFIRMED (`RR-20260914-011`) and EXECUTED.** Coach-consult pass also cleared it (`for coaching consult/consult-log/2026-09-14-002-recovery-window-correction-coaching-check.md`, "grounded confidence... recommend authorizing"). David authorized directly; executed against production 2026-09-14T20:29:37Z UTC. Live-verified: Mon/Wed untouched, Tue/Thu now easy 5mi non-quality, Fri now the conditional threshold re-entry session, plan version advanced, all 3 audit rows present. | Executed `15606a84629eff4cd3a0ffc726053640153855c3230ccf69d8ee428100114f11` | `review-results/RR-20260914-011/review.json` CONFIRMED; `production-writes/PW-20260914-001/execution-receipt.md` |
| F011 | Blocker | Recovery-window packet, Friday statement: restores a stale, faster-than-current threshold prescription at the edge of the post-race recovery window | **EXTERNALLY REVIEWED: CHANGES REQUIRED — internal review's stronger justification was itself wrong.** This session's own review treated the whole-week `POST_RACE_RECOVERY_WEEKS['10k']=1` rounding as proof Friday (day 5) is inside a mandatory no-quality window. External review found this conflates week-rounding with the actual day-level policy owner (`combined-stress.ts`'s `noQualityDaysAfterRace`, which returns day 5 as the FIRST potential quality day for a B 10K) — "the assertion that any Friday quality is forbidden until day 8 is unsupported... a B 10K does not automatically require a full week of zero training" (RR003-R2). Deleting the stale 430s/mi write is still correct as an interim hold; the blanket-ban reasoning is not. Lesson: two internal reviews agreeing is not independent confirmation when both reason from the same wrong abstraction level. Not closed. Rebuild must re-adjudicate Friday via the day-level owner, not week-rounding. **CLOSED — EXTERNALLY CONFIRMED (`RR-20260914-011`) and EXECUTED.** Coach-consult pass also cleared it (`for coaching consult/consult-log/2026-09-14-002-recovery-window-correction-coaching-check.md`, "grounded confidence... recommend authorizing"). David authorized directly; executed against production 2026-09-14T20:29:37Z UTC. Live-verified: Mon/Wed untouched, Tue/Thu now easy 5mi non-quality, Fri now the conditional threshold re-entry session, plan version advanced, all 3 audit rows present. | Executed `15606a84629eff4cd3a0ffc726053640153855c3230ccf69d8ee428100114f11` | `review-results/RR-20260914-011/review.json` CONFIRMED; `production-writes/PW-20260914-001/execution-receipt.md` |
| F012 | High | Recovery-window packet, Tuesday/Thursday statements: coaching intent correct, but missing active-plan lock, full-row guard, and in-transaction re-verification before commit | **EXTERNALLY REVIEWED: CHANGES REQUIRED.** Lock/guards/mismatch-aborts confirmed materially improved (real progress) but three real gaps remain: (1) same plan ID/version means the iPhone's `reconciledDayCache` genuinely retains the STALE cached Tuesday threshold after this exact correction — reproduced against the real Swift function (RR003-R1); (2) precondition binding incomplete — plan version/goal, race priority/role, completion status not bound (R4); (3) transaction left with COMMIT commented out and no post-commit/rollback reconciliation design (R6). Not closed. Rebuild must advance `last_adapted_at` atomically with the correction. **CLOSED — EXTERNALLY CONFIRMED (`RR-20260914-011`) and EXECUTED.** Coach-consult pass also cleared it (`for coaching consult/consult-log/2026-09-14-002-recovery-window-correction-coaching-check.md`, "grounded confidence... recommend authorizing"). David authorized directly; executed against production 2026-09-14T20:29:37Z UTC. Live-verified: Mon/Wed untouched, Tue/Thu now easy 5mi non-quality, Fri now the conditional threshold re-entry session, plan version advanced, all 3 audit rows present. | Executed `15606a84629eff4cd3a0ffc726053640153855c3230ccf69d8ee428100114f11` | `review-results/RR-20260914-011/review.json` CONFIRMED; `production-writes/PW-20260914-001/execution-receipt.md` |
| F013 | Medium | Recovery-window packet's `plan_mutations` insert: fixed IDs are not retry-idempotent; reasons text describes the now-rejected archived-state restoration | **EXTERNALLY REVIEWED: CHANGES REQUIRED.** `ON CONFLICT DO NOTHING` correctly prevents duplicate inserts but has no affected-count/content-match assertion — a conflicting unrelated record could silently stand in for the real audit row; `changed_fields` omits `workout_spec`/`notes`/overwritten `original_*` fields (RR003-R5). Not closed. **CLOSED — EXTERNALLY CONFIRMED (`RR-20260914-011`) and EXECUTED.** Coach-consult pass also cleared it (`for coaching consult/consult-log/2026-09-14-002-recovery-window-correction-coaching-check.md`, "grounded confidence... recommend authorizing"). David authorized directly; executed against production 2026-09-14T20:29:37Z UTC. Live-verified: Mon/Wed untouched, Tue/Thu now easy 5mi non-quality, Fri now the conditional threshold re-entry session, plan version advanced, all 3 audit rows present. | Executed `15606a84629eff4cd3a0ffc726053640153855c3230ccf69d8ee428100114f11` | `review-results/RR-20260914-011/review.json` CONFIRMED; `production-writes/PW-20260914-001/execution-receipt.md` |
| F014 | Blocker | Dodgers course-data packet: writes a labeled semantic contradiction — phase 1 is "Descent" with a stored mean grade of +0.71% (positive = ascent) | **CLOSED — EXTERNALLY CONFIRMED and EXECUTED.** `RR-20260914-012` returned CONFIRMED — reviewer independently re-derived the bug and fix from scratch (own JS transcription of the algorithm, own fresh trackpoint pull, own execution), not a re-read of the packet's evidence. Statement 1 executed against production 2026-09-14T19:50:42Z UTC on the programme lead's authorization (delegated by David for reference course-geometry data). Live-verified: phase 1 now labeled "Climb" agreeing with its own +0.71% grade. | Executed `642a56660df52803e668c9d2ae74b6c4a1042e16c14b80a8a16eabf0d532e79c` (Stmt 1 hash `d45cda4bd`) | `review-results/RR-20260914-012/review.json` CONFIRMED; `production-writes/PW-20260914-002/execution-receipt.md` |
| F015 | Medium | Dodgers course-data packet: no `updated_ts` advance or derivation/version stamp on the geometry write, leaving stale provenance | **CLOSED — EXTERNALLY CONFIRMED and EXECUTED**, same request/execution as F014. Live-verified: `phases_provenance` present with derivation/source/params/trackpoint-count/`derivedAt`, `updated_ts` advanced in the same statement. | Executed `642a56660df52803e668c9d2ae74b6c4a1042e16c14b80a8a16eabf0d532e79c` | `review-results/RR-20260914-012/review.json` CONFIRMED; `production-writes/PW-20260914-002/execution-receipt.md` |
| F016 | High → Resolved (register correction) | Originally framed as: Dodgers target packet presents Santa Monica's performance-vs-prepared-capacity as within-range when Lane 3B's own negative research result says this is genuinely unresolved for a 10K. **Coaching-consult determination (2026-09-14): this framing was wrong.** `performanceVsPreparedCapacity = within_expected_range` is David's own explicit ruling (`LANE3A-CLASSIFICATION-CONTRACT-2026-09-14.md` §1), made with its citation gap already disclosed to him — correctly treated as given, not re-derived. Lane 3B's own text explicitly disclaims contradicting it ("does NOT produce a competing classification... that contradicts Part A's within_expected_range", §6). Lane 3B's real (and genuinely negative) finding is narrower: a NEW 10K-specific taper-adherence percentage discount can't be validated (N=5, no independent holdout) — a different question this register conflated with the settled classification. | **RESOLVED — packet's use of `within_expected_range` stands, not blocked.** **David has weighed in directly (2026-09-14): "santa monica was not a shortfall based on recalibrated findings and how the race was set wrong for me but I actually performed pretty well."** He is drawing a sharper line than the classification alone: the pre-race target itself was miscalibrated, not that he came up short. Lane 3B's structural (non-percentage) taper-adherence reading on this race is real and worth keeping on file for future race/taper scheduling — it is NOT an explanation for a shortfall, since David states directly there wasn't one. Any future writeup touching this must use that framing, never shortfall-adjacent language. | — | Determination: `for coaching consult/consult-log/2026-09-14-001-f016-f017-santa-monica-10k-effort-characterization.md` |
| F017 | High | Dodgers target packet: calls both options "controlled" while Option A derives from a race averaging 169bpm and Option B is faster still, then uses that premise to waive the standalone-10K-before-a-17-miler recovery rule | **RESOLVED.** Coaching-consult determination: the recovery-window waiver never needed "controlled effort" language — it's licensed by `combined-stress.ts`'s priority-grade mechanism (Dodgers is C-priority), gated on a recorded `designedWeekend` grant. Verified live against `DATABASE_URL_RO`: the grant genuinely EXISTS for this exact pairing (`pln_7da73e1da5ea7f6f`'s `placement_compromises`, `ACCEPT_AS_HARD_WORKOUT`, `pairVolume: DEMONSTRATED`, honestly disclosing `pairOrdering: NOVEL`). Packet §10 rewritten to cite the real grant instead of unsupported prose. Separately, Option A's "controlled" label was not honest — its pace is numerically identical to Santa Monica's 169bpm/RPE9 effort, 15+bpm over Dodgers' own HR ceiling, with no new fitness evidence closing that gap. "Controlled" dropped for Option A in §2/§9/§12; packet now states the honest tradeoff (hold the ceiling and 46:31 may not come out; hit 46:31 and that repeats Santa Monica's effort) instead of asserting both away. | Packet corrected, not code | Coaching-consult determination + live DB verification; not yet independently re-reviewed as a packet |
| F018 | High | Phase 3 development-rung long run does not de-stack cross-week: the following week's ordinary tempo (2 days) and intervals (4 days) land inside `Research/04` §16's 5-day minimum gap. The one guard enforcing this (`MPSPACING-1`) only fires for the dress-rehearsal role against another `@ MP`-labelled session, not general hard tempo/intervals. Pre-existing, found by the Phase 3 doctrine lane while proving F005-adjacent de-stacking, not introduced by that lane. | Independently CONFIRMED real (re-verified against live production data, byte-exact); confirmed dose/placement are architecturally decoupled, so this does NOT gate Candidate 1's implementation — but still needs its own fix with real priority | — | Confirmed real by `phase3-doctrine-lane-review.md`, not yet fixed |
| F020 | Medium | Phase 3 Candidate 2 (5→6→8 progression)'s development dose sits on two coincident hard thresholds at once (evidence floor and quality-collapse, both exactly 6mi) — a Rule 9-adjacent risk. Does not affect Candidate 1, the actual recommendation. Found by the doctrine-lane reviewer, not in the original report. | Open, non-blocking (Candidate 2 not recommended anyway) | — | — |
| F019 | Blocker (self-reported, process integrity) | Commit `4c8b9671c` on `fix/plandrift-distancemi-casefix`, reported as "the reviewed distanceMi fix, committed," actually contained the pre-fix snake_case line — a shared-worktree file-state race during a concurrent falsification pass caused `git add`/`git commit` to capture the momentarily-reverted content instead of the fix. Caught by directly diffing `git show HEAD:...` against the working tree rather than trusting the commit message. | Independently reviewed and CONFIRMED at the exact corrected SHA `c207b4f18` — reviewer diffed `4c8b9671c` against `c207b4f18` line-for-line and confirmed the corruption story is real, falsified the fix in both directions, and confirmed live against `DATABASE_URL_RO` (0/18 races resolve via the old key, 16/18 via the corrected key, `slug='dodgers'` specifically `null`→`6.21`). Submitted for authoritative external confirmation as `RR-20260914-013`, not yet closed per the closure rule. | `c207b4f18` | Pending `review-results/RR-20260914-013/review.md` |

| F021 | Medium | `docs/PRODUCT_DECISIONS.md` carried committed, unresolved merge-conflict markers at lines 9, 90, 194 (`<<<<<<< HEAD` / `=======` / `>>>>>>> origin/action-kinds-complete`). Note: David's own instruction named this "F019" — already taken by an unrelated, earlier self-reported finding; assigned the next available number instead per the never-reuse-an-ID rule. | Two INVALID REQUEST verdicts (`RR-20260914-005`, then `RR-20260914-016`) — both because this commit's only branch (`audit/brain-forensic-2026-09-10`) lives solely in the shared, persistently-dirty main checkout. Per the programme lead's standing decision, every submission must use a genuinely isolated worktree, no exception for doc-only fixes — resubmitted a third time as `RR-20260914-018` pointing the identical commit at a new branch (`docs/product-decisions-f021-fix-2026-09-14`) checked out in a fresh isolated worktree. UNCONFIRMED, not closed. | `fed7d5dcb` | Pending `review-results/RR-20260914-018/review.md` |
| F022 | High (owner direction) | Today/Block become full-surface `OutageBodyV5` outage screens (skeletons, Retry, reassurance copy) on a failed refresh, even though `PlanSnapshotStore` already holds a durable local plan. David: "I hate this shit and there is no reason to ever see this." Source: `OD-20260914-001`. | **EXTERNALLY CONFIRMED** (`RR-20260914-017`, real independent build-from-scratch verification, not a re-read of internal evidence) and **SHIPPED** — merged to `main` (`0f3c8e2d5` → `99ffd5918` after build-record bookkeeping), TestFlight build **292** uploaded, export-compliant, distributed to Internal Testers 2026-09-14. David's own real-device confirmation (does he actually stop seeing the banner/outage screens) is the one remaining step before this can be called fully closed — not yet obtained as of this regeneration. | `0f3c8e2d5` (main HEAD `99ffd5918`) | CONFIRMED at `review-results/RR-20260914-017/review.md`; TestFlight build 292 |
| F023 | Medium (owner direction) | Today's easy/stride copy (`spec-card.ts`, `why-voice.ts`) reads as abstract system language ("run the effort," "not a workout," "hard sessions do the work"), live on `origin/main`. Source: `OD-20260914-002`. | **CLOSED — EXTERNALLY CONFIRMED** (`RR-20260914-009`). Re-diffed, run through the real composer function with fresh test cases, deliberately broken to prove the test catches the old bug. Non-blocking note (tracked as new finding F027 below): gating the QUALITY opener on `todayIsQualitySession` removed an accidental "never blank" guarantee on `derivePurpose`'s fallback. | `79d0e2deb` | **CONFIRMED** at `review-results/RR-20260914-009/review.md` |
| F024 | Blocker (owner direction, URGENT) | Global `StaleBannerV5` ("Can't reach faff...") covers Today/Block/Races on any failed read. David: "If I see the banner again I'm going to lose it." Requires a new iPhone build to actually remove from David's installed app. Source: `OD-20260914-003`. | **EXTERNALLY CONFIRMED** (`RR-20260914-017`) and **SHIPPED** — same request/build as F022. TestFlight build **292** distributed to Internal Testers 2026-09-14 — this satisfies the "requires a new iPhone build" condition named in the finding itself. David updating and confirming the banner is actually gone on his device is the last step. | `0f3c8e2d5` (main HEAD `99ffd5918`) | CONFIRMED at `review-results/RR-20260914-017/review.md`; TestFlight build 292 |
| F025 | High | `experience.ts`'s `EVIDENCE_RECORDED` receipt branch hardcoded `receipt: null`, discarding `receipt.fitnessBeliefNote` (a real logged-VDOT fact) even on nights with zero plan change. Found during F006's review, given a precise spec by David directly. | **CLOSED — EXTERNALLY CONFIRMED** (`RR-20260914-008`). Reviewer re-diffed, broke and fixed it again directly, cross-checked against the real database rather than trusting the evidence file. Non-blocking note: the implementer's report's full-suite pass/fail counts don't match a strictly read-only DB role's reproduction (4 files/10 tests, not 1/7) — annotate, not a defect. | `e1bb466fd` | **CONFIRMED** at `review-results/RR-20260914-008/review.md` |
| F026 | Medium (gate coverage) | `spec-card.ts` authors genuine Layer-1 runner-facing prose (NOTE table, `cardForUnprescribableType`, `cardWithoutSpec`) with zero `check-coach-voice.sh` coverage — confirmed unaffected by edits to its NOTE table. Structurally identical to the already-carved `projection-trend.ts` exception. Found during F023's fix, specified in full by David. | **CLOSED — EXTERNALLY CONFIRMED** (`RR-20260914-010`) — this unit's first independent check of any kind, since it had no internal reviewer pass before submission; the external reviewer confirmed the gate genuinely scans this file and genuinely catches a real violation. Non-blocking integration note: this branch and F023's (`RR-20260914-009`) both touch `spec-card.ts` on disjoint lines — normal merge check when both land on the same base. | `b209114503` | **CONFIRMED** at `review-results/RR-20260914-010/review.md` |
| F027 | Low (non-blocking, tracked from F023's confirmation) | `why-voice.ts`'s `whyClauses()` gating the QUALITY-phase opener on `todayIsQualitySession` removed an accidental "never blank" guarantee on `derivePurpose`'s fallback — not currently reachable in production (`derivePurpose` always returns non-empty facts today per F023's own independent review), but now a latent, unenforced coupling rather than a structural guarantee. | Open, non-blocking, low priority — surfaced by RR-20260914-009's external CONFIRMED verdict, tracked here per this register's own "never let a finding disappear" rule rather than left as a dangling note in a closed unit's evidence. | — | — |
| F028 | High (owner direction) | No proactive notification when a plan-changing correction lands — David confirmed PW-20260914-001 landed correctly (Tue/Thu show corrected "EASY 5mi" copy) only after force-quitting and reopening the app; there was no push/in-app signal telling him anything changed. David's exact words: "if the plan does update then how would I, the runner, know?" Directly violates his own standing rule (already on record: "pace changing, plan changing, etc" is explicitly listed as when to notify; "I dont ever want to cause panic, plan regression, or random plan change without warning"). The write mechanism (cache eviction via plan-version bump) works — there is simply no delivery-side notification layered on top of it. | Open — queued behind the adaptation-mechanism investigation per explicit priority ordering, not yet dispatched | — | — |
| F029 | High (owner direction) | Week-strip day-color indicator disagrees with the day-detail card. Thursday 9/17's corrected day card shows EASY (green gradient) correctly, but the week-strip's M-T-W-T-F-S-S underline above it is still orange (the prior quality/intervals color) — two surfaces showing different facts about the same day, same shape as prior "two screens disagree" findings. Suspected cause (not yet confirmed): the week-strip may read from a separately-cached source that didn't receive the same plan-version-bump cache eviction PW-20260914-001 relies on for the day-detail view. Confirmed via David's real post-correction screenshots. | Open — queued behind the adaptation-mechanism investigation per explicit priority ordering, not yet dispatched | — | — |
| F031 | Blocker (Rule 21 / core mission) — root cause confirmed, doctrine-resolved | **Exact gate found**: `web-v2/lib/plan/adaptation-authority.ts:113` — `AUTOMATIC_ADAPTATION_AUTHORITY: false`, David's own deliberate 2026-09-02 ruling. **CLOSED as a bug — working as designed.** Coach-consultant doctrine ruling on the Q1 sub-question (should a race-triggered rebuild reprice via ordinary evidence churn): **not a defect** — "compute at read time" (Rule 10) has no exception for "unless a race also happened that day"; freezing it specifically because it moved down on a race night would be the same asymmetry CLAUDE.md already warns against elsewhere. Q3 (evidence-clock scoping, structurally important on its own) split out as its own finding: **F038**. | Seal: closed, working as designed. | — | `for coaching consult/consult-log/2026-09-14-006-progression-mechanism-reconciliation.md` |
| F039 | RESOLVED — coincidental convergence, not a bridge; seal confirmed intact | Live-executed the real `resolveThresholdCapacity` and confirmed by import-graph grep: the 09-03 rebuild and the `adaptation_shadow_log`'s nightly cron are two independent call sites of the SAME pure, side-effect-free canonical resolver (Rule 10's intended architecture, not a leak). Zero code path anywhere reads FROM either shadow table INTO `generate.ts`/`capacity-resolver.ts`/`pace-corpus.ts` — both tables are write-only from the engine's side, read-only (admin diagnostic) from the operator's. Live proof: the rebuild's persisted anchor (vdot 47.8, pace 430, confidence to 15 significant figures, 3 evidenceIds in order) is byte-for-byte identical to the shadow cron's independent call 11 hours earlier against the SAME unchanged `runs` evidence — exactly what one pure function called twice on unchanged inputs produces, not evidence of a hidden handoff. `recompute_paces` confirmed still excluded from `PROPOSABLE_KINDS` (F031's seal independently reconfirmed) — nothing dispatched touched or will touch this. | RESOLVED — no fix needed, this is correct architecture; reported to programme lead per their explicit ask | — | `programme-internal-working/00-master-programme/F039-F040-SHADOWLOG-INVESTIGATION-2026-09-14.md` |
| F040 | **CLOSED — EXTERNALLY CONFIRMED (RR-20260914-021) AND DEPLOYED** (`774d7b2d4`, then re-fixed for a real ACTIVEPLAN-1 gap and confirmed live at `9fbc79543`) | Live-execution ruled out both original hypotheses and found the actual mechanism: `buildPrescriptionRunMatches` never populated a `version_rows` field `day-resolver.ts`'s Pass 1b needs to alias a run stamped against a now-archived plan version. **Reconciliation against F038's landed fix determined this precisely (not assumed): F038's reign-stitching fix closes this defect as a side effect ONLY for dates where the reign-winning row happens to be the same row the run was stamped against (2026-09-08 — confirmed) — NOT in general (2026-09-01 — confirmed still broken under F038 alone, because the run was stamped against a DIFFERENT archived plan version's row for that date than the one reign-stitching picks; see new finding F047).** A small, contained follow-up (populate `version_rows` via the same `jsonb_agg` pattern already used elsewhere in `day-resolver.ts`) was implemented on top of F038's commit and live-verified: both dates now match correctly. Honest caveat, not a loose end: re-evaluating `THRESHOLD_PACE` with the corrected sessions still reads REFUSE — but now for two separate, pre-existing, correct reasons (09-01 excluded for hills/heat without a trusted adjustment; 09-08 grades INSUFFICIENT due to the already-documented missing work-duration parser) unrelated to F038/F040. The matching defect is genuinely closed; the shadow log does not start approving as a result, and that is reported plainly rather than implied otherwise. | Open — fix implemented and live-verified, pending submission for external review (combined with F038, same file/branch chain) | — | `programme-internal-working/00-master-programme/F038-F040-RECONCILIATION-2026-09-14.md` |
| F041 | Low (informational, not confirmed as a defect) | A real six-session stretch (2026-07-07 through 07-21) where the prescribed pace target held steady while every session missed it, with no visible response in either shadow-log table — both tables' coverage starts too late to include this window. Flagged by the coach consultant as a genuine blind spot, explicitly NOT confirmed as a defect (the mirror-image question to everything else in this thread — missed-target evidence, not earned-progression evidence). Recorded per this register's own "never let a finding disappear" rule rather than left as a footnote inside a consult-log answer. | Open, non-blocking, informational only — no investigation dispatched | — | `for coaching consult/consult-log/2026-09-14-007-full-history-doctrine-application.md` |
| F046 | Medium (real, masked pre-existing defect, surfaced by F034's fix — NOT a regression from F034 itself; now seen a second time independently) | `_rolling_seven_ceiling.test.ts`'s historical CIM fixture stopped breaching its own ceiling once F034's headroom-reserve fix gave the block real spendable headroom — exposing what looks like a pre-existing `layoutWeek` day-placement discontinuity that was previously masked because the block was authored at 99.7-99.8% of its ceiling (no room to reveal a placement bug that only shows once real headroom exists to place). Flagged by the F034 fix implementer as a background follow-up rather than papered over or silently left failing; not investigated further here. **Second sighting (RR-20260914-031, F074's review):** the same test file failed on an unrelated full-suite run, independently flagged by the reviewer as "very likely the same defect" rather than opened as a duplicate finding — a real Rule-9 cliff signature in `generate.ts`'s ramp logic, per that reviewer's read. Not yet confirmed identical (same test file failing isn't proof of the same root cause), but worth investigating together rather than as two separate open items. | Open — surfaced twice now (background task `task_6dbc913b`; RR-20260914-031), not yet investigated to confirm whether both sightings share one root cause | — | `programme-internal-working/00-master-programme/F034-FIX-2026-09-14.md`; second sighting noted in `for external review/review-requests/RR-20260914-031/` |
| F045 | High (CONFIRMED BROKEN, not lag — real ingestion-path exclusion, affects every Faff-watch-sourced run) | Weather enrichment check for David's run today (`-184425083906740`) found this is not enrichment lag. The nightly cron's row-selector (`enrichRecent()`, `web-v2/lib/weather/openmeteo.ts`) only matches 4 GPS field shapes and never checks `routePolyline` — but `web-v2/app/api/watch/workouts/complete/route.ts` (the ingest endpoint for every Faff-watch-sourced run) only ever writes `routePolyline`, never the scalar/array fields the cron filter checks. So every polyline-only run is structurally invisible to the batch job, not slow — confirmed live: of 37 polyline-only runs in David's history, 17 have NEVER been enriched (`weather_enriched_at` still NULL). The 20 that did get enriched came from an entirely separate, unscheduled path (`lib/coach/run-state.ts` lazily enriching whenever that run's detail is opened in the app) — not the cron working as designed. Cron ledger confirms the job runs on schedule and finds zero eligible rows site-wide, so simply waiting will not fix this run; it will hit the same exclusion again tomorrow. | Open — confirmed broken, fix not yet dispatched | — | `programme-internal-working/00-master-programme/WEATHER-ENRICHMENT-CHECK-2026-09-14.md` |
| F048 | High (CONFIRMED, David's own deliberate on-device test, centerpiece finding) | David deliberately cut rep 6's stride recovery short today (31s actual vs 60s prescribed, `recoveryEndedEarly`) to test whether the app reads this as a positive readiness signal or a compliance miss. **Live-traced honest answer: nothing in the currently-shipping code interprets it as either — it is recorded correctly and unread everywhere a runner or coach would see it, for two concrete, live-confirmed wiring gaps, not a missing doctrine.** A correct, deliberate mechanism for exactly this scenario already exists (WALKBACK-2, 2026-09-09) but is inert in practice: (1) `resolveWorkoutVerdict` (`lib/execution/verdict.ts`) reads `spec.rep_rest_s` for the prescribed recovery duration, but an easy+strides `workout_spec` (this run's actual shape) stores it under `strides_recovery_s` instead — live-executed with the real spec exactly as the real caller passes it, confirmed `recoveriesHonestOf` returns `null` (no signal), flips to a real true/false when the field name is corrected. This makes WALKBACK-2 unreachable for every easy+strides session — the exact session shape its own code comment cites as motivating it. (2) On the phone, `TodayAfterV5.completionNote()` is the one function that would compose the positive sentence ("0:31 of 1:00 · advanced early"), but only fires when `sectionPieces` (the GPS-route-keyed breakdown) is empty — today's run has a real GPS route, so `sectionPieces` wins and uses a different path that returns nil for recovery phases with no knowledge of `recoveryEndedEarly` at all. `RunDetailV5.swift` (run-history screen) has zero awareness of this mechanism. Per Rule 11, this is explicitly NOT "nothing flags it as wrong so it reads as positive" — it is genuinely unread, a fourth fact distinct from both. | Open — two concrete wiring gaps identified, fix not yet dispatched | — | `programme-internal-working/00-master-programme/REP6-RECOVERY-INTERPRETATION-2026-09-14.md` |
| F047 | Medium (real, distinct, unregistered — surfaced incidentally during F038/F040 reconciliation, not re-diagnosed) | A run's `planWorkoutId` stamp appears to resolve against whichever plan was ACTIVE at completion/sync time, not the plan that actually reigned over the run's own calendar date. Live evidence: for 2026-09-01, two different archived plan versions each carry a real `plan_workouts` row for that date (`wko_eaa8cfd7cb94310b` under the plan reigning `[08-31, 09-03)`, which covers 09-01; `wko_470a1327c80a75c3` under the NEXT plan, reigning `[09-03, 09-14)`, which does NOT cover 09-01) — and the run is stamped against the LATER plan's row, not the one whose reign actually covered the day it happened on. This is the same "which plan's clock am I reading" confusion class F038 fixes on the READ side, appearing here on the WRITE side instead. Consequence: `ownedDaysSql()`'s reign-stitching (the correct, doctrine-established resolver) and a run's own stamped `planWorkoutId` can point at two different, both-real rows for the same date — which is exactly why F040's `version_rows` fix (aliasing every version, not just the reign-winner) was necessary rather than optional. Not re-diagnosed beyond this — the write-time stamping mechanism itself needs tracing to confirm the exact cause. | Open — surfaced, not investigated | — | `programme-internal-working/00-master-programme/F038-F040-RECONCILIATION-2026-09-14.md` |
| F043 | High (CONFIRMED — complete absence of a restore path, not a partial gap) | Two independent voice-cue engines exist, one per device, sharing no code: phone (`TreadmillCueEngine.swift`, treadmill-only — the phone has no voice-cue capability for outdoor runs at all) and Watch (`SpokenCues.swift`, the one that actually fires during real outdoor runs — confirmed live-shipping source via a symlink from `native-v2` into `legacy/native`). Both engines latch `AVAudioSession.setActive(true, options: [.duckOthers, ...])` on a one-shot boolean at the first cue, and **nothing anywhere ever calls `setActive(false)` or any deactivation** — no `AVSpeechSynthesizerDelegate` conformance exists on either class, so this isn't a partial-coverage gap, it's a complete absence of a restore mechanism. Strongest evidence: the codebase's OWN separate `ChimePlayer.swift` (Watch tone chimes) has a code comment describing this exact symptom verbatim and was already fixed with a `deactivate()` method wired into `WorkoutTracker.swift`'s `end()`/discard paths — `SpokenCues` never got the equivalent fix. Fix scope: add the same `deactivate()` pattern to `SpokenCues`, wired the same way; equivalent for `TreadmillCueEngine` via `LiveRunTreadmillV5`'s `.onDisappear`. Not device-verified (static source trace only, stated honestly per Rule 13). | Open — confirmed, fix not yet dispatched | — | `programme-internal-working/00-master-programme/F043-AUDIO-DUCKING-NEVER-RESTORES-2026-09-14.md` |
| F042 | High (hypothesis PARTIALLY confirmed — real, but self-imposed, not OS throttling) | Located the real Watch source (`native-v2` symlinks to `legacy/native`). One single clock drives everything: `WorkoutEngine.swift`'s `tick()`, one Task loop, feeding every countdown, the elapsed clock, guardrail rotation, AND the phase-advance/haptic-firing decision itself. Manual-timer-loop category confirmed (zero uses of `Text(timerInterval:)`/`ProgressView(timerInterval:)` anywhere in the app) — but the throttle is **self-imposed by the app, not watchOS fighting it**: `startTimer()` explicitly sleeps 5s instead of 1s whenever `isLuminanceReduced` (wrist-down/Always-On dimmed) is true, per two dated, commented engineering decisions ("WRIST DOWN COSTS LESS"). This produces exactly David's symptom (updates every 5s while dimmed, then an instant catch-up jump on wrist-raise, bounded ≤4s stale) — but by design, not by an OS mechanism the app failed to account for. Confirmed systemic across every live display (elapsed clock, guardrails, splits, drift samples), matching his "in general" framing. `WKExtendedRuntimeSession` ruled out — the app already correctly uses `HKWorkoutSession`/`HKLiveWorkoutBuilder`. Real constraint for any fix: the interval-advance decision rides the SAME throttled loop as the display, and `Text(timerInterval:)` fires no callbacks, so display and decision logic would need to be split, not just swapped. Not device-verified (source trace only, stated honestly per Rule 13). | Open — mechanism identified (self-imposed 5s throttle, not OS-imposed), fix not yet dispatched — this is a real design trade-off (render churn vs. live-feel) that may need David's call on whether to keep it, not purely a bug | — | `programme-internal-working/00-master-programme/F042-WATCH-TIMER-THROTTLING-2026-09-14.md` |
| F038 | **CLOSED — EXTERNALLY CONFIRMED (RR-20260914-021) AND DEPLOYED**, confirmed live at `9fbc79543` | **Scoping investigation live-traced every named mechanism and narrowed this substantially from its original framing.** Confirmed real and live-reproduced across today's actual plan rebuild: `canonical_adaptation_shadow_log`'s `WEEKLY_VOLUME`/`LONG_RUN` levers (`web-v2/lib/adaptation/canonical-shadow/live-input.ts`) DO reset their historical evidence window on every rebuild — `readPlan`/`readPlanWorkouts` scope to the single active plan only instead of reusing the already-correct cross-plan-reign query (`owned-days.ts`'s `ownedDaysSql`) that solves this exact class of bug elsewhere. Live proof: `WEEKLY_VOLUME`'s measurable-week count dropped 2→1 and `LONG_RUN` flipped from a real HOLD to "0 recent long runs to read" at the exact instant of today's rebuild, despite continuous real training throughout. **But the two mechanisms the original framing named as affected are NOT — both independently live-traced and confirmed immune**: `progression-pass.ts`'s ACCELERATE gate already uses the correct reign-stitched query (`distinctEvidenceWeeks` went 5→6 across the same rebuild, i.e. it kept counting), and `adaptive-ramp.ts`'s `tryAdaptiveBump` has no 3-week plan-relative dependency at all (its `belowTierUpper` gate is a legitimately current-plan-scoped question, not a "weeks of evidence" one — see F034). **Net effect on David today: zero** — the defective code feeds only a diagnostic shadow-log table with zero live consumers, nothing a runner sees or a production write reads. Real bug, small and contained fix (one file, reuse existing `ownedDaysSql` for the backward-looking branch only; forward-looking reads correctly stay current-plan-scoped), suitable for a normal implementation pass, not a foundational one. | Open — correctly scoped, safe to dispatch as a normal implementation pass (zero live consumers = no regression surface on David's actual plan); NOT the wall denying him a push — F034 is | — | `programme-internal-working/00-master-programme/F038-EVIDENCE-CLOCK-SCOPING-2026-09-14.md` |
| F034 | Blocker (Rule 21/22 / core mission — RESOLVED to a structural finding, fix requires David's/coach's judgment call) | **All 6 gates before the write call now traced live against real production data via `tsx` execution against `DATABASE_URL_RO` — not a coding bug, a structural "wall dressed as five individually-defensible bars."** 6 of 7 "active users" are QA/seed accounts with zero runs ever recorded — their gate failures are correct, not evidence of an app-wide defect. For David (the only real account), two gates independently read false today: (1) `lastQualityOnPace` — genuinely fluctuating (6/18 sliding pairs passed over 90 days), correctly false today because one interval session on 09-03 was cut to 34% of planned volume and correctly earns no progression credit; not a wall on its own. (2) `belowTierUpper` — **the structural finding**: `composePlan` spends the tier ceiling down to 99.7-99.8% of its own recomputed ceiling at every authoring event BY DESIGN (module's own header names this as intentional), and with David's plan rebuilt 3 times in 2 weeks, the narrow window where live demonstrated peak could out-run the ceiling before the next rebuild keeps getting reset. Same shape as F038 (a plan-authoring-relative mechanism standing in for runner-scoped reality). No single gate/number is wrong; ACWR (0.69, well clear), `lastLongClean`, and `noBumpRecent` all pass for David and are ruled out as blockers. 4 unimplemented fix options identified (reserve headroom at authoring time; compare against the specific upcoming week not the whole cycle's peak; loosen the 2-of-2 quality-session requirement; reduce rebuild-triggered re-anchoring frequency) — none built, all require a physiology/product judgment call this diagnostic pass was explicitly told not to make. **CLOSED — EXTERNALLY CONFIRMED (RR-20260914-022) AND DEPLOYED**, confirmed live at `0e0a0cd36` (the app's core upward-progression mechanism can now actually open real headroom at authoring time — coach consultant directed Option 1 only, 0.06 reserve empirically calibrated against the full 11,687-archetype corpus) | `0e0a0cd36` | `programme-internal-working/00-master-programme/F034-FIX-2026-09-14.md` |
| F032 | Medium (Rule 10/20, narrowed by doctrine ruling) | **DOCTRINE-RESOLVED.** The "44.1 vs 46.6" tension is a false conflict — two resolvers, two questions, disjoint evidence, confirmed via live code execution to 15 significant digits (`VDOT-466-PROVENANCE-2026-09-14.md`). Coach consultant additionally traced `season_anchor_vdot`'s actual consumers (`generate.ts:10388-13348`) to resolve a real tension between the two source investigations: **David's actual prescribed pace (`t_pace_s_per_mi`) is confirmed race-blind and correctly fresh — `vdot_last_reviewed`'s dormancy does NOT touch it.** But a SEPARATE stored field, `pace_blend.season_anchor_vdot` (shape of a goal-gap/progress-display payload, paired with `goal_vdot`/`measured_progress_fraction`), genuinely IS the stale, 4-month-frozen value, mislabeled `season_anchor_source: 'measured_vdot'`/`provisional: false`. Real Rule 10/20 violation, but its blast radius is progress-display honesty (whatever reads this for the Progress/Gap screen), NOT actual training pace. **Directionally important**: both real races feeding the dormant cascade (AFC, Santa Monica) computed 44.1 — LOWER than 46.6 — so fixing this would likely move a displayed number DOWN, not represent a denied push. Tracked as its own actionable finding, F037. Open question the consultant flagged and can't resolve alone: whether AFC's raw 44.1 was a clean result or had its own prep-gap discount like Santa Monica's — worth checking if anyone wants to weigh both races as real corroborating signal. | Doctrine-resolved — see F037 for the actionable fix | — | `for coaching consult/consult-log/2026-09-14-006-progression-mechanism-reconciliation.md` |
| F037 | Medium (Rule 10/20, progress-display honesty, not pace pricing) | `pace_blend.season_anchor_vdot` is stored as a frozen 4-month-old value (`vdot_last_reviewed`, last touched 2026-05-19) but labeled `season_anchor_source: 'measured_vdot'` / `provisional: false` — asserting current-measured confidence on an onboarding artifact. Feeds whatever consumes `pace_blend`/`measured_progress_fraction` for goal-gap/progress display (not confirmed which screen — check `fitness-trajectory.ts`/`GapPanel` per prior session memory). Fix direction: either recompute this field live like `t_pace_s_per_mi` already correctly does, or relabel its `source`/`provisional` fields honestly if it's meant to stay a distinct, deliberately-slower-moving anchor. Note: fixing this will likely move the displayed number DOWN (both real races compute 44.1, below the current 46.6), not up — this is an honesty fix, not a progression unlock. | Implemented (branch `fix/f037-season-anchor-vdot-honesty-2026-09-14`), submitted for external review at `RR-20260914-035` — not yet reviewed. Consumer trace corrected: `fitness-trajectory.ts`/`GapPanel` do NOT read this field (confirmed by grep) — the real defect was write-side, `generate.ts` trusting an inherited anchor forever with no freshness stamp. Fixed via Option A (recompute live): new 84-day freshness gate (`isAnchorStampExpired`, cited to Research/01), missing stamp = expired (Rule 11); `t_pace_s_per_mi` confirmed untouched (appears only in comments in the diff). Rule 18 falsified. Known gap: no live DB numbers confirmed yet (sandboxed worktree lacked `DATABASE_URL_RO`), synthetic fixtures match the register's cited real numbers. | `20804eb63e0387ed2458265a0aa3c1855f51beba` | `for external review/review-requests/RR-20260914-035/` · `for coaching consult/consult-log/2026-09-14-006-progression-mechanism-reconciliation.md` |
| F033 | **CLOSED — EXTERNALLY CONFIRMED (RR-20260914-020) AND MERGED TO MAIN** (`5d113fb06`, deploy pending confirmation) | David's 2026-09-01 threshold session (clean execution — HR fully in-band, full duration credit, fastest of the 4 sessions supporting the current threshold anchor, all 4 reps beat target pace) is discounted to 0.75 corroboration weight, but the discount is an ARTIFACT, not earned. Live-executed the real `pace-corpus.ts` against `DATABASE_URL_RO` (same method as the VDOT-466 investigation): the entire 0.75 comes from `contextFactor = PRESCRIBED_WINDOW_AUTHORITY = 0.75` firing because 09-01 falls inside a mechanically-computed 14-day pre-race "taper" window (`TAPER_WEEKS_BY_DISTANCE['10k']=2 weeks` × 7, whole-week rounding) — but CLAUDE.md's OWN already-documented doctrine (Research/08 §9.1) says a genuine 10K taper tops out at 10 days. 09-01 sits 12 days pre-race — 2 days before the real taper would even start; it's caught only by the week-rounding overreach, the same "two windows in this app disagree" class CLAUDE.md's Rule 8 section already names for a different pair of tables. Consequence: correcting this doesn't flip the corroboration-count gate (already clears k=3), but it would raise the weighted threshold-pace read and confidence — i.e. the fix points toward MORE readiness to progress, not less. Exactly the "bar for upward evidence is quietly higher than it looks" asymmetry Rule 21 warns about. | **Coach consultant confirmed: no coaching judgment blocks this, pure taper-window granularity artifact, fix it** — same divergence class CLAUDE.md's own Rule 8 section already names for a different table pair. Fix dispatched. | — | `programme-internal-working/00-master-programme/SEPT1-THRESHOLD-WEIGHTING-INVESTIGATION-2026-09-14.md`; consultant confirmation in `2026-09-14-006-progression-mechanism-reconciliation.md` |
| F035 | Blocker (owner direction, PERMANENT override) — **fix implemented and verified, pending commit/submission** | David, directly and today, permanent and unambiguous: "Regardless I don't want it there. Ever." — a standing override of the design brief's rule that modelled/estimated values carry a visible amber "~" marker. **Fix implemented across TWO live render sites, not one**: `ValuesV5.swift`'s `FaffValueText.body` (the canonical render path, app-wide) and a second, previously-unidentified live site, `PacesMovedV5.swift`'s `modelledCaption`, found by an app-wide grep the brief specifically asked for. `Theme.V5.modelledMark` deleted outright (not left as dead code). VoiceOver's "estimated..." announcement is unchanged — only the visible glyph is retired. `check-modelled-mark.sh`'s guard flipped from enforcing presence to enforcing absence and widened to catch a bare `modelledMark` reference (the literal-tilde-only regex would never have caught how the mark was actually drawn) — falsified in both directions per Rule 18. **Render-verified (Rule 13) on a real compiled build** via the app's own `-faffV5Screens` debug catalog (real binary, real types, sample data, not a fixture that skips the code path): the canonical "Projected 3:16:45" example renders with no glyph. Second site (`PacesMovedV5`) verified by source read + successful compile, honestly NOT by a second on-device screenshot (simulator navigation issue, disclosed plainly rather than glossed over) — no live-account render was possible since the app requires email+password sign-in and no password is ever entered. Docs updated (`faff-iphone-design-contract.md`, the external design handoff README, `PRODUCT_DECISIONS.md`) with dated override entries, history preserved not deleted. | Open — implemented, self-verified by the implementer (not yet externally reviewed); pending commit to an isolated worktree and RR submission | — | `programme-internal-working/00-master-programme/TILDE-REMOVAL-PERMANENT-2026-09-14.md` |
| F036 | RESOLVED (process integrity — confirmed fabrication) | David disputed that he ever gave the "explicit instruction" quoted in commit `adcea6f15` ("MARKER-RESTORE-1", 2026-09-09) as the basis for restoring the tilde — his words: "that was a mistake or something out of context." **Independent investigation confirms: no real source exists anywhere in this project's history for the disputed quote.** It appears nowhere except the disputed commit and a same-session decision-log entry written one minute later restating the same unsourced claim — not independent corroboration. Its content is a close first-person paraphrase of the already-written design brief's own rule, its connective reasoning rebuts an argument only the commit's own author could have been reading in that moment, and its formal sentence style is categorically unlike every other directly-quoted, dated David statement on this exact code path (`"we dont need the tilde. its obvious and implied the number is calculated."` — lowercase, contracted, real dictation — vs. the disputed quote's three polished declarative sentences). Conclusion stated plainly, per the F019 honesty standard: an agent fabricated an attributed instruction, most likely by sliding from "the brief says X" to "David said X" while writing the commit message, not through intent to deceive but fabrication all the same. | RESOLVED — honest finding reported to programme lead and David without softening; no further investigation needed. Substance is superseded regardless by David's fresh, direct, permanent instruction ("I don't want it there. Ever.") — see F035. | — | `programme-internal-working/00-master-programme/TILDE-QUOTE-PROVENANCE-INVESTIGATION-2026-09-14.md` |
| F030 | Resolved — not a bug | The amber "~" tilde mark is live on David's real installed TestFlight build 292 on the pace band/HR ceiling and stride table. **RESOLVED: this is working as intended, not a defect.** The tilde was retired 2026-08-21, then deliberately, explicitly RESTORED by David on 2026-09-09 (commit `adcea6f15`, "MARKER-RESTORE-1", quoted: "modelled numbers must visibly carry the amber `~` marker... Do not close this as 'VoiceOver carries estimated.'"). Nothing has touched that code since — build 292 (9/14) is correctly showing the 9/9 decision, 5 days later. Full rendering path traced and independently re-verified via a real Simulator build against the exact `main` tip build 292 shipped from. `CLAUDE.md`'s design-doctrine line describing the tilde is accurate, not stale — no correction needed. | Working as designed | `programme-internal-working/00-master-programme/TILDE-DISCREPANCY-INVESTIGATION-2026-09-14.md` |
| F044 | Medium (data-integrity — a stored value the runner and the coach both consumed is wrong, not just imprecise) | Today's run stored `avgCadence: 143` (stride 5) and `avgCadence: 133` (stride 6) on two 20-22 second stride segments, and the coach consultant built a real "compressed recovery costs him first" narrative on top of those numbers before anyone checked plausibility. **Confirmed wrong, not just suspect, by two independent lines of evidence.** (1) Data-quality: each stride phase carries only 4-6 total HR samples across the whole interval (~1 reading per 4-5 seconds) and there is no raw cadence-sample array in the run JSON at all — `avgCadence` is a single stored scalar with nothing behind it to verify against, unlike HR. (2) David's own direct, unprompted testimony, live in this session: "there is no way in hell I ran the last strides at 143 or 133 cadence" — his real range is ~155 (lowest, even fresh) to 165 (typical target) to ~170 (fast efforts); 130s "NEVER happens." He also confirmed effort felt equal across all 6 reps, route was his normal flat route with nothing unusual, and no fatigue/nutrition factor — ruling out the obvious alternative explanations for a real cadence collapse. **Root cause traced (best-supported, code-and-data-evidenced, honestly not 100% certain):** stride phases stream from the Watch app's `WorkoutTracker.swift`, where per-phase `avgCadence` time-weight-averages a raw, unsmoothed mirror of CoreMotion's `CMPedometer.currentCadence`, refreshed only when CoreMotion's own lag-prone callback fires. Critically, cadence has NO staleness watchdog, while HR and pace in the SAME file both do (`lastHrSampleAt`/`hrStaleAfterSec` zeroes a frozen HR reading after 20s) — on a multi-minute interval a few seconds of CoreMotion lag is negligible; on a ~20-22s stride it can be 25-50% of the whole window. Data confirms the shape: every "Walk back" recovery phase reads 113-127 spm; strides 1-4 read 160-165 (matches David's real range); strides 5/6 (143, 133) sit exactly between the walking and running bands — consistent with a phase-boundary lag artifact, not a real physiological collapse. The 5-second `paceSamples` telemetry (which IS persisted, unlike cadence) shows an identical ramp-up shape in ALL six strides including the good ones, since pace self-corrects as a true distance/time ratio while cadence's live-snapshot averaging doesn't — and rep 6's preceding recovery was also cut short (34s vs. the normal ~62-64s), consistent with a timing mismatch specific to that rep. Honestly could not fully confirm (no raw per-second cadence timeline is persisted anywhere to verify against, unlike HR/pace) — states plainly that a brief, unrelated CoreMotion gait-detection glitch confined to those two reps shares the identical code path and data footprint and cannot be ruled out. Fix shape identified, not implemented: (1) add a cadence staleness watchdog mirroring the existing HR pattern; (2) persist a per-phase cadence timeline mirroring `phaseHrSamples`/`phasePaceSamples` so a future case is verifiable rather than inferred. The "compressed recovery costs him first" framing remains retracted. | Open — root cause traced, fix identified but not implemented | — | `programme-internal-working/00-master-programme/F044-AVGCADENCE-SHORT-INTERVAL-2026-09-14.md` |

| F050 | High (real, LIVE, currently-reachable defect on real production data — not hypothetical) | Today's run-consumption trace surfaced this live on the actual run being traced: the post-run recap and Today card both tell David "The plan moved after this run" — but the responsible `coach_intents` row (`vdot_auto_recalc`) fired 04:04:34 UTC, ~17 HOURS BEFORE this run finished (~20:57 UTC), from an unrelated same-calendar-day plan rebuild. Root cause, live-confirmed: (1) `postrun/load.ts`'s adaptation lookback is calendar-day granular (`ts >= dateISO::date AND ts < dateISO::date + 2 days`), not gated on the run's own clock time, so anything anywhere in the run's calendar day — including hours BEFORE it — gets swept in as "after this run"; (2) the clarifying-clause mechanism specifically built 2026-09-05 to prevent exactly this confusion only fires when `evidence.role === 'CORROBORATES'` — this run's role is `CONTEXT_ONLY`, so the clause silently skips, even though the underlying confusion is the identical shape it was built for; (3) independently, the `vdot_auto_recalc` row itself carries a genuinely empty `why` field. **Sharpened by external review, sharper mechanism than originally stated**: the calendar-day-granular lookback was deliberately widened on 2026-09-02 to avoid missing a late cron pass — a real tradeoff, not an oversight — but as a side effect it can't distinguish "before this run, same day" from "after this run." The clarifying sentence itself already EXISTS and is correct (`describeAdaptationCause('vdot_auto_recalc')` returns "your race result recalculated your fitness baseline directly") — it's just never reaching the runner because this event's role is `CONTEXT_ONLY`, not `CORROBORATES`. And the "empty why" isn't a blank string: `coach_intents.value` for this row is the bare string `"44.1"`, not a JSON object — `JSON.parse` gives a number, `.why` on a number is `undefined`, and the safe-why guard correctly returns null for a shape it was never built to see. **Confirmed NOT the same root cause as F038/F040/F047** — those are run-to-plan-workout matching across plan versions; this is event-to-run attribution via calendar-date proximity instead of real clock ordering. Same mistake family, different files, no shared fix. Fix shape: filter the lookback on the run's own `startUtc` instead of/alongside the calendar-day bound, and reconsider the `CORROBORATES`-only gate for reason codes like `vdot_auto_recalc` that carry a self-contained cause regardless of evidence role. Net effect unchanged: David reads a plan-change notice attached to an unrelated, earlier event, with zero explanation — live, right now. | Open — root cause fully traced and independently verified; fix not yet dispatched, queued behind tonight's higher-priority threads | — | `programme-internal-working/00-master-programme/RUN-CONSUMPTION-TRACE-2026-09-14-184425083906740.md` |
| F049 | High (real, precisely-scoped gap — confirmed by direct code read, not inferred) | Coach consultant investigation: an easy run's HR-ceiling verdict (`easy-discipline.ts`) compares only the run's AVERAGE HR against `hr_cap_bpm` — today's run averaged 142 against a 151 cap and was marked "hit" despite 41 real seconds above the cap late in the phase (see F044/HR-ceiling debrief). A genuine drift-aware mechanism does exist (`hr-thirds.ts`, splits a run into early/middle/late thirds, flags real drift >8bpm) but it's wired only into the weekly adaptation/progression pipeline, not into the per-run easy-discipline check — so a single run's own "did I stay in the zone" verdict never sees it. Even where the thirds mechanism does run, it works on per-mile HR averages, not raw ~5-second samples; the codebase's own comment in `activity-evidence.ts` names this exact limitation unprompted ("may never look like an interruption" for a short excursion inside a mile) and calls the finer-grained fix "a future pass" — i.e. openly not yet built, not something dug out by inference. Today's run is a clean, real, live example of exactly what this gap predicts. Fix shape (wire finer samples into `easy-discipline.ts`, or build a separate simple "N consecutive seconds over cap" check for easy runs) is an implementation call, not yet made. **Addendum (design review, finding 005): a cheaper, more-accurate fix may already be sitting live on the watch — `WorkoutEngine.swift`'s `hrOverCeiling` flag checks live HR against the ceiling every tick, correctly, honestly (no sticky false-positives), but is pure ephemeral UI state that's discarded every run instead of persisted. Persisting its transitions (or a running over-ceiling-seconds counter) into the run payload may be simpler than reconstructing drift from mile-level or 5-second samples after the fact. Source-traced, not directly observed firing on today's run (circumstantially corroborated via the run's own maxHr/hrZonePcts/absence of a ceilingLift field).** | Open — not yet dispatched; fix approach may be simpler than originally scoped, per design review's addendum | — | `for coaching consult/consult-log/2026-09-14-010-hr-drift-detection-gap.md`; `for design review/findings/2026-09-14-005-watch-already-knows-the-hr-ceiling-breach-live-and-throws-it-away.md` |

| F052 | High (Rule 19 class incident — full prebuild-chain failure ran undetected for ~11 hours) | `main`'s deploy pipeline failed 3 consecutive times (13:02, 13:17, 14:34 local) on a pre-existing `check-coach-voice.sh` violation (an em dash in `native-v2/Faff/Faff/ViewsV5/HostsV5.swift:2942`, unrelated to any of the commits it was blocking) before anyone noticed — last successful deploy before this incident was 04:37 local. **Confirmed via Railway's own deployment metadata (commit SHA per build, not assumed): 4 commits sat undeployed for ~11 hours, not just F033** — `0f3c8e2d5` ("Today/Block never go outage-shaped, global stale banner deleted"), `99ffd5918` (TestFlight build 292 record), `5d113fb06` (F033's fix), and the em-dash fix itself (`09406ad1a`) all landed together in the first successful build (15:37 local, deployment `02eed8e9`, confirmed SUCCESS via `railway deployment list --json`, not inferred from the push). Fix: trivial, mechanical, punctuation-only (split one sentence per Rule Four's own guidance), verified against the full local ~30-gate prebuild chain before pushing directly to `main` (treated as the Rule-19 operational-incident case per CLAUDE.md's own bucket, not routed through the RR queue, since it carried zero coaching/logic/data risk and was actively blocking already-confirmed production work). Recorded per the LTHR-incident precedent (Rule 19's own worked example) — this is the same shape: a gate chain failure that ran silently until someone happened to check. | RESOLVED — fixed and confirmed deployed (SUCCESS, `02eed8e9`); a repeatable "when did main last actually deploy" check is being built as a standing follow-up so this isn't discovered opportunistically next time | `09406ad1a88641e452b479053a392867bfeda123` | Deployment confirmed via `railway deployment list --json` |
| F051 | **CLOSED — EXTERNALLY CONFIRMED (RR-20260914-023) AND DEPLOYED**, confirmed live at `9fbc79543` | External reviewer independently ran RR-20260914-021's own regression sweep rather than trusting its self-reported evidence, and found the packet's characterization of one test failure was wrong on every count claimed: reported as "1 unrelated timeout under parallel load, passes cleanly in isolation, byte-identical to origin/main" — actually a **deterministic** `_belief_source_pins.test.ts` hash-pin mismatch on `lib/training/pace-corpus.ts` (a file-hash check, no timer involved), reproduces **every time** in isolation (not intermittent), and the file is **genuinely not byte-identical to origin/main** — it carries F033's real, confirmed content change (RR-020) to what `contextFactor` resolves to for a real activity, still unmerged at time of this check. Root cause is substantive, not just a mischaracterized diagnosis: `shadow-evidence-epoch.ts`'s own pin mechanism exists specifically to force an explicit decision when a resolved value like this changes (bump `SHADOW_EVIDENCE_EPOCH`, or re-pin with an argued reason) — nobody has made that call yet, so the pin is correctly failing and was incorrectly explained away as a flaky timeout instead of surfaced. Does not block RR-20260914-021 (F038/F040, confirmed separately, different files) — but the underlying epoch decision for F033's change is still outstanding and needs an owner. **Mischaracterization source traced and owned**: it was the code agent (this session) — RR-20260914-021's evidence line was copied from a sub-agent's report and submitted without independent re-execution. Corrected at its root: independently computed `pace-corpus.ts`'s digest by hand, confirmed the mismatch is real, traced F033's actual diff, confirmed branch (a) (moves the 2026-09-01 session's resolved threshold pace 440→436 s/mi), bumped `SHADOW_EVIDENCE_EPOCH` to `2026-09-14.f033-taper-window-correction`, re-pinned with an argued reason in the file's own established style, falsified in both directions. | Open — fix implemented and self-verified (digest computed independently, Rule 18 falsified both directions), submitted for external review as **RR-20260914-023** given this is a judgment call | — | `programme-internal-working/00-master-programme/` (see RR-20260914-023 request.md for full trace) |

| F056 | High (real, precisely traced, David hit this live on his own phone — real priority) | A completed run doesn't appear on Today until force-quit/relaunch. Root cause traced precisely by external review: `FaffApp.swift` posts `.faffForegroundRefresh` twice on foreground (immediately, then again after `HealthKitImporter.importIfConnected` finishes — specifically to catch a just-landed run). `SurfaceStoreV5.swift`'s `ForegroundWork.shouldLoadOnForeground` coalesces the two into one `load()` if they land within 3s of each other (`foregroundLoadCoalesceSec`), measured from when the FIRST load STARTS, not when it finishes. If the HealthKit import completes in under 3s (plausible — REQUESTSTORM-2's own notes cite comparable 0.7-2s round-trips), the second, load-bearing post gets silently swallowed and the first load already ran on stale "no run yet" data — nothing re-fetches after that. Force-quit resets `lastForegroundLoadAt` to `.distantPast`, which is why that "fixes" it and a plain foreground doesn't. **Third instance of a related bug family in this exact code** (original single-throttle bug, then REQUESTSTORM-2) — each fix moved the throttle without re-examining whether the coalescing window itself could swallow a load-bearing second post. Fix direction: the window should originate from load-FINISH (or the import's own completion), not load-start — or the second post should bypass coalescing entirely when it's specifically the post-import one. Unconfirmed, worth ruling in/out before calling a fix complete: `WatchSync.swift`'s own completion-relay queue (~line 208-229) has an independent 60s throttle that may not reliably trigger `.faffForegroundRefresh` when it drains — a second possible route to the same symptom if David was wearing the Watch. **Fix landed.** Bypass mechanism chosen over the window-edge fix (confirmed the window-edge approach would have made it WORSE — the HealthKit import and the surface's own GET run as concurrent tasks, so a finish-stamped throttle produces a SMALLER gap than start-stamped on ordinary timing). WatchSync path traced: the literal 60s-throttle hypothesis was NOT confirmed (real delivery callbacks bypass it directly), but a real, independently-reachable second route to the identical symptom WAS found (`flushPendingCompletions()`'s own post shares the same notification/window) and fixed with the same mechanism. Verified via a real integration test against the actual NotificationCenter/observer code path on a real booted Simulator, falsified per Rule 18 (reverted, reproduced the exact symptom, restored, confirmed green). | **CLOSED — MERGED TO MAIN.** Standing merge authorization confirmed real via David's Desk, directly from David, 2026-09-15 (CLAUDE.md, locked). Composed at the exact reviewed SHA (`53fdbb21d`), rebased cleanly onto current main (no conflicts, content byte-identical, verified by direct diff), full `FaffTests` suite re-run twice clean (552/552, uncontended) before push. Fast-forwarded onto `main` at `2d9c0cae865ecf31393ecf5886ec16554af1fb2a`. | `2d9c0cae865ecf31393ecf5886ec16554af1fb2a` | `programme-internal-working/00-master-programme/F056-FOREGROUND-REFRESH-COALESCING-2026-09-14.md` |
| F055 | **UPGRADED — Medium, CONFIRMED RECURRING, not a one-off** | `check-action-kinds.sh` has now FAILED on Railway twice tonight, on two different, unrelated commits, each time reporting a DIFFERENT set of missing `GENERATOR_REGISTRY` keys (first: 10 keys including DURATION_PROGRESS_OFFER/FREQUENCY_CHANGE/HOLD/etc.; second: ADD_WORKOUT/REFUSAL/RESCHEDULE/WORKOUT_TYPE_CHANGE) — neither commit touched `facets.ts`/`GENERATOR_REGISTRY`/anything this gate scans. Both times reproduces 0/1 locally against the identical commit content. This script is a pure grep/awk scanner with explicitly no TypeScript toolchain dependency (its own header states it runs "on a cold container with no TypeScript toolchain") — it should be fully deterministic against a given file, so genuine non-determinism on Railway's builder specifically (not reproducible against identical local content, different specific failures each time) points at real infrastructure flakiness in Railway's build environment itself — a stale/inconsistent cache mount, or a git-checkout race on the builder — not a bug in the script's own parsing logic. Both occurrences resolved cleanly on an immediate retry with zero code changes. Real enough now to name plainly, not real enough yet to have a fix (nothing here is under this project's own control to patch — it's Railway's build infrastructure, not the check script). | Open — recurring, cause not fully diagnosable from application code; retry has fixed it both times; worth flagging to Railway/infra ownership if it happens a third time | — | — |
| F053 | Low-Medium (real lexicon gap, confirmed on BOTH iOS and web, found via proactive coach-voice audit) | `HostsV5.swift` (iOS) has five distinct error cases (offline, timeout, server error, cancelled, unknown) that all end in the bare phrase "Try again." with no qualifier — "software with nothing to say," exactly the failure mode the coach-voice lexicon's guard 5 exists to catch. **Confirmed NOT iOS-only**: `components/faff-app/views/TodayView.tsx:5438` has the identical bare-fallback shape ("Could not save. Try again."). The lexicon bans "please try again" and "try again in a moment" but not the bare phrase itself — a substring-matching gap, not a missing rule, and a per-string iOS-only patch would leave the same problem live on web. Also confirmed clean and NOT a false-positive risk: `'That did not save. Nothing was written, so it is safe to try again.'` (9 files) explains what happened and earns its "try again" — do not bundle that pattern into this fix. Fix direction: patch the lexicon itself (ban bare "try again", or flag any short string ending in it with nothing substantive before it) so both platforms are caught by one rule, plus fix the specific five iOS strings and the one web string to actually say what happened and reassure training data is safe. **Scope expanded further, real, not just more sites**: `HostsV5.swift`'s settings-error type has a SECOND string table with the identical defect — a sibling `shortCause` property (same fact "said as a clause rather than a whole screen," per its own comment, used for inline partial-load banners), 5 of 6 cases also ending in bare "Try again." A fix touching only the already-reported `message` property leaves `shortCause` broken — check for this sibling wherever the fix lands. Two more independently confirmed sites, same shape: `RPEV5.swift` (2 call sites) and `BlockV5.swift`. One near-hit correctly ruled out by reading context rather than trusting a grep match: `DecisionsSectionV5.swift`'s apparent bare "Try again." is actually part of a real two-line good-pattern string. | Open — not yet dispatched; full confirmed scope: HostsV5 (2 tables × 5 cases), TodayView.tsx, RPEV5 (×2), BlockV5 — separately track F079 (different root cause) | — | `for coaching consult/consult-log/2026-09-14-013-proactive-voice-audit-01.md` |

| F054 | Medium (real, well-reasoned candidate — code-level only, not yet rendered/confirmed) | Independent trace, NOT the same panel David reported (his was the Easy/green panel, already checked clean on a fresh build — see F050-era gradient-seam thread). The Sep 8 gradient-discontinuity fix (`DesignV5/PanelV5.swift`, 3-pass smoothing kernel over 65 stops) was tuned and verified against the Easy/rest/phase/long ramp's middle-stop geometry (76%) — but Quality and Race panels use a DIFFERENT middle-stop location (52%, a deliberate David-ruled choice for text-clarity reasons, not itself a bug) via `Theme.V5.DayState.darkInkLocations`. The shared, global easing-hint constant (`hints = [0.34, 1.08]`) is applied identically regardless — landing at ~44.7% relative position on the tuned Easy geometry but ~65.4% on Quality/Race's actual [0,0.52] segment, a meaningfully different curve than what the smoothing pass was calibrated to erase. Race compounds this further with its own slightly different raw CSS hints. Net: Quality and Race panels may carry a smaller-but-real version of the same discontinuity seam the Sep 8 fix was believed to have closed everywhere. Decisive test: render Quality or Race day-state and check for a hard line near the panel's actual halfway point (not 76%). **On-device render (real Threshold/quality day, real data, clean build): no visible seam** — full-panel inspection, a 2x-stretched crop of the 55-85% band (covering both 76% and the predicted 65.4% locations), and a pixel-level second-derivative scan all found nothing above the grain-noise floor. The stop-geometry mismatch this finding identifies is still real and verifiable in the code; it just isn't producing a measurable visual defect under this test. Best inference: the Sep 8 fix's 3-pass smoothing is strong enough in practice to keep this residual case below visible threshold too — inference, not proven. | Checked — real code-level mismatch confirmed, but no visible on-device defect found; not a live bug at this time, no fix needed unless a real report surfaces | — | `for design review/findings/2026-09-14-004-today-gradient-seam-not-reproduced-on-fresh-build.md` (addendum) |

| F057 | High (real, currently live on David's app, part of the fix already exists and just isn't merged) | Pre-existing post-race copy audit (before tonight) found 9 wrong sentences in post-race recap copy: 1 outright false, 5 misleading, 2 unsupported, 1 contradictory. Coach consultant verified directly against `origin/main` (git ancestry check, not trust): **none of the U4 fix work has shipped** — `lib/postrun/experience.ts` on main was last touched 2026-09-12, before the race that copy describes even happened. All 9 sentences confirmed still live verbatim via exact-string grep against main, not paraphrase, including #2 (an unconditional "some landed inside, some did not" claim that fires even at zero hits) and #5 (its contradictory pairing). **4 of the 9 (#2, #3, #5, #7) already have a fix — falsified, verified, sitting unmerged on `u4-postrace-truth` + two correction branches + a classification-v3 branch.** Merging closes those four with zero new implementation. The other 4 (#1, #4, #8, #9) were never in U4's scope and have no fix started. Caveat for whoever merges: U4's own demo screenshots won't reproduce exactly since the Santa Monica `goalOutcome` evidence has moved on since the report was written — not a defect in the fix. **Real final state traced independently** — the 4 named branches resolve to only 2 distinct commits, not 4. Correctly EXCLUDED a second commit (correction-v2) that would have required an out-of-scope adaptation-engine belief-source-epoch decision (trips 2 production doctrine gates), confirmed via an empty diff that the 4 target sentences don't depend on it. Rebased cleanly onto current main, all falsifying tests pass (126/126, 2 DB-gated skips). **EXTERNALLY REVIEWED: CHANGES REQUIRED (RR-20260914-027).** Independently confirmed #2/#5/#7 are genuinely fixed (old wording still live on main, new conditional wording in the diff, byte-identical cherry-picks re-traced patch-by-patch rather than trusting the packet). But **#3 ("Under review.") is only half-fixed** — `readPlan`'s new `reviewWindowElapsed` branch still returns `HELD_FOR_EVIDENCE` either way, that fact is never threaded onto the wire, and `WorkoutResultV5.swift` (the file that literally renders "Under review.") isn't even in the diff's 23 changed files. So once a review window elapses — the exact case this fix targets — a runner sees "Under review." right next to a Why panel that now honestly says nothing further is scheduled: a fresh, undisclosed contradiction, same shape as the #5 defect this whole thread exists to catch. Small, well-scoped gap, not a rejection of the approach — #2/#5/#7 need no further work, resubmission can keep the rest of the diff. **#3 gap closed**: `reviewWindowElapsed` threaded through `PostRunPlanImpact` → `PostRunWire` → the Swift-decoded model → `WorkoutResultV5.swift`'s `planStatusLine`, which now renders "No further review scheduled." once elapsed instead of unconditional "Under review." Verified: 102/103 tests passing, `tsc` clean, real `xcodebuild` BUILD SUCCEEDED. Resubmitted as **RR-20260914-029** (supersedes RR-027). **RR-20260914-029 CONFIRMED** — all 4 target sentences (#2, #3, #5, #7) now fully closed, independently re-verified. | **CLOSED (sentences #2/#3/#5/#7) — MERGED TO MAIN.** Same standing-authorization basis as F056. Composed at the exact reviewed SHA (`1854eae9e`), rebased twice onto an advancing main (5 commits, both times clean, content re-verified byte-identical file-by-file each time), full `FaffTests` suite re-run clean (552/552, uncontended) before push. Fast-forwarded onto `main` at `945f4a3b611b0a1131833658bf8406e316104901`. #1/#4/#8/#9 still need new work, not started (separate, smaller effort, unchanged). | `945f4a3b611b0a1131833658bf8406e316104901` | `programme-internal-working/00-master-programme/F057-SENTENCE3-WIRE-FIX-2026-09-14.md` |

| F058 | Medium (real, render-confirmed on David's real data, one-line fix) | Races screen has always shown the blue Long-run gradient instead of the orange/red Race gradient the design brief specifies — render-confirmed on David's real CIM data (Goal 3:00:00 / Projected 3:22:49 / Gap +22:49), screenshot unambiguous. Same shape as F029: a documented, self-flagged discrepancy nobody verified once real data existed. `RacesV5.swift`'s own file header (written before `/api/v5/races` existed) already flagged that the prototype's CSS disagreed with the brief on this color; the real route (`app/api/v5/races/route.ts:427`) landed later with `dayState: 'long'` hardcoded, matching the prototype's literal value rather than the brief the Swift comment had already flagged. Nobody closed the loop once the real payload existed. The Swift file's own Xcode preview data uses the correct `'race'` value, so anyone previewing in Xcode has seen it right while the shipped app shows it wrong — likely why it went uncaught visually. Fix: one line, `dayState: 'race'` instead of `'long'`. | Open — fix implemented, verified (tsc/tests/gates clean, downstream logic confirmed color-only), submitted for external review as **RR-20260914-024** | — | `for design review/findings/2026-09-14-006`; fix at `for external review/review-requests/RR-20260914-024/` |

| F059 | Medium (real, location now confirmed by David's own annotated screenshot — likely a DIFFERENT mechanism than F050/F054's gradient-stop hypothesis) | David's original gradient-line report, now precisely located: a thin horizontal band right below the status bar / dynamic island row, above the "TODAY" label — i.e. very near the TOP edge of the screen, not mid-panel. This does NOT match either previously-tested location (the Sep 8 fix's 76% stop, or F054's predicted ~65.4% stop on Quality/Race panels) — both of those are well down into the panel body, not at the very top edge. Design review's fresh-build render test (finding 004) covered full-panel + a 55-85% band crop and found nothing, which is now explained: it was looking in the wrong place entirely. Working hypothesis, not yet confirmed: this may not be the `PanelV5.swift` gradient-stop discontinuity mechanism at all — a seam at the very top edge, right at the status-bar boundary, is a classic signature of a SEPARATE status-bar/safe-area overlay view not color-matching the gradient beneath it, or a scroll-stretch/rubber-band region, rather than a CSS easing-curve math error. Needs a fresh on-device look specifically at that top band, not a re-check of the existing 55-85% crop. | Open — precise location now known, needs a fresh targeted render/trace at the correct location | — | (pending) |

| F060 | Low-Medium (real Rule 17 finding, presentation layer only — confirmed not a data bug) | Settings' Coach Decisions history shows the same HOLD decision card twice, word-for-word identical ("Holding the plan as it is" / "the upcoming long run already carries a race-pace segment") — once marked STILL OPEN/WAITING ON YOU, once SETTLED/EXPIRED. Design review checked whether this was a data bug before flagging it: queried `plan_workout_proposals` directly, confirmed it is NOT a bug — genuinely two distinct rows (id 10, raised 9/7 against the old 9/20 long-run workout; id 15, raised 9/14 against the new workout after a plan rebuild superseded the old one). Root cause: card title/reasoning are fixed per-action-kind/per-evidence-facet template strings, so two legitimately different events produce byte-identical rendered cards. Real Rule 17 finding ("the runner reads a sentence once") on the one screen whose purpose is proving the coach's decision-making is trustworthy — a settled card identical to the currently-open one adds nothing and reads as a glitch even though the underlying data is correct. Fix direction: the card template needs to include something that actually differentiates the two events (which workout/date it was raised against, or similar) rather than only the fixed reason text. | Implemented (branch `fix/f060-duplicate-coach-decision-card-2026-09-14`), submitted for external review at `RR-20260914-034` — not yet reviewed. Presentation-layer only, no backend change: `dateISO` was already unconditionally on the wire and already decoded, just never drawn — added a small subtitle ("About the session on Sep 20.") to differentiate cards. Rule-18 falsified. | `3e6af3f8b208df76cbe653a9217ae82262540d68` | `for external review/review-requests/RR-20260914-034/` · `for design review/findings/2026-09-14-007` |

| F061 | High (real, directly observed BOTH states on the same account, no "unconfirmed" caveat needed) | Shoes screen silently shows zero shoes on a failed fetch, with no error/retry UI at all — design review directly observed both states minutes apart on David's real account, no code change between them: first load showed empty "IN ROTATION" straight to "Add a pair"; second load rendered all 5 active shoes correctly. Independently confirmed via `GET /api/shoe` that David has 8 real shoes (5 active, 3 retired) the whole time — the data was never gone. Root cause, read directly: `ShoesHostV5.load()` in `HostsV5.swift` is `shoes = (try? await API.fetchShoes())?.shoes ?? []` — ANY failure (network blip, timeout, decode error) silently becomes "you own zero shoes," with no error state, no retry, nothing. Textbook Rule 11 (a failed read must never present as a real empty state). The fix pattern already exists ~200 lines away in the same file — `SettingsLoadFailure`, added 2026-09-07 for exactly this reason, per David's own quoted instruction at the time: "Do not swallow the error or replace every failure with 'Can't reach faff.'" Whatever specifically triggered this instance (design review's local test server was slow) is incidental — the code has no failure path other than silent-empty regardless of cause. | Open — not yet dispatched, existing fix pattern in the same file to reuse | — | `for design review/findings/2026-09-14-008` |

| F062 | Medium (real plain visual defect, on every Block load for every runner — NOT scoped as accessibility, per David's standing rule: no dedicated accessibility work until told otherwise) | A stray fault-colored "—" dash sits permanently under the QUALITY phase headline on Block, every load, unconditionally — not state-dependent, visible to every sighted user. Traced precisely: `GET /api/v5/block` correctly sends `dose: null` (Block covers a 16-week phase, not a day, so it has no dose concept). But `BlockV5.swift`'s panel unconditionally does `FaffValueText(model.panel.dose.unreadableIfAbsent, ...)`, and `unreadableIfAbsent` maps nil to `FaffValue.unreadable`, rendered with the fault color — a nil value that should draw nothing at all is instead shown as a visible error indicator to every user, every time. The correct pattern already exists in the same codebase: `TodayBeforeV5`'s shared `HeroDayPanelContentV5` component handles this exact shape correctly (`if let dose {...}`, draws nothing when absent), with its own header comment naming precisely this case. `BlockV5` duplicates the hero-panel layout inline instead of reusing that shared component, and the duplication is where it diverged. **Follow-up confirmed the other 3 `unreadableIfAbsent` call sites are clean, not a cluster**: `RaceDetailV5.swift`'s 3 sites are already correctly guarded (`showsColumns()` gates on `isEmpty==false` before `unreadableIfAbsent` ever runs — same fix already landed there previously, with its own comment "AN UNSET GOAL IS NOT AN UNREADABLE ONE"); `RacesV5.swift`'s 1 site is lower-risk by construction (dose only null in a case that doesn't occur on real data). F062 is confirmed isolated to Block. | Open — not yet dispatched, existing correct pattern in the same codebase to reuse; confirmed isolated to Block, no other call sites affected; fix as a normal correctness bug at normal priority | — | `for design review/findings/2026-09-14-009` |

| F063 | High if real, unconfirmed (source-traced only, honestly disclosed) — could affect a brand-new runner's literal first screen | Same `unreadableIfAbsent` bug class as F062 may reach `OnboardingHostV5.submit`'s day-one reveal poster: it runs the identical `today.panel.dose.unreadableIfAbsent` conversion, and `OnboardingRevealPanel` draws it with no guard, same unguarded shape as Block. If a new runner's day one happens to land on a rest day (not confirmed impossible), their very first screen after finishing signup shows a stray fault dash instead of their prescription. Design review traced this from source only — explicitly NOT render-confirmed, since reaching it needs a genuinely fresh signup state not available against the walk-substrate this pass. **Reachability confirmed real, not moot**: design review read `lib/plan/generate.ts:8445`'s "get them running on day one" mechanism directly — it's a deliberate mitigation that relocates an easy run onto the anchor day when it would otherwise be rest, but its own header comment names two real no-op cases: a low-frequency week that's all long+quality with no easy day to relocate, and an anchor day unavailable per the runner's stated days with no earlier day available either. A low-frequency onboarder, or one whose available days don't align with the anchor, can genuinely land day one on rest. **Code agent's own investigation found a deeper, more general root cause than the two no-op cases above**: the safeguard's guard clause (`generate.ts:11539`) checks `input.startMondayISO`, but per WEEK-ALIGN-1 (dated 2026-08-24) that value is now always the training-week BOUNDARY (day after the runner's long-run day), not the actual visible first day the runner sees. For the common case of a Sunday long run, the boundary is always Monday, so the guard's `getUTCDay() !== 1` check is always false and `frontLoadFirstRun` **never runs at all**, regardless of what day anyone actually onboards on — not just the two narrower no-op cases already named. For other long-run days the guard does fire, but targets the boundary's weekday, not today's, so it can "fix" a day that gets clipped away as pre-signup while the real day-one slot is untouched. Confirmed via `_audit_placement.test.ts` still exercising the pre-WEEK-ALIGN-1 contract (passing the literal onboarding day directly) rather than the boundary-snapped value production now supplies — no test currently proves this didn't reopen. Code-trace only, no executed failing fixture produced. | Implemented, both causes fixed (branch `fix/f063-dayone-doseless-plan-2026-09-14`), submitted for external review at `RR-20260914-032` — not yet reviewed. Cause A: `OnboardingRevealPanel`'s dose dash was rendered via `.text(_:color:)` sugar, leaving it at `V5.fault`'s off-panel red (measured, contrast-failing on all six ramps) instead of the panel's own ink — fixed to call `FaffValueText` directly with `mark`/`fault` pinned to `V5.OnPanel`, matching `HeroDayPanelContentV5`/`BlockV5.swift`/`RacesV5.swift`. Cause B: `frontLoadFirstRun`'s guard read `startMondayISO` (always the WEEK-ALIGN-1 boundary) instead of the runner's actual first day — fixed via a new optional `ComposePlanInput.firstOwnedDayISO`, wired only at the real onboarding call site, byte-identical for every other caller (self-caught a near-miss mid-implementation where a naive fallback would have silently disabled the mitigation for non-migrated callers — caught by the full-suite digest check, fixed by running the old guard verbatim in the fallback branch). Rule 18 falsified for both causes. | `e9997e9f091450f2dd30fe9e14c15d519eff5138` | `for external review/review-requests/RR-20260914-032/` · `for design review/findings/2026-09-14-010` |

| F064 | Medium (real, DEPLOYED confirmed but NOT LIVE-VERIFIED — a genuine gap between those two states) | The composeReviewTrigger coach-voice fix is correctly deployed — confirmed via the real API that David's actual `reviewTrigger` text is exactly the fixed version (real numbers, no "doctrine" jargon). But it is currently invisible on his actual rendered screen: `BlockV5.swift:421-430` gates the whole reviewTrigger block on `thesis.coachLine` being non-empty, and on his real account right now that field is empty — a different field from the top-level `model.coachLine` (which does render: "Your races fade with distance faster than your speed predicts..."). So the fix is correct and shipped, but a sibling gate is currently suppressing it entirely, independent of the fix's own quality. Not asserting the gate itself is wrong — may be intentional (don't show the footnote without its own headline) — but real and worth an explicit decision rather than assuming "deployed" means "seen." Found via design review correctly re-doing an earlier check after realizing their first pass compared against a stale local branch, not origin/main — self-corrected plainly, not defensively. | Open — needs a decision on whether the `thesis.coachLine`-empty gate is intended behavior; not yet dispatched | — | (pending, design review's re-check) |

| F065 | Medium (real, render-confirmed on Watch via the app's own real-engine simulation harness, not a fixture) | Watch stride board shows "STRIDE 3 OF 6" as a huge headline AND "Rep 3 of 6" as a smaller line directly underneath — the same count stated twice, in two words, on the watch's tightest screen. Render-confirmed via `_SessionSim.swift` (`-sim <archetype> -at <seconds>`), the app's own built-in harness mounting the real `WatchRunSurfaceV5` driven by the real engine, not a fixture. `WatchRouterV5.swift`'s `phaseBoard()`/`phaseName()`/`phaseContext()` looks like the current, doctrine-compliant path — a sibling component (`PhaseFaceV6`) has its own comment describing fixing this exact redundancy shape once already ("removing the name entirely is what actually fixed it") — but what actually rendered doesn't match what that path should produce. Two real, unresolved possibilities, honestly flagged rather than guessed at: a literal `phase.label` field bypassing the router's own name logic, or a still-reachable pre-V6 board. Root cause not yet pinned down. Separately: reused the same harness against David's known 2026-09-02 recovery-dead-end plan shape (`-sim recovery-end -warp 60`) — ran through to overtime cleanly, no stuck state, consistent with that fix holding (though the transitional recovery board itself wasn't caught before the warp passed it, so not a full confirmation of that specific board). **Root cause now fully pinned down** (full read of the board view files first, ruling out the persistent-phase-board path): `FacesPhaseV5.swift`/`FacesRunV6.swift` confirmed genuinely clean — phase name/metric role are accessibility-only BY CONSTRUCTION, never a visible `Text`. The real redundancy is a DIFFERENT board — the transition MOMENT takeover, not the persistent phase board. `WorkoutEngine.swift`'s `advance()` work-phase-entry branch (~line 1749) does `flash(.phase(title: p.label, sub: sub), for: 1.6)` where `p.label` is the phase's own stored label (literally "Stride 3 of 6", confirmed in `_SessionSim.swift`'s builder) and `sub` is independently computed as "Rep 3 of 6" (the `hasBand` branch drops pace and leaves a bare rep-count). Both reach `WMomentPhaseChange` as real, visible Text (38pt `WDisplayWord` + 22pt Text directly below) — not one accessibility-only as originally suspected. A nearby comment (`REPCOUNT-1`) describes fixing a related bug (the rep count used to be computed twice and could disagree) — that fix made the two numbers always agree, which likely made this redundancy harder to notice, but didn't remove the underlying "same fact, two phrasings" problem. | **Root cause confirmed, ready for implementation.** Fix: when `p.label` already carries a "N of M"-shaped label, `sub` shouldn't restate the count — either suppress it or drop the redundant prefix when `hasBand` is true. | — | `for design review/findings/2026-09-14-011`, root-cause trace in `SETTINGS-AUDIT-2026-09-14.md` |

| F066 | **SPLIT AND RESOLVED into two separate outcomes — racebail CLEARED (confirmed working), thresholdbail CONFIRMED REAL AND STILL BROKEN.** Finding 022, clean re-test per the agreed plan: same approved watch UDID rebooted (`simctl shutdown`+`boot`, no new device/permission), FaffWatch rebuilt fresh from current source (the on-disk `.app` predated this by 3 weeks), both archetypes re-run with the same ~110s polling rigor as the original test, well past the mile-10 gate and 120s sustain window. **Racebail: FIXED.** Bail board fired correctly — "MILE 10 HEART RATE OVER 163 / The A goal is gone from here · run the B plan..." with Cut it short / Run it out — confirming the device-state confound (the stale Motion & Fitness dialog) fully explains the original failure; the mechanism itself works. **Thresholdbail: STILL BROKEN**, same device, same rebuild, same session — HR mocked 180-191bpm the whole window against a 173 threshold, bail board never appeared, workout ran deep into overtime (34mi on an 8.5mi interval session), identical to the original test. This is the clean split result the pending re-test was designed to produce: proves the honest downgrade was correct (racebail's mechanism was never broken, the test setup was) AND proves thresholdbail has a real, distinct, separately-caused bug that survives the exact fix that cleared racebail. The two archetypes diverge exactly where their code does — racebail is mile-gated (`scope: "mile-10"`, `switch_to_b_goal`), thresholdbail isn't (`scope: "work"`, `drop_to_easy`); since the shared sustain-accumulation machinery (`ruleScopeReached`, `ruleBreachSec`, `shouldOfferBailNow`, `offerBailIfDue`) is now proven working end-to-end via racebail, the bug is most likely specific to the work-scope/`drop_to_easy` path, not the shared core. | **Device-level closed (Rule 13 satisfied) — both archetypes confirmed working on real hardware/simulator, pending external review before merge.** Design review rebuilt FaffWatch fresh from the working tree (confirmed the fix's presence via `git diff` first), same approved rebooted UDID, ran `-sim thresholdbail -warp 60 -hr 185` with the same ~110s polling rigor as the split test — well past the 120s sustain window. Bail board fired correctly: "HEART RATE OVER 173 AND STILL C.../ The stimulus is already banked · forcing the rest of the reps buys fatigue, not fitness." with Cut it short / Run it out. Racebail was already confirmed working. Both archetypes now device-confirmed. `legacy/native/Faff/FaffWatch Watch App/WorkoutEngine.swift`, `tick()`'s `planComplete` early-return branch (was lines 1073-1079): `noteRuleMetric()` — the function that accumulates `ruleBreachSec`, which `shouldOfferBailNow` reads — was only ever called *after* that early return, so the instant a workout's phases finish (`planComplete = true`), every later tick short-circuits before reaching it and `ruleBreachSec` freezes. Shared-core bug, exposed differently by scale: racebail's real-time-anchored ~197s test window closes the 120s HR-sustain check *before* `planComplete` fires, so it never hits the freeze; thresholdbail's distance-anchored reps (`repUnit:.distance`) complete via a **fixed-rate mock accrual** in `WorkoutTracker.swift`'s `startSimulatorMock()` (`0.0045 * warp`, unrelated to the rep's own target pace) that finishes the whole set in ≈39 real seconds under warp — well under the 120s sustain window — so `planComplete` trips and freezes `ruleBreachSec` at ~39, permanently below the 120s threshold; the board can never fire and the session runs into overtime (the 34mi-on-8.5mi overrun). `ruleScopeReached`/`ruleScopeFromMile` and the `action` field (`drop_to_easy` vs `switch_to_b_goal`) were both traced and ruled out as the divergence — scope trivially returns true for non-`mile-N` scopes, and `action` isn't read anywhere in the Watch engine/router (display-only). **Fix**: moved the `noteRuleMetric` call into the `planComplete` branch too (~8 lines, comment cites F066) — extends *when* the existing check runs, changes no threshold/comparison, can't affect racebail's confirmed-working path (unreached there) or double-fire (`bailAnswered`/`pendingQuestion` guards intact). `xcodebuild ... -destination 'generic/platform=watchOS Simulator'` build succeeded, zero errors, no simulator booted per instruction (host was CPU-oversubscribed). Secondary harness-only observation logged separately as F088. | — | `programme-internal-working/00-master-programme/F066-RACE-BAIL-INVESTIGATION-2026-09-14.md`; split confirmed at design review finding 022; device-closed at finding 022 update; fix in working tree at `legacy/native/Faff/FaffWatch Watch App/WorkoutEngine.swift` |
| F066-ORIGINAL-CONTEXT (kept for history, superseded by the row above) | Real instrumentation (NSLog probes at every named link — `bailRule`, `ruleScopeReached`, `noteRuleMetric`, `shouldOfferBailNow`, `offerBailIfDue`, the `.onChange(of: engine.ruleBreachSec)` observer, `canOfferBail` — captured live via `simctl spawn log stream`, verified by actual screenshot per Rule 13) confirms the ENTIRE chain fires correctly: `ruleScopeReached` flips true exactly at mile 10, `ruleBreachSec` climbs 1-for-1 with wall clock, `shouldOfferBailNow` flips true at exactly 120s, `offerBailIfDue` FIRES and sets `pendingQuestion` correctly. Both named hypotheses ruled out with live evidence: nothing else occupies `pendingQuestion` (no routing conflict), and the `-hr` mock is confirmed to reach the same `tracker.heartRate` the real engine reads (no mock-injection gap). **The actual cause found**: a stale, leftover-from-a-different-build native watchOS "Motion & Fitness — Don't Allow" SYSTEM permission sheet was covering the entire screen at the exact moment of firing — an OS-level modal, invisible to and unroutable by the app's own `WatchRouterV5` interrupt-priority model since it isn't a `WInterrupt` the app can see or route around. Confirmed stale (its copy text didn't match the installed bundle's `Info.plist`), confirmed `simctl privacy grant motion` does not clear it, confirmed dismissing it by hand and re-running the IDENTICAL harness produces the correct `FaceBailOfferedV5` board verbatim ("MILE 10 HEART RATE OVER 163" / "Cut it short" / "Run it out"). **Real, durable, genuinely open architectural question this surfaces, NOT yet verified on real hardware**: if motion/fitness authorization is ever genuinely unresolved at race time on a real watch (not just this shared-simulator artifact), the same invisible-OS-modal-blocking-a-safety-board failure mode could occur for real, with no mechanism today for the app to detect or route around an OS-level permission sheet blocking safety UI. No fix implemented — correctly left as a real, deliberate architectural decision rather than rushed, given the safety stakes. **Reconciliation done, answered precisely and honestly, not defensively.** Design review confirmed both their racebail and thresholdbail tests ran on the IDENTICAL simulator device (no reboot between them) — a real shared variable they hadn't flagged when originally reporting "confidence raised by a second independent reproduction." They did NOT personally observe a Motion & Fitness dialog in any of ~15 screenshots across both tests, but their sampling was periodic (every 10-60+ seconds), not continuous — a transient dialog appearing and clearing between two captures would be invisible to them. **Honest, correct conclusion: this is no longer "two independent confirmations of a broken mechanism" — it's two failed reproductions on one device whose permission state was never directly verified, consistent with either a genuinely broken mechanism or the same environmental artifact.** Their own evidence doesn't distinguish between the two. Proposed next step, agreed: a clean re-test on the SAME already-approved device after a full reboot (`simctl shutdown` + `boot` on the existing UDID — not a new device, doesn't need a new permission grant) rather than continuing to debate the existing screenshots. | Open pending one clean re-test (reboot the existing approved device, don't mint a new one, re-run both racebail and thresholdbail); real root cause for the ORIGINAL racebail failure is still confirmed (the stale dialog was directly observed and fixed by dismissing it) — what's unresolved is only whether thresholdbail's separate failure shares that cause | — | `programme-internal-working/00-master-programme/F066-RACE-BAIL-INVESTIGATION-2026-09-14.md` |

**Second, independent reproduction confirms this is NOT one test's quirk.** `thresholdbail` (`-sim thresholdbail -warp 60 -hr 185`) is a structurally different rule from racebail — different kind ("bail" not "abort"), different action ("drop_to_easy" not "switch_to_b_goal"), different scope (ungated "work", not mile-10-gated), different threshold (173 vs 163), different workout shape (a short 4x1mi interval session, not a marathon). Same result: HR mocked ~190bpm continuously from launch through warmup and all four intervals, bail board never appeared, the whole 8.5mi workout ran to completion and into overtime with nothing ever offered. Two independent rule instances, different kind/scope/threshold/workout, both failing under conditions each one's own code says should trigger — much harder to explain as a test-setup artifact, much more consistent with a real break in the SHARED machinery both paths route through (`noteRuleMetric` → `ruleBreachSec` → `shouldOfferBailNow` → `offerBailIfDue`). | Open — urgent, confidence raised by cross-check; code agent already dispatched with runtime instrumentation, explicitly told not to just re-read source again | — | `for design review/findings/2026-09-14-012` |

| F067 | Downgraded from Blocker to known architecture debt — external review traced live reachability and found no active incident on either real delivery surface | Independent product review (IPR-20260914-001): `resolveRunnerState()` and `classifySafety()` (which can return STOP) never call each other, and the prescription resolver takes no safety input at all — the Brain Constitution's own §15 names this exact combination "forbidden." The codebase's own internal audit (`lib/runner-state/ownership.ts`) already tracks this `verdict: 'OPEN'` — genuinely known, just never surfaced in `CURRENT.md` or this register, so it never entered the process that actually gets things fixed. SOURCE-grounded on the architecture gap; TENTATIVE on live reachability (no downstream safety check found, but not every consumer was exhaustively traced). **External review traced live reachability precisely.** `resolveRunnerState()`/`classifySafety()` genuinely never call each other, confirmed — and `RunnerState`'s own type declares `'illness'`/`'injury'`/`'stop'` as valid values that `resolveRunnerState()`'s real implementation never populates, a real wired-but-disconnected slot. But BOTH actual prescription-delivery surfaces independently gate on safety correctly: the phone (`/api/v5/today/route.ts`) calls `resolveSafety()` directly and short-circuits before composing any prescription on injury/illness/unresolved safety; the Watch (`lib/watch/build-workout.ts`, SAFETYSTOP-1) separately calls `resolveSafety()` and correctly distinguishes MODIFY from STOP. No live incident possible today on either real surface — this is confirmed architecture debt (two independent hand-written copies of the same gate, not one shared arbitration layer; a future third surface could add itself without remembering the pattern — `/api/v5/block` currently has no gate at all, though nothing runnable is at stake there), not an active safety hole. **Independent product review reached the same conclusion separately, unprompted, and self-corrected its own original framing accordingly**: `resolveRunnerState()` is explicitly documented as having zero authority ("nothing in this module writes, mutates a plan, or is called by any live path") — it feeds only a shadow comparison engine sealed off by `AUTOMATIC_ADAPTATION_AUTHORITY: false`, confirming the disconnection is real but was the wrong pairing to worry about for live risk. Two independent tracing efforts converging on the same downgrade is strong corroboration. One additional real note from that pass: the "safety stops the engine" unit test suite concedes in its own comments it never runs against a live database. Separately found in passing: the phone's injury branch blanks the WHOLE day's prescription for ANY injury driver, while the Watch correctly ships an easy session on MODIFY — over-conservative on phone, not a safety risk, but a real Rule-16-shaped cross-surface inconsistency worth fixing eventually. | Open, low urgency — real architecture debt (centralize the duplicated gate) plus the phone/Watch MODIFY-handling inconsistency; neither is a live incident | — | `for independent product review/reports/IPR-20260914-001` |
| F068 | RESOLVED — was a programme-lead verification error, not a false claim. F035's real work is genuinely complete and good | **Correction, same night.** The programme lead checked `docs/faff-iphone-design-contract.md` against `origin/main` and found the old tilde text still there, concluding F035's own "docs updated" claim was false. That check compared the wrong ref: F035's register entry never claimed to be merged — it explicitly said "Open — implemented... pending commit to an isolated worktree and RR submission." The actual work sits on `fix/f035-tilde-removal-permanent-2026-09-14` (commit `1334022f0`), never merged, so of course `origin/main` still shows the pre-fix content — that's expected, not evidence of anything wrong. External review traced the real branch and independently confirmed ALL of F035's claims are true and substantial: the design-contract doc carries a real dated override blockquote; `PRODUCT_DECISIONS.md` has an 88-line entry covering the full round-trip history honestly (including the F036 fabrication incident); the external design-handoff README is genuinely modified with the same note. Went further given F036's precedent and independently re-verified the code claims too (ValuesV5/ThemeV5/PacesMovedV5 diffs read in full and match; `Theme.V5.modelledMark` genuinely deleted; `TreadmillView.swift`'s compile-fix is real and necessary) and re-falsified the widened gate themselves (reintroduced a bare `modelledMark` reference, confirmed `check-modelled-mark.sh` catches it, restored clean). Everything F035 claims is genuinely true. The programme lead's own new verification-gate rule (checking a claim proves the EXACT wording, not a nearby one) is exactly what would have caught this before it went out — noted as a live example of why that rule exists, applied against the programme lead's own output this time. | RESOLVED — F035's disposition stands as originally written (implemented, pending RR submission); no further doc/code work needed, only the pending merge itself | — | `for independent product review/reports/IPR-20260914-002` |
| F069 | Medium (real, structural, genuinely undecided — not a bug to fix blindly) | Independent product review (IPR-20260914-003): the backend genuinely builds a full periodized plan for a goal without a booked race (Roadmap Priority 3's bar already met — good news). But `training_plans.mode` is hardcoded `'race-prep'` even for goal-only plans, so the gate meant to hide this from the phone (not ready there yet) never fires — Today/Block render as normal race screens, while Races independently 404s with unrelated wording. Real open product choice, not just a bug: David may prefer goal-only runners just use Today/Block normally rather than have the surface hidden — needs that decision before a fix direction is chosen. | **RESOLVED — David's ruling, direct, via David's Desk (2026-09-14 night), verbatim: "keep it hidden for now but flag that we do need to build a goal only option eventually."** Surface stays hidden/hardcoded as-is; a real goal-only mode is confirmed wanted, just not now. | — | `for independent product review/reports/IPR-20260914-003`; ruling via David's Desk |
| F070 | Low (informational — a coverage-tracking accuracy issue, not a product defect) | Independent product review: the 27-area coverage matrix marks Shoes as "NOT STARTED" — confirmed wrong, it's fully working end-to-end including the native screen. The row cited no evidence at all for its claim, which in hindsight was the tell. Worth a spot-check of other coverage-matrix rows for the same failure mode (an assertion with no cited evidence behind it). | Open — low priority, informational | — | (pending) |
| F071 | Low-Medium (real, scale bigger than expected, not urgent — roadmap already schedules dead-code removal last) | Independent product review: the native app ships an entire second, unreachable legacy UI tree (`TodayView`/`TrainView`/`SettingsView`/etc.) behind a debug launch flag no real user passes. Not urgent, but the scale is bigger than "some old files" — worth knowing before dead-code removal is eventually scheduled, not a surprise to discover then. | Open — not urgent, matches existing roadmap sequencing (Priority 7) | — | (pending) |

| F072 | High (real, well-scoped data-integrity risk — a proven fix already exists on the other platform) | Independent product review (IPR-20260914-004), Watch scan — mostly reassuring: phone and Watch genuinely agree on prescription (Watch decodes the same `lib/training/prescriptions.ts` output, computes nothing independently), offline execution is architecturally sound (WCSession persistence + network-free run engine + a crash/battery-death recovery snapshot), and the phone/Watch concurrent-recording lock already works symmetrically. One real gap: the Watch fixed same-day `workoutId` collisions in production back in July (`P1-34`) with a small `#HHmm` start-time suffix — never ported to the phone. `PhoneRunTracker.swift` documents its own overwrite behavior in its own comments ("same workoutId → same id, so re-POSTing overwrites"), meaning a same-day re-recorded run on the phone can silently overwrite a previous run's data. This is the precise cause behind a "live risk, currently reachable" the coverage matrix already names qualitatively for run execution. Backend already tolerates the `#HHmm` tail (built for the Watch), so this is phone-side-only work, small scope, with a working, proven pattern to copy rather than invent. | **CONFIRMED** at `RR-20260914-030` — reviewer independently traced the full mechanism (sessionSuffix match, backend regex tolerance, `stableBigintFromString` row-targeting) and independently falsified (reverted the suffix line, rebuilt, watched the collision reproduce, restored). 4/4 tests. **CLOSED — MERGED and DEPLOYED, full Rule 19 chain confirmed.** Merged to `main` at `e6fbd4045` under David's standing merge authorization (2026-09-14/15 night). External reviewer independently confirmed via `check-deploy-status.sh`: Railway's most recent deployment is SUCCESS at `245712f97d3f` (origin/main's tip, a direct descendant of `e6fbd4045`) — live in production, not just pushed. | `e6fbd4045ac2f40651c548efbb9e6fb738f2123d` | `review-results/RR-20260914-030/review.json` CONFIRMED; deploy confirmed by external reviewer, 2026-09-15 |

| F073 | Medium (product decision, David-directed — a UX guard complementing F072's data-integrity fix) | David, direct: "after a run is completed the watch should not allow you to run it again. can go to the stats and then also provide a 'just run' option if a 2nd run really does happen." Distinct from and complementary to F072 (same-day `workoutId` collision silently overwrites data if a second run does happen): this is the UX-flow guard that should make an accidental re-run unlikely in the first place, while still permitting a genuine intentional second run via an explicit "just run" escape hatch rather than blocking it outright. On completing a workout, the Watch's default post-completion action should route to stats, not straight back into "start this workout again"; a deliberate second run stays possible via a clearly separate action. **Confirmed on phone too, firsthand, not a guess**: design review tapped RUN on a real completed workout (finding 003's same run) and the RUN sheet still offered to start it fresh, no indication anything had already happened — same shape as the Watch complaint. **Flow spec delivered, one shared shape for both surfaces**: (1) default landing after completion is recap/stats — phone already does this correctly by construction (`TodayAfterV5`), Watch needs the same default; (2) the RUN entry point checks completion state (the same `after_run` signal Today's own screen already reads) before offering today's prescription as startable, swapping to a "done" state once true; (3) a genuine second run gets its own explicit, separate action — "Just run," reusing the app's existing no-goal casual mode rather than inventing new mechanism, never the default; (4) F072's data-safety fix stays load-bearing regardless — the flow reduces accidental second runs, doesn't replace the guarantee a real one is handled correctly. Approved as a faithful implementation of David's stated intent, not a new decision requiring separate sign-off. | **CONFIRMED** at `RR-20260914-030` — phone side re-verified clean (2/2 tests including exhaustive other-states-still-allow-Start check); Watch side confirmed correctly built against the real source (see F087 for a related, non-blocking source-tree finding surfaced during this review) via `xcodebuild -destination 'generic/platform=watchOS Simulator'`, no device needed. **CLOSED — MERGED and DEPLOYED**, same SHA/chain as F072. | `e6fbd4045ac2f40651c548efbb9e6fb738f2123d` | `review-results/RR-20260914-030/review.json` CONFIRMED; deploy confirmed by external reviewer, 2026-09-15 |

| F074 | High (real, on the app's actual front door, coverage matrix wrong for the third time) | Independent product review (IPR-20260914-005): onboarding collects 5 "fitness" answers from a new runner and silently discards most of them before the plan is ever generated. 3 of 5 are collected, shown back to the runner, then explicitly discarded before the network call (`_ = a.recentRaceDistance`; effort pace and time-off fields never referenced outside the onboarding file). A 4th (experience level) is sent but deliberately unread — the plan router's own comment: "ACCEPTED AND UNREAD... the parameter stays so `_declared_level_inert.test.ts` can keep turning this knob and proving nothing moves." Net effect: a runner with a real recent race time, a runner with a real known pace, a runner returning from time off, and a self-described beginner all currently get the identical plan. Framed as an open question, not a flat bug for all of it: the inert experience-level field might be a deliberate, defensible doctrine call (self-report ranks below measured evidence) — but that doesn't explain discarding recent-race-time and effort-pace data, which doctrine itself ranks as strong evidence, not self-report. Separately: the 27-area coverage matrix's "not reviewed" status for onboarding is now the third area (after Shoes, F070) found stale on inspection — worth a fuller spot-check of the matrix's own claims, not just individual rows as they come up. | **Doctrine-grounded analysis delivered, sharper than the original framing.** Coach consultant found this isn't actually 4 open questions — field 4 (self-reported experience level) is already SETTLED by a dated 2026-09-02 decision (`TIEREVIDENCE-2`, David's own words: "My actual history — not an onboarding label — must determine appropriate load," enforced and tested across 8 dimensions) — correctly inert, don't touch. Applying that same ruling consistently argues FOR fixing the other 3, not against: recent race time is doctrine's own rung-1 evidence (should write into `races.actual_result` through the existing pathway); effort pace should feed the plan as `USER_PRIOR` evidence via an already-shipped precedent (`coldStartThresholdCapacity` already accepts self-reported weekly volume the same way when no logged mileage exists); time-off/prior-mileage should use the same mechanism plus the existing detraining-discount doctrine (needs a new backend field, not new capacity logic). Three "wire to existing, proven machinery" fixes plus one "confirm settled, don't touch" — not four genuinely open design questions. | Implemented, all three fixes built (branch `fix/f074-onboarding-evidence-2026-09-14`), submitted for external review at `RR-20260914-031` — not yet reviewed. Backend-only; migration 172's two new `profile` columns are written but NOT applied (awaits David's per-statement go). Native wiring (all three fields) and `generate.ts` wiring are separately-tracked, explicitly out-of-scope follow-on work, stated in the submission. | `6f3d6919b80997f5888ed369b5a309c4d4db77ce` | `for external review/review-requests/RR-20260914-031/` · `for coaching consult/consult-log/2026-09-14-017-f074-onboarding-evidence-doctrine.md` |

| F075 | Real, render-confirmed, locked-brief conflict needing David's decision | Design review pinned down the exact on-screen mechanism behind F067's "phone over-conservative on injury" note. Inserted one synthetic minor-severity injury row into the LOCAL walk-substrate scratch DB only (not production, deleted immediately after, confirmed via DELETE 1), rendered David's real Today screen against it: a 56pt "NOT TODAY" headline sits directly above the engine's own correct verdict text ("Easy running only. The Left calf gets a few easy days before anything harder comes back.") — a direct on-screen contradiction, stop vs. easy-is-fine, one line apart. Root cause: NOT a code bug — `lib/plan/injury-builder.ts` genuinely distinguishes minor/moderate/major correctly and the verdict text correctly reflects a MODIFY-tier injury. The gap is that `InjuryFlareV5`'s "Not today" headline is FIXED text regardless of severity, and that's the LOCKED DESIGN BRIEF's own literal spec for this screen (13a: "Headline 'Not today'") — not something introduced later in code. Correctly flagged as a brief conflict per design review's own charter (never silently override a locked design system) rather than dispatched as a code fix. | **David's ruling (direct, 2026-09-14 night): "the design spec was only to set the base, it does not make the law. so if it doesnt make sense, change it."** Fix authorized — the locked brief's fixed "Not today" headline is not to be treated as immovable when it produces a real on-screen contradiction. | Open — dispatched: coach consultant to propose severity-tiered headline copy (MODIFY-tier minor injury needs something that doesn't contradict "easy running is fine" one line below, coach voice: short, direct, no hype), then code agent wires `InjuryFlareV5`'s headline to the same severity tier `injury-builder.ts` already correctly computes | — | `for design review/findings/2026-09-14-014` |

| F076 | Informational, positive — a real capability confirmed solid, not a defect | Independent product review (IPR-20260914-006): race continuity across a season is genuinely solid, not just assumed. Every plan-authoring pass re-derives from live history tables; named mechanisms (`RAMPBASE-1`, `CONTINUOUS-RESTORE-1`, `POSTRACE-RESTORE-1`) exist specifically to stop a new block silently resetting below where the runner already is, and mid-block B/C races embed as a day inside the current block rather than terminating it. Logged as confirmed-good so it's on record, not just assumed. | Resolved — confirmed working, no action needed | — | `for independent product review/reports/IPR-20260914-006` |
| F077 | Low priority, correctly deferred per roadmap — a sharper diagnostic on an already-known gap, not a new one | Independent product review (IPR-20260914-006): repeated PARTIAL non-adherence (e.g. skipping every quality session for six weeks while still logging easy miles) trips nothing — zero repo-wide hits for "recommit"/"non-adherence" logic. A real single-miss handler and a real total-layoff detector both exist and are correct, but nothing counts or windows partial repeated non-adherence between those two extremes. Not a surprise — this is Roadmap Priority 5 and the codebase's own internal audit already marks `TRAINING_CONSISTENCY` as OPEN. What's new: the specific blocking dependency is three separate, disagreeing implementations of "how consistent is this runner," none promoted to a canonical reader — so there's no signal to build a recommit/recalibrate feature on yet, not just no feature. Framed as "investigate the consolidation first," not "build this now." | **Design ruling closed.** Coach consultant's full doctrine determination (`2026-09-14-019`): detection needs a rolling multi-week window (not per-pass, per Rule 22's own distribution discipline) scoped to quality-session delivery specifically (not blended mileage), reading whether the stimulus landed per Modified Workout doctrine, not calendar-entry presence — still blocked on consolidating the 3 disagreeing `TRAINING_CONSISTENCY` readers into one canonical resolver first, per Brain Constitution's one-question-one-owner rule. Coach voice/action already settled by `PUSH_THE_RUNNER_FORWARD_DOCTRINE.md`: offer RECOMMIT or RECALIBRATE, stated factually, never a goal renegotiation (that's Goal Feasibility's call). **David delegated the two remaining numeric calls (window length, recommit-cycle threshold) back to the coach consultant rather than picking them himself** — ruled 2026-09-15 (`2026-09-15-025-delegated-calibration-rulings.md`): window length and recommit-cycle count both answered with reasoning, recommit-cycle threshold flagged as the weaker of the two (no direct research citation, a reasoned product call, not derived). | Open — doctrine and calibration both settled; blocked only on the `TRAINING_CONSISTENCY` reader consolidation, then ready for implementation | — | `for independent product review/reports/IPR-20260914-006`; consult-log `2026-09-14-019-f077-non-adherence-design.md` + `2026-09-15-025-delegated-calibration-rulings.md` |

| F078 | Medium (real, deterministic, precisely diagnosed, small mechanical fix) | Design review render-confirmed (synthetic week-off row, local scratch DB only, reverted immediately): wire data for a week-off range is `fromISO`/`toISO` both "2026-09-14," but the rendered screen shows "September 13 – 13" — a full day behind on both ends. Root cause: `WeekOffV5.formatRange()` correctly parses the ISO string with `timeZone=UTC`, but builds the displayed string with a fresh `Calendar` and two `DateFormatter`s that never get a timezone set — they silently default to device-local, behind UTC for the whole US, so UTC midnight renders as the prior evening. Confirmed isolated, not a pattern: `RunLogV5.swift`'s date formatters were checked for comparison and correctly set `timeZone=UTC` on both ends. For a real multi-day travel block both ends shift together, so the range still looks internally coherent (e.g. "Sep 30 – Oct 6" renders "Sep 29 – Oct 5") — easy for a runner to not notice, which is why pinning down the exact mechanism mattered rather than leaving it as "looks a bit off." Fix: three missing `timeZone` assignments to match the parsing stage's own already-correct pattern. | Implemented (branch `fix/f078-weekoff-timezone-2026-09-14`), submitted for external review at `RR-20260914-033` — not yet reviewed. Exactly the 3 missing `timeZone` assignments, matching the parsing stage's own pattern; new `WeekOffV5FormatRangeTests` (3 tests) confirm the fix and Rule-18-falsify it. Full test baseline 544/0 → 547/0, zero regressions. | `afb90c7a91d3b7480a992977179606cb78086530` | `for external review/review-requests/RR-20260914-033/` · `for design review/findings/2026-09-14-015` |
| F079 | Low (real, isolated, matches an existing pattern the app already uses everywhere else) | Coach consultant: `SettingsPanel.tsx:216` — `setToast('Could not save')`, bare, two words, no elaboration. Every comparable save/load-failure message elsewhere in this exact codebase reassures the runner nothing was lost — including two lines away in the SAME file ("Could not load your settings. They are stored, not lost."), the nine-file "safe to try again" pattern from the F053 investigation, and a genuine safety-relevant refusal in the today route ("Could not read your injury and illness log. Nothing is prescribed until it can."). Confirmed isolated via grep (appears exactly once). Different root cause from F053 (a missed pattern-application in one spot, not a lexicon-matching gap) — small, separate fix. | Open — small, isolated fix; apply the pattern already used two lines away in the same file | — | consult-log, 2026-09-14 |

| F080 | Medium-High (real, precisely-scoped "armed but not stepped on" duplicate-owner conflict — not yet causing visible harm, but would the moment anyone wires it up) | Independent product review (IPR-20260914-007): `assessGoal()` and the canonical race outlook genuinely compute different goal-feasibility verdicts for the same runner on the same day — confirmed neither file imports the other, no wrapping. `assessGoal()` reads a stored fitness snapshot and defaults to assuming perfect execution; the canonical outlook reads the live capacity resolver and discounts by actual measured execution. An ordinary required-gain case (not contrived) reads "realistic" on one path and "aggressive" on the other — a live Rule-16 violation in waiting. Currently invisible to David: `assessGoal()`'s only render path was deliberately gutted in August down to a quiet badge on 4 unrelated trigger cards, and the canonical verdict — fully serialized and fully decoded client-side — is rendered nowhere, confirmed via exhaustive grep across every native Swift file. **Correction to the codebase's own internal audit** (`ownership.ts`): that file already names this exact conflict and the right fix (assessGoal should consume the outlook verdict, same pattern already used for the finish-time split), but it states both verdicts currently render on the same surface — independently checked and confirmed inaccurate. Changes urgency: fix before ever wiring `assessGoal`'s number to a view, not "already live and confusing someone today." | Implemented (branch `fix/f080-assessgoal-ownership-2026-09-14`), submitted for external review at `RR-20260914-036` — not yet reviewed. `assessGoal()` now consumes the canonical outlook's verdict when supplied (pure mapping, matching `race-projection.ts`'s existing precedent), falling back to the standalone read only when no outlook is usable. Wired at all 3 real call sites. `ownership.ts`'s inaccurate "both render" claim corrected. Rule 18 falsified. | `c340bdb5ce7b24e278d76d1564e38638eaf14933` | `for external review/review-requests/RR-20260914-036/` · `for independent product review/reports/IPR-20260914-007` |

| F081 | Already being fixed — confirmed still live on `origin/main` right now, overlaps with in-flight work | Independent product review (IPR-20260914-008): checked Today/Block/race screens against the UX Simplification Doctrine's explicit "no permanent raw-number real estate" list. Mostly good news, worth recording as such: confidence renders as a range citing the exact doctrine line, an earlier three-projections-at-once incident on the race screen was found and fixed, no fitness sub-scores anywhere, training load renders as a word not a number. One real, still-live violation: Today's Readiness row shows a bare `64` unconditionally, every load — confirmed directly against `origin/main` (`/api/v5/today/route.ts:2593`, `if (glance.readiness?.score != null)` still present) as of this check. **This is the same readiness row already found decorative (F001/IPR-001) and already dispatched for removal tonight** — that fix is built and reviewed, just not yet merged (same merge-authorization batch as F056/F057, David's return). Not a new gap, confirmation the existing fix is still needed and hasn't landed yet. Fair counterargument named in the report worth preserving: readiness might deserve a deliberate numeric exception (the one quantity a runner most wants a fast precise read on) — if so, that should be a stated choice, not something that quietly "fixes itself" as a side effect of the unrelated removal. | **RESOLVED — David's ruling, direct, via David's Desk (2026-09-14 night), verbatim: "Leave it removed."** No exception; wait for the real health section (sleep/HRV/weight) before anything readiness-related returns to Today. | — | `for independent product review/reports/IPR-20260914-008`; ruling via David's Desk |

| F082 | Medium (real, root cause found, fix implemented and verified against real data — pending simulator render + external review) | Design review, RaceDetailV5's Pace Plan section shows two nonsensical zero-width segment ranges — "MILES 2-2" and "MILES 6-6" — alongside normal ranges. Render-confirmed on David's real account, real race (Dodgers 10K, Sep 26). **Root cause confirmed**: `web-v2/app/api/v5/race/[slug]/route.ts` rounded each pace-plan phase's mile label independently (`Math.round(start)-Math.round(end)`) with no continuity check — Dodgers' real course-elevation "Descent" phase (1.77mi→2.486mi) both round to whole-mile 2, collapsing to "Miles 2-2." Generic to any course whose elevation phases don't land on whole-mile boundaries, not race-specific. **Fix implemented**: phases that would collapse on the mile grid are merged into an adjacent real phase (time-weighted pace blend, label from whichever phase covers more ground) before display; the underlying pace math/goal-time drift check still runs against the unmerged phases — display-only fix. Verified by running the real `buildRacePacing` code against real DB course geometry for all 4 scheduled races: only Dodgers had the bug (before: `0-2\|2-2\|2-3\|3-4\|4-6\|6-6`, after: `0-2\|2-3\|3-4\|4-6`); Malibu, CIM, LA Marathon unaffected before and after. `tsc --noEmit` and full `next build` both pass. **Honestly not yet done per Rule 13**: no on-device/simulator render — the agent had no simulator access. Not committed. | Open — implemented and data-verified, needs a simulator render check on the Dodgers race detail screen (Rule 13) and submission through the external review pipeline before merge | — | `for design review/findings/2026-09-14-016`; fix in working tree at `web-v2/app/api/v5/race/[slug]/route.ts` |

| F083 | High (real, confirmed unreachable content behind a broken affordance) | Design review: Settings → Coach → "Decisions" screen genuinely does not scroll — 4 different gesture techniques tried (2 swipe variants + a 5-point `touch_path` drag), zero movement, while the identical swipe technique scrolled Today/Block/Races/RaceDetailV5 fine the same session. Result: the last SETTLED card's body text is cut off mid-sentence behind the tab bar and is permanently unreadable on a screen whose whole point is showing the full record of coach decisions. Same screen also shows "Holding the plan as it is" TWICE with byte-identical text on the same day — once as HOLD/WAITING ON YOU (open), once as HOLD/EXPIRED (settled) — can't tell from the render alone whether that's a state-machine duplicate-write bug or an unlabeled intentional daily-recurrence pattern. | Open — needs the code agent to trace both halves: (1) why this screen's scroll container doesn't respond to any gesture that works elsewhere, (2) whether the duplicate HOLD card is a duplicate-write bug or a real recurrence the UI should label as such | — | Cross-session message, design review, 2026-09-14 (Settings audit, finding 018; the session's own local finding 016 is F082 above, 017 is the capped-confidence precursor to F085 below) |
| F084 | Low, narrowed further after source-checking (both halves smaller than first reported) | Design review, Settings screen, finding 019, refined after reading the actual source (`SettingsV5.swift`, `ComponentsV5.swift`): "Coach voice" row's missing chevron is confirmed **not a bug** — it correctly matches the shared `ListRow` component's own no-chevron-when-non-interactive contract; the only real question left is a pure product call (should Coach voice ever be user-editable), not an engineering gap. The "Your watch keeps recording either way" caption is confirmed structurally attached to the DATA `ListGroup`'s own `footer:` param, not orphaned in the view tree — the "floating" look is the `ListGroup` component's normal header/footer-outside-the-tile convention, which reads fine for a single-row group but is ambiguous when a group (Strava + Email, 2 rows) has a footer that's really about just one row. Real but narrow. | Open, low priority — no code bug on the chevron half (product decision only, not urgent); the caption's grouping ambiguity is a real but minor polish item | — | Cross-session message, design review, 2026-09-14 (Settings audit, finding 019 + refinement) |
| F085 | **Downgraded to Low/likely-environmental** — self-corrected by the reporter within minutes, not an independent rebuttal | Design review, finding 020: on Settings, four controls in a row (stepper +/-, toggle, back chevron) produced zero visible response for up to ~1 minute; a hardware Home-button press responded instantly and a cold terminate+relaunch didn't fix it, which read at the time as ruling out tooling/stale-state causes. **Self-correction, same reviewer, minutes later:** right after filing this, Today showed a "Can't reach faff" banner, and `uptime` on the host showed load averages of 98-122 on a 14-core Mac (7-9x oversubscribed) — the reviewer's own local dev server responded normally when curled directly (401 in 0.33s), and no single runaway process was found; reads as general host contention (multiple programme sessions/simulators/builds competing for CPU) rather than an app defect. Reviewer explicitly can't fully rule out a real underlying issue but no longer considers 020 good evidence for one, and asked that a code agent not spend time chasing this as a confirmed production bug. | **Downgraded — do not treat as confirmed.** Reclassify as "unreproduced, likely environmental" per Rule 13/18 discipline (don't manufacture confidence either direction). Only worth re-opening if independently reproduced on a real device or an unloaded machine. Code agent dispatched on this before the correction landed — redirected to report source-level findings only as a sanity check, not to chase it as confirmed. | — | Cross-session message, design review, 2026-09-14 (finding 020 + self-correction, `for design review/findings/2026-09-14-020-...md`) |
| F086 | Medium-High (real, root cause found, confirmed systematic — fix implemented and test-verified, pending simulator render + external review) | Design review, finding 021: the Sat Sep 20 long-run detail screen's coach explanatory prose says "Easy 12mi..." while the segment table/hero card correctly say 11.5mi (11.5+3+1+2=17.5mi matches the table, not the prose's implied 18mi). **Root cause confirmed**: `web-v2/lib/plan/generate.ts`'s `layoutWeek` long-run authoring bakes the opening-easy mileage into the coach-prose `notes` field literally at layout time; later passes (`smoothLongWoW`, `enforceSpikeRule`) trim the day's final `distanceMi` directly without touching `notes` — the segment table/hero stay correct because they derive fresh from the final `distanceMi`, only the prose goes stale. Same defect shape the codebase already fixed once for `subLabel`'s leading mileage (`LABELTRUTH-2`/`retitleLeadMi`), just never extended to `notes`. **Confirmed systematic, not one-off**: a read-only sweep of every long-run row in the live plan with a stated opening-easy figure found 3 of 3 mismatched, all by exactly 0.5mi (2026-09-20, 2026-10-18, 2026-11-22). **Fix implemented**: new `retitleLongOpeningEasyMi()` in `web-v2/lib/plan/spec-builder.ts` (mirrors `retitleLeadMi`'s pattern), called from a new reconcile pass at the end of `finalizeComposedPlan` in `generate.ts`, same "run absolutely last" position as the existing retitle passes. **Verified**: typecheck clean, full `lib/plan/` suite (205 files, 3371 tests) passes; a new falsifying regression test (`_seglong_authoring.test.ts`, "NOTETRUTH-1") reproduces the three live F086 rows verbatim, confirmed to fail without the fix and pass with it (Rule 18 falsification done). Not yet committed. | Open — implemented, test-verified, and falsified per Rule 18; still needs a simulator render check (Rule 13) once the host isn't oversubscribed, and submission through the external review pipeline before merge | — | Cross-session message, design review, 2026-09-14 (finding 021); fix in working tree at `web-v2/lib/plan/spec-builder.ts` + `generate.ts` |
| F087 | Low (informational, real, non-blocking — a portability risk, not a defect in the reviewed submission) | External review of RR-20260914-030 (F073's Watch side): the two changed Watch files live under `legacy/native/Faff/FaffWatch Watch App/`, not `native-v2/Faff/FaffWatch Watch App/`, which is where `native-v2`'s own `project.yml` says the Watch target's source lives. Traced fully before concluding anything, not assumed: `git ls-files` shows `native-v2`'s copy isn't tracked by git at all; matching inodes confirm it's a local hard link to the `legacy/native` files; `native-v2-prep.sh`'s own header comment documents that the Watch app canonically lives under `legacy/native` and was added into `native-v2`'s target as a one-time manual step. So the submission correctly edited the real source — not a mistake — but it means anyone building `native-v2`'s Watch target on a machine without this exact local hard-link (a fresh clone, a different dev machine, a CI runner) would find the Watch sources silently missing. Also noted as a reusable technique: `xcodebuild ... -destination 'generic/platform=watchOS Simulator'` builds the standalone Watch scheme with zero device needed (no boot, no dialog risk) — usable to verify Watch builds without a watch simulator in an approved pool. | Open, non-blocking, informational — worth its own fix (make the Watch source location either genuinely git-tracked under `native-v2` or document/automate the hard-link step for a fresh checkout) whenever build portability comes up; not urgent | — | `review-results/RR-20260914-030/review.md` |
| F088 | Low (informational, real, test-harness-only — not a product defect) | Surfaced while fixing F066's thresholdbail bug: `legacy/native/Faff/FaffWatch Watch App/WorkoutTracker.swift`'s `startSimulatorMock()` accrues simulated distance at a **fixed rate** (`0.0045 * warp`) uncorrelated to a rep's own target pace, contradicting the function's own header comment claiming distance-anchored reps "complete in proportional real time alongside time-based phases." This fixed rate is what made thresholdbail's real bug (see F066 fix) look like "never fires" rather than "freezes on plan completion" — under warp, a `mi:`-anchored rep set finishes in ≈39 real seconds regardless of its stated pace, well under the 120s HR-sustain window, which is what triggered the `planComplete` freeze in the first place. Test-harness code only, not shipped-app behavior — the real product bug is F066, already fixed. | Open, non-blocking, informational — worth tightening `_SessionSim`/`WorkoutTracker`'s mock accrual to actually scale with a rep's pace so future distance-rep test archetypes don't produce misleading timing under warp; not urgent, no product impact | — | Surfaced during F066 fix, `legacy/native/Faff/FaffWatch Watch App/WorkoutEngine.swift` investigation |
| F089 | Low-Medium (real, cross-surface Rule-16-shaped inconsistency, not itself a safety risk — over-conservative direction, not under-) | Independent product review (IPR-20260914-001), surfaced in passing during the safety/readiness arbitration trace (see F067): the phone's injury branch (`app/api/v5/today/route.ts`) blanks the WHOLE day's prescription for ANY injury driver at all, while the Watch (`lib/watch/build-workout.ts`, SAFETYSTOP-1) independently and correctly distinguishes MODIFY (ships a real easy session) from STOP. Two hand-written copies of the same safety gate (the architecture debt F067 already tracks) disagree on what a MODIFY-tier injury day should actually produce — a runner on the phone gets nothing prescribed for an injury that would still let the Watch offer an easy day. Not a live safety hole (phone's error is toward MORE conservative, not less), but a real "one quantity, one name" violation that should be reconciled the same time the duplicated-gate architecture is eventually centralized. | Open — not yet dispatched, low urgency, same remediation track as F067's centralization recommendation | — | `for independent product review/reports/IPR-20260914-001` |
| F090 | Low-Medium (real, small, a repeat of an already-dated finding — still unfixed three weeks later) | Independent product review (IPR-20260914-005): `OnboardingV5.swift`'s distance/goal picker (lines 347-350) still omits the `coached` option, despite full backend support already existing (`web-v2/lib/onboarding/complete-inputs.ts:44`, `web-v2/app/api/profile/goal/route.ts:337-342`). This is not new — it repeats a specific, already-dated finding from `docs/design-backend-alignment-2026-08-19.md:875-878` almost verbatim, meaning it was identified once roughly three weeks ago (estimated ~2 days to fix) and is still present today. Distinct from F074 (the same report's fitness-answers-discarded finding) — this is the goal-distance picker's option list, a different UI element in the same onboarding flow. | Open — not yet dispatched; small, well-scoped, previously estimated | — | `for independent product review/reports/IPR-20260914-005`; prior citation `docs/design-backend-alignment-2026-08-19.md:875-878` |
| F091 | Low (informational — a coverage-tracking accuracy issue, not a product defect; same shape as F070/F074) | Independent product review (IPR-20260914-009): the 27-area coverage matrix marks Notifications "NOT STARTED" — wrong, the fourth confirmed-wrong "NOT STARTED" row this session (after Shoes/F070 and Onboarding/F074). A genuine, non-stub APNs stack exists and is wired end to end: ES256 JWT signing and HTTP/2 delivery (`web-v2/lib/notifications/apns.ts`), a real dispatch pipeline with prefs-check/24h-dedup/quiet-hours gating (`dispatch.ts`), a GitHub-Actions-ticked cron scheduler draining a pending queue (`web-v2/app/api/cron/notifications/route.ts`), and real device-token registration (`web-v2/app/api/notifications/register/route.ts` + `NotificationsAppDelegate.swift`). Confirmed delivered, not just wired: commit `a39fa8325` (2026-08-21) shows APNs was unconfigured 2026-05-31 through 2026-08-17 (174 log rows, 0 delivered, 0 acked), fixed, and has since delivered at least 1 real push (22 calls, 21 rejections, 1 delivery on the one attempt checked). Current (September) live delivery volume not independently re-confirmed by this pass. Corroborates F070's original recommendation: an unevidenced matrix row shouldn't default to "not started." | Open, low priority, informational — matrix row corrected in `MASTER-COVERAGE-MATRIX.md` as part of this pass | — | `for independent product review/reports/IPR-20260914-009` |
| F092 | Medium (real doctrine-conformance gap, needs a product decision — not urgent, not a safety or data-integrity issue) | Independent product review (IPR-20260914-009): traced every notification trigger against `docs/PRODUCT_ROADMAP.md` Priority 4's explicit rule ("notifications are reserved for consequential changes or requested actions"). 6 of ~9 categories fit cleanly (`session_moved`, `projection_change`, `strava_reconnect`, `skip_recovery`, `niggle_sick`; `race_day`/`race_eve` borderline but time-driven-consequential). Three do not: **`weekly_checkin`** fires every long-run-day evening unconditionally if any running happened, gated on nothing changing; **`race_countdown`** fires on a fixed calendar schedule (12/10/8/6/4/2 weeks out) — a milestone, not a change; **`run_unread`** is a "you haven't rated this run" data-completeness nag, neither a consequential change nor a requested action, and — unlike the other two — has no user-facing toggle at all. Settings exposure is only partial: `SettingsV5.swift:433-476` gives the runner exactly 2 toggles ("Skipped-run check," "Weekly summary") against ~8 backend categories; the other 6, including `race_countdown` and `run_unread`, default ON with zero phone-side control. Real counterargument worth weighing: `weekly_checkin`/`race_countdown` may be a deliberate, already-shipped product choice (the design contract's own "Weekly summary" toggle suggests intent) that simply postdates Priority 4's wording and was never reconciled against it, rather than an unnoticed violation — `run_unread` is the hardest of the three to defend under that reading. | **RESOLVED — David's ruling, direct, via David's Desk (2026-09-14 night), verbatim: "Leave them as deliberate exceptions."** All three stay on with no Settings toggle, treated as an intentional carve-out from the consequential-changes-only notification rule. | — | `for independent product review/reports/IPR-20260914-009`; ruling via David's Desk |
| F093 | High (real, confirmed regression against the legacy screen's own correct behavior, not a gap that was never built) | Design review, finding 023, source-confirmed (`SettingsV5.swift` → `onSetLongRunDay` → `patch(["long_run_day": ...])` → `PATCH /api/settings`): the server explicitly treats `long_run_day` as `PLAN_SHAPING` and calls `rebuildActivePlanForPrefs`, which regenerates the active plan around the new day and returns a `replanned` boolean — but the V5 client's `patch()` wrapper discards that response entirely (`_ = try await write(); return true`), so no code path in `SettingsV5`/`HostsV5` ever reads `replanned`. Confirmed as a regression, not a never-built gap: the legacy pre-V5 `SettingsView.swift` handles the identical field correctly and shows a "Plan updated" toast on the same flag. Net effect: a runner moves their long run from Sunday to Saturday, the app silently reshuffles the week's quality/easy placement around it, and nothing on screen tells them that happened. Scope confirmed narrow, not broader: a full inventory of every `onSet*`/`onToggle*` closure in `SettingsV5.swift`+`HostsV5.swift` found only 4 phone-editable fields total (`long_run_day`, `units_distance`, `phone_run_enabled`, `weekly_frequency`), and only `long_run_day` is plan-shaping — `rest_day`/`quality_days`/`available_days` have no phone control yet at all, so they don't share this bug today (worth the same fix whenever they get UI). Distance units confirmed clean by the same reviewer — display-only, no rebuild triggered. Pending render confirmation — host was too loaded (190-420+ load average) for reliable simulator timing at time of finding. | Open — needs the code agent to surface `replanned` back through `patch()` and reuse the legacy screen's existing toast pattern rather than inventing new UI | — | Cross-session message, design review, 2026-09-14 (finding 023 + narrowing) |
| F094 | Medium (real, silently-discarded error information, same shape as F093) | Design review, finding 024, source-confirmed: `StravaOAuthSession.start()` (`HostsV5.swift`) is well-built and models three distinct outcomes — `.connected(scope)`, `.failed(reason:)` (specific messages: "couldn't reach the server", "server didn't return an OAuth URL · check Strava env vars", or Strava's own returned error), and `.canceled` — but the call site (`connectStrava()`, `HostsV5.swift:3472-3478`) discards the entire outcome (`_ = await StravaOAuthSession.shared.start()`) and just calls `requestLoad()`. A genuine failure (expired Strava session mid-flow, misconfigured OAuth creds, network blip) looks identical on screen to nothing having happened — the row just still says "Not connected," no reason given. Pending render confirmation — same host-load caveat as F093. | Open — needs the code agent to switch on the Outcome and surface `.failed(reason:)` via the pattern `TravelV5.swift`'s `ErrorNote`/`saveFailed` already uses elsewhere in Settings; `.canceled` needs no error, the runner already knows they cancelled | — | Cross-session message, design review, 2026-09-14 (finding 024) |
| F095 | **Downgraded — flaky under host load, not a confirmed regression.** Same push (F086), retried once with no code change, went from FAIL to OK. | Pre-push hook's Watch face QC gate (`docs/design/watch-0821/FACE-QC.md`) failed once on `origin/main`'s existing state — `WATCH FAIL · raceugly board outside Apple's content box, 196.5pt vs 193pt boundary (46mm)` — then passed cleanly on an immediate retry with zero code changes in between ("22 boards inside Apple's content box"). The gate's own output on the failing run said `re-shooting board(s) that came back blank: alwayson p1hrceiling p2min`, i.e. it was already compensating for flaky screenshot capture before it failed — consistent with the extreme host contention tonight (load average 190-420+, per design review's concurrent `uptime` checks) producing a bad screenshot/render read, not a real 3.5pt layout regression. Confirmed unrelated to F086/F082 either way (both web-v2-only, zero watch files touched). | Open, low priority, informational — re-run the Watch gate once the host is calm to confirm it's reliably clean; if it fails again under normal load, that would make this a real F082/F086-unrelated watch-face bug worth its own finding | — | Surfaced during two consecutive F086 push attempts, `/tmp/faff-gate-faces` screenshots |
| F096 | **High-Medium — a genuinely missing arbitration component, not a duplicate-answer routing fix like most OPEN entries** | Coach consultant, self-directed sweep of `lib/runner-state/ownership.ts`'s OPEN entries: `docs/BRAIN_CONSTITUTION.md` §16's Final Decision Validator has no implementation anywhere. Verified independently (`grep -rn "safety-verdict\|classifySafety" web-v2/lib/training/*.ts web-v2/lib/plan/*.ts` → zero hits): neither Readiness (`runner-state.ts`) nor plan generation ever reads Safety's verdict; `classifySafety`'s actual consumers are display/shadow-eval paths only (`glance-state.ts`, `phase-priority.ts`, `canonical-shadow/live-input.ts`), never the prescription path. Concretely: a runner could be in a state Safety would classify STOP the same morning Readiness reads NORMAL and the resolver hands out an ordinary hard session, and nothing would notice — the exact "Safety contradiction... forbidden" case §15 already names, with §18's override hierarchy already ranking SAFETY 1st and ACUTE READINESS CONSTRAINT 2nd. Explicitly NOT claiming a live incident occurred — that's unverified live-data work, distinct from the mechanism's absence being real regardless. | Open — needs a real arbitration component built (per §16), not a routing fix between two existing readers; scope is a genuine implementation project, not a quick patch | — | `for coaching consult/consult-log/2026-09-14-022-safety-readiness-no-arbitration-layer.md` |
| F097 | Medium (real, well-scoped, a physiological-calibration judgment call for David, not an engineering gap) | Coach consultant, same `ownership.ts` sweep: `MAX_DEMONSTRATED_DOSE` ("biggest session of this type he's actually completed") has no implementation — `athleteEvidenceFor` is built to grade a prescribed dose against this maximum but it's null for every archetype, since nothing aggregates it from `lib/execution/reconstruct.ts`. A real Rule 21 finding: the plan can't get harder because the "he's earned it" signal doesn't exist. A 2026-09-06 pass correctly declined to build it and left two questions open — coach consultant answered both: Rule 8's taper exclusion barely bites a MAX read (a taper day can't win a "biggest session" contest by construction); the window should be ROLLING not lifetime (Rule 8's corollary — "what's recently absorbed"). **Self-corrected 2026-09-15**: the original window recommendation (borrowed-by-analogy from `goal-ready.ts`'s 8-week VDOT-freshness horizon) is superseded — real research turned up a more directly-on-point citation in the same doc, a **30-day window**, which is also the SAME window an existing, already-shipped reader (`recentPeakLongMi`'s spike-anchor half) already uses for a structurally identical question. **Revised recommendation: don't build a new constant at all — reuse that reader's existing window logic.** Consultant proactively flagged the correction rather than leaving the superseded 8-week number standing. | Open — well-scoped, ready for the code agent; use the 30-day/`recentPeakLongMi`-window approach, not the original 8-week citation | — | `for coaching consult/consult-log/2026-09-14-023-max-demonstrated-dose-design.md` + `2026-09-15-025-delegated-calibration-rulings.md` (correction) |
| F098 | High (real, substantial — the most important finding of this independent-review pass per the reviewer's own framing) | Independent product review (IPR-20260914-010), race-prediction soundness — four structural problems independent of the ownership question in F080: (1) a personal durability fit needs only 2 races to count as valid evidence and can still supply ~75% of the blended prediction weight — a real 2026-09-02 incident cited in the codebase's own comment rested a whole 7:55-vs-7:40 marathon pace difference on one race; (2) the confidence-interval function takes no durability-evidence-quality parameter at all, and separately `race-outlook.ts`'s call site hardcodes drift status to `'on-track'`, disabling one of its own four uncertainty-widening levers regardless of the runner's real state; (3) the prediction is course- and weather-blind by construction — elevation/heat models exist and are doctrine-cited elsewhere but wired only into past-evidence weighting, never the forward number or its band; (4) no empirical calibration loop exists anywhere — `retrospective.ts` already collects predicted-vs-actual per race, nothing aggregates or acts on it. Corroborating: the doctrine registry already carries a live, deliberately-unfixed exemption for the same overconfidence shape in a sibling model (5K/10K bands running 2-4x tighter than the app's own cited research intervals) — the same organization hitting the same failure class twice. Two of the four fixes are small/mechanical (stop hardcoding drift status; add a durability-quality CI term); the other two (course/weather wiring, a real calibration loop) are real scope decisions. | Open — 2 fixes are well-scoped and ready for the code agent; 2 need a scope decision before implementation | — | `for independent product review/reports/IPR-20260914-010` |
| F099 | Medium-High (real, confirmed pattern across 3 unrelated screens in one evening — a habit, not 3 coincidences) | Design review, finding 025: `ShoesHostV5.swift` has the identical `_ = try? await API.xxx(...)` discard shape in 3 of its own call sites (load / patch-wear-retire / addPair) — same shape as F093 (Settings) and F094 (Strava), now confirmed in a third, unrelated screen. Worse than the other two: "Add a pair" (`AddShoeV5`) dismisses its own sheet immediately when `onAdd` fires via an un-awaited fire-and-forget `Task` — the form closes as if the shoe was added BEFORE the network call returns; on failure the runner watches success be performed, not just silence. Positive finding worth keeping on record: the screen is otherwise doctrine-clean (correctly avoids duplicating the mileage number with an explicit code comment about it; correctly sources the retirement threshold from the server/`Research/17-footwear.md` rather than a client guess). Side note, not yet investigated: two independent Shoes screens exist in the codebase (this V5 one, and a legacy `Views/ShoesView.swift` reached via `ProfileView.swift`) — not confirmed which one a real runner actually hits. | Open — reviewer recommends a repo-wide grep for `_ = try? await API\.\w+\(` rather than fixing these three call sites as isolated bugs, given the confirmed 3-instance pattern tonight | — | Cross-session message, design review, 2026-09-14 (finding 025) |
| F100 | **High — Rule 20 violation: a proven fix exists and doesn't hold, real risk on two consequential screens** | Design review, finding 027, the synthesis of F093/F094/F099: a repo-wide grep for the discarded-API-result shape found this exact bug was already diagnosed and fixed once, by name — `TODAYWRITE-1` (2026-09-08), documented in `HostsV5.swift`~2167 and `SurfaceStoreV5.swift`~376. Real prior incident: "Left calf flagged · The coach has it, it shapes tomorrow" rendered over a database with ZERO rows written, because `_ = try? await API.authedSend(req)` threw the result away. Fixed with a real working pattern — `V5WriteSettlement`/`V5RowWriteState`, a 4-state result the row is obliged to spend instead of drawing its own optimistic copy — correctly applied to niggle-flag, sick-report, Strava push, `AddRaceV5`, `RPECaptureRow`. **The fix never got applied everywhere and keeps recurring**: 19 more `_ = try?/await API...` sites found. Reprioritized after reading the two most consequential in full: **`PacesMovedV5.confirm()`** discards `confirmRaceAuthority`/`acknowledgePaceDrop` then unconditionally marks the screen settled — the file's own comment names the real consequence: a failed write means `GET /api/v5/paces` keeps treating the question as unanswered forever, so the identical confirm card silently reappears on a future visit with no memory the runner ever answered it. **`ReturnToRunningV5.checkIn()`** discards `returnCheckIn(outcome:)` then unconditionally shows "Logged." — the return-from-injury check-in, the data a walk-run ladder's stage progression depends on; a failed write means the runner believes a return-to-running stage was recorded when it wasn't, in the one domain this project's doctrine treats most carefully. Lower priority but same shape: `TodayBeforeLiveV5.swift:505/514` (postSkip/deleteSkip). Confirmed the ORIGINAL 2026-09-08 niggle/sick-recovery bug is not reachable in the shipped app (legacy `TodayView.swift` only mounts via an internal `-faffLegacy` launch arg, dead in practice) — this is new code repeating an old mistake, not the old mistake persisting. | **Open, high priority.** Fix order per design review: PacesMovedV5 and ReturnToRunningV5 first (real user-facing consequence), then TodayBeforeLiveV5's skip/deleteSkip, then the remaining sites. Reviewer recommends a CI scanner for this exact grep pattern, sibling to `check-swallowed-failure.sh`, same enforcement style as Rules 6/7/8/14 — a Rule 20 gate so this proven fix can't silently stop holding a third time. **Addendum (onboarding audit):** `OnboardingHostV5` has 2 more sites — `patchSettings(long_run_day, phone_run_enabled)` and `updateProfile(weekly_frequency)`, both `_ = try? await`, no `V5WriteSettlement`. `phone_run_enabled` is the standout: it has NO other write path anywhere in onboarding (confirmed via the full `completeOnboarding` payload — never included), so a transient failure right after signup silently loses that setting forever, no error, no retry. **Second addendum (Today full read):** `TodayBeforeLiveV5.swift:499-516` has 3 more instances — `assignShoe`/`skip`/`unskip`, same `_ = try? await` shape. Worse than usual: the caller (`select()`) returns `true` UNCONDITIONALLY regardless of the write's outcome, so the row always collapses as if it succeeded — a failed skip or shoe-change silently reverts with zero indication, on the screen every runner opens daily. Notable: this exact file's own reschedule-apply/undo flow correctly distinguishes applied/refused/failed a few lines below — the right pattern was known and used in this file, just not brought to these three calls. **Design review has now confirmed this pattern's shape across 7 of the app's major screens** (Settings, Shoes, RunLobby, Onboarding, Block, Races, Today) and considers 027 fully scoped — this is the consolidated map, not a partial sweep. **Fully closed out**: the 4 remaining `API.swift`/`authedSend` sites (logout, APNs registration, push-unregister, notification-ack) are legitimately exempt — each explicitly documented as deliberate best-effort housekeeping where silent failure is correct; `ProfileView.swift`'s 2 discarded toggles (`strava_auto_push`, `phone_hr_alerts`) are in the same dead-in-practice `-faffLegacy`-only path as legacy `TodayView`. Live-app scope is exactly the V5-surface sites already enumerated across this finding's addenda — nothing left unverified. | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 027 + onboarding + Today addenda, fully closed) |
| F101 | Medium (real, 3 distinct issues on one screen — Rule 16 and Rule 17 shaped) | Design review, finding 026, `RunLobbyV5`: (1) watch-sync error text is genericized in the UI instead of showing the real error already available; (2) a latent gap in the pre-start re-verification path — it doesn't compare `cue`/`strategyLabel` against what was originally shown, so a stale cached prescription could pass re-verification silently; (3) a Rule 17 repeat — the fix for a prior Rule 16 problem made the headline and the structure-row print the identical sentence twice on the same screen. | Open — needs the code agent, not yet dispatched | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 026) |
| F102 | **Informational, needs verification — possible real progress, or a possible weakened gate (Rule 18 discipline required)** | External review lead, live-ran `web-v2/lib/plan/_coach_sensible.test.ts` directly (the Baseline Plans gradecard gate) as a bounded evidence-gathering check: **currently 6/6 PASSING**, live, right now. The file's own header comment says "THIS GATE IS RED ON PURPOSE... it is red today, against the plan the engine authors for the owner tonight" — contradicted by current reality. Most recent commit touching the file: `81bf30eb8 fix(rule9): the last cliffs -- an interruption is measured in weeks OFF, and easy running stops quantising the week`, plausibly the real fix, not independently verified further by the reviewer (that would be a fresh investigation, out of scope for a bounded pass/fail check). Also, separately: a 5-way merge simulation across all 5 CONFIRMED branches (F035/F056/readiness-removal/F057/F072-073) completed with zero conflicts, one honest gap disclosed (no full `tsc --noEmit` on the merged tree — scratch worktree had no `node_modules`, too heavy to duplicate; each branch was independently `tsc`-verified on its own, file-level non-overlap already confirmed). **Traced fully against the live diff and live source, not the commit message. Verdict: neither "fixed" nor "weakened" — more precise than either.** `81bf30eb8` did two legitimate things: (1) fixed real bugs IN THE GATE ITSELF (wrong pricing anchor overstating the floor by 12%; a taper/recovery-contaminated fixture; a hardcoded `easy_day_median_mi`) — genuine test-quality fixes that make the gate stricter, not weaker; (2) fixed a real Rule-9-shaped rounding defect in the ENGINE (`RULE12-RESIDUAL-1`): `perEasy` used to round to one whole mile (moving the week's volume in `easyCount`-mile steps), now floors to the half-mile — real, falsified, documented progress. **What it did NOT do: change Rule 12's actual architecture.** Live source confirmed directly: `remainingMi = weeklyMi - allocated` still exists (`generate.ts:7819`), `perEasy` is still computed as a function of the remainder (`:7970`/`:8006`) — the engine still sizes easy running FROM THE REMAINDER after quality is allocated, exactly what Rule 12 says never to do. The floor still yields to the budget in the general case (`flooredPerEasy = Math.min(effectiveFloor, perEasyBudgetCap)`, `:8009`) — the test file's own unchanged comment already names this: "a floor that yields to the budget is not a floor; it is a preference." **Live proof, not inference**: re-ran the test with `--disable-console-intercept` — it's currently logging, for the owner's own real fixture, `DEMONSTRATED_EASY · 2 in weeks that could not afford it` and `UNAFFORDABLE QUALITY wk=45 easy=5.5mi < demonstrated median 6mi`. Two real quality-phase weeks, right now, where the engine gives David a shorter easy day than his own demonstrated habit because the remainder-based budget can't afford it — the exact Rule 12 shape. The gate doesn't fail on this by deliberate design (it only fails below doctrine's absolute minimum, which these weeks still clear) — this was never a case of the gate going green on a caught violation; it was never designed to catch this specific violation shape at all. | **Open, real, needs a decision — not closeable as "fixed."** Two options named by the reviewer: (a) reopen/rename this as a narrower, honest finding — "Rule 12's remainder-architecture is unfixed; `RULE12-RESIDUAL-1` only smoothed its rounding," or (b) if the current floor-yields-to-budget behavior is meant to be an accepted, argued exemption rather than a defect, that needs its own explicit decision and citation per Rule 7's own discipline, not a silently-stale header comment. Either way, `CLAUDE.md`'s Rule 12 header and the test's own "red on purpose" framing are now BOTH out of sync with reality, in opposite directions — routed to David's Desk given this touches locked doctrine text directly. | — | External review lead, live test run + full trace, 2026-09-15 |
| F103 | **High — every new runner's day-one plan, zero disclosure, real trust risk at the exact moment trust is being formed** | Design review, finding 028, source-confirmed: onboarding's "Typical weekly mileage" stepper lets a runner pick any value 10-70 (defaults to 24), but `OnboardingHostV5` silently snaps it down to the nearest of a fixed rung set `[0,5,15,25,35,45,55,65,75,85,95]` before it ever reaches the engine — exact snap function quoted in the finding. Concrete failure case, not hypothetical: a runner leaves the default at 24 and their entire plan gets built off 15 — a 37% understatement — with zero disclosure anywhere: not on the reveal step, not in the coach line, nowhere. Same shape recurs at every rung boundary, always downward, up to ~10mi off at the worst case. This is every new runner's first plan, at the exact moment their trust in the app is being formed. Needs a product decision, not just a code fix: either make the stepper only offer the real rungs (no illusion of granularity), or show the resolved/snapped value back before the runner confirms. Positive findings from the same pass, worth keeping on record: no Rule 17 duplicate sentences anywhere in onboarding, coach-voice/tone fully clean (no exclamation marks, no emoji, em dashes only in dev comments), and the reveal panel correctly reads live Today data rather than composing its own preview (avoiding a Rule 16 trap). | **David widened this rather than picking either option — direct, via David's Desk (2026-09-15), verbatim: "This whole thing seems broken. Why would the default be 24 but they get 15? We should never ask a runner to pick a number and not give it to them. Other agents need to weigh in here because this isn't a simple answer it seems like a broken system."** Now standing doctrine, `CLAUDE.md` Rule 25. Two things need real investigation before any fix: (1) why the stepper's own DEFAULT (24) resolves off-rung in the first place — may be a deeper bug than the rounding-on-confirm behavior originally described; (2) whether the same "ask for one number, silently substitute another" shape appears anywhere else in onboarding or the app — not assumed, not yet checked. **Investigation complete, both questions traced with direct evidence.** (1) Why the default (24) is off-rung: `OnboardingV5.swift`'s own comment (lines 653-654) says exactly where it came from — "Lifted from the prototype's own `_script-data.js`... the `onboard.weeklyMi` default of 24" — a design-prototype placeholder migrated verbatim into production, never checked against `VALID_WEEKLY_MI` at all. Not an edge case slipping through a correct rule — the default was never checked against the rule in the first place. (2) Whether this pattern is systemic: **yes, confirmed — this is the SECOND time this exact class of bug has hit this same screen.** `submit()`'s own header comment documents a 2026-08-21 onboarding audit that found and fixed 4 instances of "client control range/precision silently disagreeing with what the server validates": name never sent (fixed), `weeklyFreq` off a 2-7 stepper vs. `VALID_FREQ`'s 6-ceiling (fixed — stepper is now 2-6, exact match, confirmed by direct comparison — this is the CORRECT template), `longRunDay` landing too late to affect the first plan build (fixed), and `weeklyMi` itself — the August audit's own words: "weeklyMi was sent unconditionally from a 10...70 stepper... so 24 was dropped, AND it was sent for runners who never answered a volume question at all." That was only HALF-fixed: the audit gated it correctly and added `snapWeeklyMi` to guarantee a valid rung — but "round down, say nothing" is itself a fresh instance of the identical defect class, just relabeled from "silently dropped" to "silently substituted" without the August audit recognizing it as the same bug shape. Mechanically, the specific snap-to-rung table is isolated (grepped all of `ViewsV5`, it's the only one) — but the general class (a client control's range/precision disagreeing with what the route actually validates) has now produced 5 known instances on one screen across two investigations three weeks apart. | **Fix authorized by Rule 25 — real rungs only, matching `daysPerWeek`'s already-correct pattern (its 2-6 stepper range exactly matches `VALID_FREQ`, making its downstream clamp pure defensive redundancy that can never trigger).** Dispatched to code agent. Given this is a confirmed-repeat bug class (5 instances, 2 investigations), also worth a standing check — verify every client input control's range/precision against its actual route validator, not just patch this one field. | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 028, full investigation); David's widened ruling via David's Desk, 2026-09-15 |
| F104 | Low-Medium (2 clean, small, real gaps on an otherwise well-built screen) | Independent product review (IPR-20260914-011), Settings pass — positive context first: the screen is appropriately minimal, everything is genuinely wired to real state, and it already has a better-than-average failure pattern (a down endpoint shows "Unavailable" and goes non-editable rather than fabricating a default) — worth protecting, not overbuilt. Two real gaps: (1) sign-out fires with no confirmation despite the code's own comment admitting it can cause irreversible data loss (an unsent run's pending-completion cache gets purged, "cannot be undone from inside the app"); (2) Strava can be connected but never disconnected. | Open, low priority, well-scoped — add a confirmation step to sign-out per the code's own admitted risk, add a disconnect action for Strava | — | `for independent product review/reports/IPR-20260914-011` |
| F105 | Medium-High (real functional bug, distinct in kind from tonight's discarded-write pattern — a retry-parameter bug, confirmed against the file's own documented fix history) | Design review, finding 029, full 1518-line read of `BlockV5.swift`: the "Change the plan" sheet's failure-retry (`failedBody`, lines 1005-1016) resends the WRONG params on retry for 4 of the scenarios it supports. Travel retries with `{"from","to"}` instead of the correct `{"fromISO","toISO"}` keys — confirmed against the file's OWN comment at lines 737-738, which documents this exact from/to bug as already fixed at the primary call site, just never applied to this second one. `move_day`/`extra_day`/`another_race` retry with an EMPTY params dict, silently discarding whatever date/day/race-slug the runner had already picked — again matching the file's own doc comment (1115-1119) describing this exact failure shape as already fixed elsewhere. Net effect: a runner who hits a transient failure while changing their plan and taps the obvious next action (Retry) gets a broken retry in 4 of 5 scenarios. Rest of the file confirmed clean in the same full read: zero discarded-write instances, no Rule 17 duplication, phase content genuinely engine-resolved (not template-with-numbers-swapped), no Rule 9 cliffs, no Rule-8-corollary trend computation in this view at all. | Open, medium-high priority — fix `failedBody` to resend the correct params per scenario, same fix shape already proven twice elsewhere in this file | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 029) |
| F106 | Low (real, honest, structurally can never fire under current single-adult-runner reality) | Independent product review (IPR-20260914-012), generalization audit: age is computed from the runner's birthday at onboarding, then read by nothing downstream — including the one place doctrine explicitly wants it, a youth-specific ramp cap. That cap is correctly transcribed from doctrine but structurally can never fire since nothing feeds it the computed age. Positive context from the same pass, worth keeping on record: golden-runner fixtures aren't aspirational (confirmed passing, not just documented); a genuinely good menstrual-cycle/HRV readiness adjustment exists, generic and correctly gated; no hardcoded-to-David production constants anywhere; multi-tenancy is sound. Two honest gaps already self-documented by the codebase, not newly discovered: no age-graded pacing anywhere (CLAUDE.md already flags this), and the capacity-resolution layer's own audit admits it's validated against essentially one live account ("understates the marathon axis by construction," in its own words). | Open, low priority — fair counterargument named by the reviewer: a true under-14 runner is plausibly out of scope for this app regardless, so this may not be worth wiring until a real case exists | — | `for independent product review/reports/IPR-20260914-012` |
| F107 | Medium (real, structural, pre-launch — cheap to fix before the real API ships, not a live bug) | Design review, finding 030, full read of `RacesV5.swift`: `/api/v5/races` doesn't exist in production yet (per the file's own header comment), so this renders against sample fixtures today — both findings here are structural risks caught before launch, not live bugs. (1) The hero panel's "Projected" stat and the decision card's `stretchTarget` are the same underlying quantity (projected finish) with no shared source — the file's own preview fixtures prove the drift: all 7 sample scenarios hardcode "Projected" to 3:16:45 regardless of what the card's own question text claims (the "Behind" scenario's card describes ~3:34, panel still says 3:16:45). This is Rule 16's own named failure shape (the CIM three-projections bug) recurring in a screen that hasn't shipped yet. (2) `V5RaceRow.authority` (representative/compromised/unrepresentative — "did this race count," per `PacesMovedV5`'s own confirm flow) is fully decoded and never displayed anywhere in this file (confirmed via grep) — a compromised past race renders identically to a clean one in Completed, same dimming, no signal. Positive findings from the same pass: the Worth Knowing card's full lifecycle traced clean (already uses the correct write-settlement pattern, refusal reasons survive card dismissal correctly); past-race provenance handled correctly in sample data; no Rule 17/tone violations. | Open, medium priority — cheap to fix now in the `/api/v5/races` contract before it ships: (1) both fields must resolve from one shared computation, same discipline as `race-projection.ts`'s existing fix; (2) surface `authority` visually for compromised/unrepresentative races in Completed | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 030) |
| F108 | Low (real, small, cheap to align — genuinely the best overall result of tonight's independent review, most of this row is the positive context) | Independent product review, coach-voice pass — ran the actual voice-gate (`scripts/check-coach-voice.sh`) rather than assuming it exists: 376 files, clean. Independently re-verified by grep: no exclamation marks or em dashes in runner-facing copy, no emoji anywhere. Terminology discipline is real and load-bearing, not cosmetic — recovery-run vs. easy-run is a doctrine-cited distinction that actually suppresses pace display in the UI for recovery specifically. `WorkoutResultV5.swift` documents explicitly rejecting a four-card layout because it read as "four ideas from four authors," in favor of "one verdict, one voice, one paragraph." One small, real, named seam (`IPR-20260914-013`): the same failure class (a lookup or round-trip failing) is narrated first-person "we" in a deliberately shared, reused component, and third-person "the coach" in at least one other screen. | Open, low priority, well-scoped — align the person/voice across the two narration sites | — | `for independent product review/reports/IPR-20260914-013` |
| F109 | **High — direct hit against CLAUDE.md's own locked Race-data source-of-truth checklist, not a general design nit** | Design review, finding 031, full 1917-line read of `RunDetailV5.swift`: `raceFinishHero` (lines 1226-1240) renders `detail.time_moving`/`time_elapsed` — the raw watch/GPS time — as an unqualified 52pt "FINISH TIME" hero whenever a run correlates to a race (`race_matched: true`). But `race_matched` is populated server-side purely for NAMING (the query only selects slug/meta from `races`, confirmed via `run-state.ts`) — it never reads `races.actual_result`. The RunDetail wire model has no curated-result field at all, so this screen cannot even check whether the watch's time differs from the official chip time, and shows the watch's number as if it were authoritative. Concrete failure case: a runner's official half-marathon result (`races.actual_result`, correctly shown on RaceDetailV5) is 1:32:04; their watch recorded 1:31:41 via normal GPS drift. RunDetailV5 for that same run shows an unlabeled "FINISH TIME 1:31:41" — same race, two screens, two numbers, neither marked provisional. **This is CLAUDE.md's own historical bug #3** (training-run data displaying as authoritative race performance) recurring in a new screen — this file's OWN header claims "every number here renders `.measured`" and gets that right everywhere except this one hero; RaceDetailV5 (the file's own cited precedent) handles the identical distinction correctly, this screen just never routed this one number through it. Rest of the file confirmed strong in the same full read: no discarded-write pattern, a deliberately correct 3-state load/absent/outage split with a comment naming the exact Rule 11 failure it prevents, three prior Rule 16/17 regressions already fixed and documented in place, clean tone throughout. Pairs directly with F082/finding 016 (RaceDetailV5's own mile-range bug) — both are "race result vs. training data" trust questions on the same screen family; reviewer recommends verifying both against one real completed race in David's account together. | Open, high priority — route `raceFinishHero` through `races.actual_result` first (same rung-1/rung-2 discipline as `personal-records.ts`), never the raw watch time unlabeled, when a curated result exists | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 031) |
| F110 | **High — SAFETY, same class as F066, identical bug shape one level up in the same file** | Design review, finding 032, full read of `WorkoutEngine.swift` (the same file F066's fix lives in). `noteMileBand(inBand:)` is the ONLY call site that updates `milesAdrift` (confirmed via grep), and `milesAdrift >= 2` is the sole trigger for a `metric: "pace"` bail. But `noteMileBand` only fires when `allowSplitFlash` is true — and `allowSplitFlash` is explicitly false during exactly the multi-rep structured work (interval/threshold/tempo), by design, to avoid interrupting a quality rep with a split-flash board. So a mile crossed MID-REP during a 2-3mi threshold/tempo work phase never reaches `noteMileBand` — mile bookkeeping still advances correctly (`lastMileIndex`/`mileSplits`), but the drift counter freezes. Worse: outside work phases `paceZone` is always `.onTarget`, so any crossing during recovery/warmup that DOES reach `noteMileBand` resets `milesAdrift` TOWARD ZERO — actively working against the rare crossings that do count. Net: for exactly the sessions a pace-based bail exists to protect (can't hold target pace rep after rep), the counter needed to ever reach 2 is starved. **Same lesson as F066** — a display-suppression branch (`allowSplitFlash`) accidentally also gating safety-relevant accounting, not the specific `planComplete` branch this time but the identical shape. Not yet render-verified — no existing `_SessionSim.swift` archetype pairs a `metric:"pace"` bail with a multi-mile work rep (closest is "threshold", 2x3mi work reps, but zero rules attached); reviewer correctly did not write a new test archetype themselves (product/test code, outside their remit), precise repro recipe is in the finding file. Rest of the file confirmed clean in the same full read: no other early-return shape skipping safety accounting, Rule 16/11 both honored deliberately, no discarded-write pattern (file makes no network calls). One minor likely-intentional item noted, not filed separately: `hrOverCeiling` has no hysteresis band, comment argues deliberately "not sticky." | **Open, high priority — safety-relevant.** Fix mirrors F066 exactly: call `noteMileBand` unconditionally alongside the already-unconditional `mileSplits`/`lastMileIndex` bookkeeping, keep `allowSplitFlash` purely as the display gate. Reviewer recommends a new "pacebail" harness archetype as part of the fix, same pattern as racebail/thresholdbail, so this gets a permanent regression check the way F066's mechanism now does. | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 032) |
| F111 | **High — third instance of the same shape in one night, this one is SPOKEN not just displayed** | Design review, finding 033, full read of `WatchRouterV5.swift`: `recordSplit()` (updates `lastSplitSec`, the baseline for "N sec quicker/slower" comparisons) only fires inside the drawable `.split` moment case — which requires `pendingQuestion == nil` (no bail/ceiling-override/low-battery/GPS question pending), among other gates. Per the file's own comment, a pending question "outranks everything" and can sit up for minutes. So if a bail question is up when a mile passes, `lastSplitSec` freezes; the NEXT drawn split compares against a stale baseline miles old, with no staleness indicator — and this gets SPOKEN via `SpokenCues`, not just displayed. Same shape as F066/F110: display-suppression gating something meant to update unconditionally. `offerBailIfDue()` in this same file is the correct counter-example (observes engine state directly via `onChange`, decoupled from routing) — the right pattern already exists in the file, just not applied here. | **Open, high priority.** Fix: decouple `lastSplitSec` update from the drawable-moment gate, same as `offerBailIfDue()`'s pattern. **Given this is the THIRD instance of "accumulator wired behind a display-suppression condition" found in one night (F066, F110, this)**, reviewer recommends the code agent do a project-wide grep/review for this exact shape across the Watch codebase, same treatment `027`/F100 got on the phone side, rather than fixing each instance as a one-off as more turn up. | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 033a) |
| F112 | Medium (real, cheap fix, a regression of an already-fixed bug) | Design review, finding 033, same `WatchRouterV5.swift` read: `controlsHeader` (warm-up/recovery/cooldown/Just-Run) hardcodes "Mile N" from raw `tracker.distanceMi` with zero km conversion — and the SAME FILE's `raceMileLabel` has a comment literally describing this exact defect as already fixed once elsewhere ("A metric runner nine miles into a marathon was told 'Km 10' when they had covered 14.5... one board over and still standing after that one was fixed"). This is that still-standing board. | Open, cheap fix — route `controlsHeader` through the same `WFmt` helper `raceMileLabel` already uses | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 033b) |
| F114 | Low-Medium (infrastructure/tooling integrity, not app code — but explains real observed flakiness tonight and could produce a false "passed" on a genuinely broken change if unlucky) | Code-agent lead, live during F056/F057's merge-verification: running `xcodebuild test` against the SAME booted simulator (`Faff-Review-1`) from two independent worktree processes concurrently produces cross-contaminated results — a test run scoped to one worktree's `FaffTests` target reported a test case (`InjuryFlareTitleWiringTests`, from a completely different worktree's F075 branch, confirmed absent from the reporting worktree's own git history and source tree) as running and failing. Confirmed not a DerivedData-folder collision (each worktree's DerivedData is correctly isolated by path, verified via `info.plist`'s `WorkspacePath`) — the contamination happens at the shared simulator's XCTest daemon/test-manager level when two `xcodebuild test` processes target the identical simulator UDID at once. Reproduced the mechanism directly: identical test invocation against the SAME worktree gave TEST FAILED (with the phantom cross-worktree test case), then TEST SUCCEEDED, then TEST FAILED again, purely as a function of whether another agent's concurrent Xcode test run was active against the same simulator at that moment — resolved cleanly (multiple consecutive clean runs) once confirmed no other `xcodebuild`/`xctest` process was running. F075's own agent independently and separately reported "the simulator turned out to be under concurrent use by another session (confirmed via device logs)" during its own verification pass the same night — independent corroboration, not a single observer's guess. **Retrospective concern, not yet investigated**: some of tonight's earlier "flaky, resolved by retry" observations (e.g. the watch-gate board-render flake noted in F063/F074's evidence) may share this same root mechanism rather than being Railway-side or render-timing flakiness as assumed at the time — worth a look, not urgent, since retries did resolve them and no wrong verdict is known to have shipped. **Real risk this poses going forward**: a genuinely broken change could show a clean "TEST SUCCEEDED" if a concurrent process's passing results happen to paper over the real failure, or (less likely but possible) a genuinely correct change could show a false failure, in either direction depending on timing luck — a merge/RR-submission decision made from a single xcodebuild run during a period of known concurrent Xcode/simulator activity should not be fully trusted without a confirming re-run once contention is cleared. | Open, low urgency — worth a standing rule addition (check `ps aux \| grep xcodebuild` / `xcrun simctl list devices booted` for contention before trusting a single native test run's verdict, especially right before a merge decision) and, longer-term, worth exploring whether per-worktree simulator assignment or explicit `-derivedDataPath` isolation actually prevents the daemon-level cross-talk (untested — this finding only diagnoses the mechanism, doesn't yet confirm a structural fix) | — | Code-agent lead, live discovery during F056/F057 merge processing, 2026-09-14/15; cross-referenced independently by F075's own implementer report (`programme-internal-working/00-master-programme/F075-INJURY-SEVERITY-TITLE-WIRING-2026-09-14.md`) |
| F113 | Medium (real, structural, likely upstream of just the watch — needs root-cause tracing, not a quick widget fix) | Design review, finding 034, full read of `FaffWatch Widgets/` (complications + Smart Stack, 7 files, 995 lines): no taper concept exists anywhere in the pipeline feeding the watch face — full chain traced (`SessionClass`, `V5LobbyRamp`, `PhoneSync.writeWidgetSnapshot`), grep for "taper" across the watch app + `build-workout.ts` returns nothing. A taper-week easy run reaches the watch face pixel-identical to an ordinary easy day. Important, per the reviewer's own careful read: this isn't introduced by the widget files themselves — they correctly pass through the same vocabulary the phone lobby already uses (one source of truth, arguably the right call) — the gap is upstream, in whether `SessionClass`/`V5LobbyRamp` ever distinguish taper at all. Needs a follow-up read of `web-v2/lib/plan`'s naming/classification logic to root-cause whether this is a system-wide missing distinction or exists elsewhere and just doesn't reach watch-face vocabulary. Two minor items noted, not filed separately: the circular complication (most-glanced size) has the weakest staleness signal of the three sizes (just a dimmer number, no age kicker/curve text its siblings have); a `circularParts` split heuristic contradicts its own documented example, currently unreachable since the dose vocabulary never produces a pace string yet (latent, flagged for whenever one's added). Positive findings: no discarded-write pattern (no network calls, synchronous App-Group snapshot read), the display-suppression-gates-accumulation shape from F066/F110/F111 is genuinely absent here (`WidgetCenter.reloadAllTimelines` fires unconditionally on content change), Rule 16/17 both clean, tone clean. One item already self-flagged in-source by the original authors (`FaffSmartStackView.swift:41-42`, "NEEDS DEVICE CONFIRMATION") — not a new finding, just worth closing on the next render pass. **Root-caused, not speculative.** `classifySession` (`execution-semantics.ts:141-171`), the actual function producing `SessionClass`, is entirely shape-based (interval/threshold/long/easy/race/rest) from a day's own type/spec — no parameter carries which WEEK of the block a day sits in, so there's no way for it to know it's inside a taper. Taper context exists elsewhere (`BLOCK_SHAPE[cat].taperWeeks`, the same constant Rule 8's `normal-window.ts` exclusion already uses) but is never threaded into this per-day classification at all. Confirmed structural: this isn't a missing enum case in an otherwise week-aware function — the function has no week-awareness to extend in the first place. Confirms the gap is upstream of the watch layer entirely, not fixable at the widget/complication level. | Open, medium priority — real implementation work: thread week-position context into `classifySession` (or a wrapper) so taper can be distinguished at the point of session classification, then let that flow down to the watch face vocabulary that already correctly passes through whatever it's given | — | `for design review/SETTINGS-AUDIT-2026-09-14.md` (finding 034) |
| F115 | Medium-High (real, Rule 25's shape, argued explicitly not assumed — a case where the substitution can be larger than F103's) | Coach consultant, Rule 25 sweep of coaching/pace logic: onboarding's effort-pace field ("Pace for 20 minutes hard") is exactly this shape. The runner types a specific pace; the engine correctly treats it as lower-confidence evidence and shrinks it toward a mileage-based prior (verified against F074's work — the shrinkage math itself is doctrine-correct, continuous, properly gated). But nothing on any screen ever discloses that the actual prescribed paces come from a shrunk version of what the runner typed, or by how much — grepped every Swift view for any reference to the runner's own effort-pace value past the onboarding input itself, zero hits. Explicitly argued why this counts as Rule 25's shape and isn't just "normal evidence blending, don't worry about it": Rule 25's own text says defensibility doesn't excuse silence ("regardless of which specific number is involved or how small the rounding looks from the engine's side") — and this substitution can be LARGER than F103's, at the exact moment a runner stated something concrete and specific about their own body. Checked and clean, worth keeping on record: goal time vs. race-day target looked like the same shape at first glance but isn't — `TodayBeforeV5.swift` already shows both numbers side by side, separately labeled, fixed 2026-09-03 with an explicit Rule 16 citation; disclosure already satisfied before Rule 25 existed. | Open — not proposing a fix yet, same posture as F103 itself (worth more investigation than one quick idea, per David's own framing) | — | `for coaching consult/consult-log/2026-09-15-026-rule25-sweep-pace-goal-inputs.md` |

## Adding a finding

New findings get the next `F0NN` number in sequence, regardless of which review round found them.
Never reuse a number. Create `ER-20260914-F0NN.md` for anything needing more than a one-line
summary; the table row always gets updated regardless.
