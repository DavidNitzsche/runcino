# Product opportunities register

This is the companion to `00-register.md` for a different kind of finding. The findings register is
for defects — something is broken, wrong, contradictory, or violates a stated rule. This register is
for the other kind of observation a review can produce: something isn't broken, it just doesn't exist
yet, or exists in a form that under-serves the runner, and a reviewer argued concretely for building or
changing it. Never mix the two — a defect that happens to suggest a nice-to-have stays in the findings
register; an idea that isn't grounded in something a report actually observed or argued does not belong
here at all.

Every opportunity gets a stable `PO-NNN` ID for life, same discipline as the findings register: never
delete a row, never reuse a number, update disposition in place if a decision is made or work starts.
Every entry must be grounded in something a real report actually observed, measured, or argued from the
codebase or doctrine — no vague aspirational language, no idea that isn't traceable to a specific piece
of evidence. If an entry can't cite a concrete source, it doesn't belong here.

Each row carries: the current limitation (what's missing or under-serving today, with evidence), the
runner value (why this matters to the actual runner, not just "would be nice"), the proposed experience
(concretely what it would look or feel like), supporting evidence/reasoning (the specific report and any
code/doctrine it cites), a rough complexity estimate, the real risk if built carelessly, a recommended
priority, and what decision (if any) is needed from David before work starts. Some entries need no
decision — "build it" is a complete answer. Most genuine product ideas do need one, since they involve a
real tradeoff or a scope call only David can make.

| ID | Current limitation | Runner value | Proposed experience | Evidence/reasoning | Complexity | Risk | Recommended priority | Decision needed from David |
|---|---|---|---|---|---|---|---|---|
| PO-001 | Nothing in the engine detects repeated PARTIAL non-adherence — a runner skipping every quality session for six weeks while still logging easy miles trips nothing. Only two mechanisms exist at the extremes: a stateless single-miss handler (`detectMissedKeyWorkout`, 7-day rolling window, one trigger per pass) and a total-layoff detector keyed on consecutive zero-run days (`runnerIsCompromised`/`detectTrainingGap`, banded 4-7/8-14/>14 days). Nothing counts or windows the space between those two extremes. Deeper blocker: three separate, disagreeing implementations of "how consistent is this runner" exist (`web-v2/lib/adaptation/adaptation-model.ts`, `week-mileage.ts`, `runner-calibration.ts`), none promoted to a canonical reader any surface can call — so there's no signal to build a feature on yet, not just no feature. | A runner who has quietly stopped doing quality work, or is chronically missing one session type, currently gets the plan adapting around that pattern indefinitely with no moment where the coach names it. This is exactly the doctrine-warned failure mode (Coaching Doctrine Brief 10, "life happens") of a coach that never notices behavioral drift and just keeps re-fitting the plan to whatever is actually happening, silently. | A canonical "training consistency" reading — promoted from one of the three existing implementations, per the Brain Constitution's "one question, one owner" rule, not a new fourth engine — feeding a real recommit/recalibrate moment when a runner crosses a named threshold of repeated partial non-adherence. Not a plan that silently reshapes around the miss (today's behavior), but an explicit coach moment: name the pattern, offer a real choice — recommit to the plan's current demands, or recalibrate it down to match demonstrated behavior. | IPR-20260914-006 §3/§7; `docs/PRODUCT_ROADMAP.md` Priority 5 ("Repeated non-adherence triggers recommitment or recalibration"); `web-v2/lib/runner-state/ownership.ts` `TRAINING_CONSISTENCY` entry (`verdict: 'OPEN'`); `web-v2/lib/plan/adapt.ts` (`detectMissedKeyWorkout` lines 2840-3008, `runnerIsCompromised`/`detectTrainingGap` lines 1570-1578/3036-3101). | Medium-Large — consolidating the 3 disagreeing consistency readers into one canonical resolver is a real prerequisite, not optional; the detection window/threshold and the recommit/recalibrate UX are built on top of that, not before it. | Low technical risk once consolidated. The real risk is product risk: getting the threshold (how much partial non-adherence, over what window, triggers the prompt) and the recommit-vs-recalibrate framing right, since a poorly-tuned version could nag a runner going through a genuinely hard patch, or stay silent too long. | Already correctly sequenced as Roadmap Priority 5 — not urgent ahead of Priorities 0-4. Investigate/consolidate the 3 consistency readers first, per IPR-006's own recommendation, before any feature-level scoping. | No decision needed yet — this is an investigate-then-decide item. Once the consolidation is done, the threshold/framing choice will need a real product call. |
| PO-002 | V5 onboarding's `submit()` sends `connectionsSkipped: true` unconditionally (`native-v2/Faff/Faff/ViewsV5/HostsV5.swift:4163`) — the current 5-step onboarding deck has no step offering to connect HealthKit or Strava at all. A stale comment (`native-v2/Faff/Faff/FaffApp.swift:325-329`) still describes the legacy flow's HealthKit-auth step as if it were current, meaning the app's own source comments now misdescribe its own onboarding. | A new runner with real HealthKit/Strava history has measured training data (actual weekly mileage, actual easy pace) sitting one permission prompt away — data that, combined with F074's fix wiring self-reported evidence into the plan, could give a day-one plan a much stronger, measured evidentiary basis than the fitness-step questions alone can ever provide. Today that history sits unused unless the runner separately discovers a connect option in Settings after onboarding is already done. | Either reintroduce a connect-a-source step into the V5 onboarding deck (closer to the design contract's original "9a" flow), or make an explicit, considered decision that connecting happens post-onboarding in Settings — and if so, delete the stale `FaffApp.swift` comment rather than leaving it describing a step that no longer exists. | IPR-20260914-005 §3 ("Smaller, related findings in the same pass"); `native-v2/Faff/Faff/ViewsV5/HostsV5.swift:4163`; `native-v2/Faff/Faff/FaffApp.swift:325-329`. | Small-Medium — a connect-flow UI likely already exists from the legacy onboarding and would need porting/restyling to V5's visual language, not building from scratch. | Low — purely additive. The only real risk is scope creep into a broader onboarding redesign if this isn't kept narrow (one step, reusing existing connect logic). | Medium — complements F074's evidence-wiring fixes; without a connect step, F074's improvements only help runners who type numbers in, not runners who already have real device history the app could read directly. | Yes — whether a connect-a-source step belongs in onboarding at all, or is deliberately deferred to Settings. If deferred deliberately, the stale comment should be deleted as part of that decision, not left behind. |
| PO-003 | Today's Readiness row shows a bare, unconditional "64" (F081/IPR-20260914-008) — a confirmed UX Simplification Doctrine violation, already scheduled for removal as a defect fix. But deleting the number outright doesn't answer a real design question the same report raises: readiness may be the one quantity a runner most wants a fast, precise read on before deciding how hard to push a run, and a bare state word could under-serve that decision if done without thought. | A runner deciding "how hard should I go today" benefits from a fast, legible readiness signal. Replacing the number with nothing more than a state word (e.g. "READY") loses granularity a number carries; removing the number as a pure defect fix, without a considered replacement, risks under-serving the exact moment the doctrine itself says Today must answer ("how should it feel?"). | A state-word-first readiness row — matching the pattern the app already uses correctly for training load ("Moderate," not a number) — with the existing tap-to-expand pillar breakdown (sleep/RHR/HRV/load) staying as the drill-down for a runner who wants more. If David wants readiness to keep a numeric read, that should be a deliberate, explicitly-recorded doctrine exception specific to readiness (distinguished from confidence decimals/evidence counts/fitness sub-scores, which this same report confirms are correctly never shown as raw numbers anywhere else in the app), not a silent default. | IPR-20260914-008 §4 (counterargument) and §9; `docs/PRODUCT_UX_SIMPLIFICATION_DOCTRINE.md`; `native-v2/Faff/Faff/ViewsV5/TodayBeforeV5.swift:741-765` (`whereYouAreSection`). | Small — this is primarily a design decision plus a small render change; a state-word system doesn't yet exist anywhere in `ViewsV5`/`DesignV5` and would need building once as a small, reusable component. | Low. | Should be decided alongside — not after — the already-dispatched F081 fix, so the bare-number removal doesn't ship a worse experience by accident that then needs a second pass. | Yes — whether readiness gets a deliberate numeric exception or the app's standard state-word treatment. Per the report, this should be a stated choice recorded in doctrine, not something that "fixes itself" as a side effect of the bare-number removal. |
| PO-004 | `native-v2/Faff/Faff/ViewsV5/SettingsV5.swift:433-476` exposes exactly 2 notification toggles ("Skipped-run check," "Weekly summary") against a backend that supports roughly 8 real categories (IPR-20260914-009). The other 6 — including the two most doctrine-questionable ones, `race_countdown` and `run_unread` (see F092) — default ON with zero phone-side control at all. | Whatever gets decided about F092's doctrine question (are `weekly_checkin`/`race_countdown`/`run_unread` deliberate exceptions to the "reserved for consequential changes" rule), a runner today has no way to individually opt out of any of the 6 currently-uncontrolled categories regardless of the answer — the choice is "the 2 exposed toggles" or "everything else, forced on." | A real per-category notification preferences screen (or an expansion of the existing Settings notification section) exposing all ~8 categories individually with their own on/off control, rather than 2 curated toggles standing in for 8 distinct backend behaviors. | IPR-20260914-009 §3 ("Settings exposure — real, but partial"); `native-v2/Faff/Faff/ViewsV5/SettingsV5.swift:433-476`; `web-v2/lib/notifications/prefs.ts:74-226` (the existing preference-gating mechanism `dispatchNotification` already checks). | Small-Medium — the backend preference-gating mechanism already exists and is real; this is primarily a Settings UI expansion plus wiring the existing gate to more categories, not new notification infrastructure. | Low. | Should follow, not precede, F092's doctrine decision — no value in building fine-grained controls for categories that might instead get removed or restructured by that decision. | Depends on F092's outcome. If any of the 3 doctrine-questionable categories are kept as deliberate product choices, this is the natural way to make that choice runner-controllable instead of silently forced. |
